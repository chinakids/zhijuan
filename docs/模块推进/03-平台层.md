# 织卷 · 平台层（数据/采集/工程）推进档案

> 本档案是该模块推进器的**规划权威源**。每轮开工先读「五、下一步计划」；收工必须更新「四、迭代日志」与「五、下一步计划」。
> 四阶段契约见 `docs/模块推进/README.md`。回执 ≤80 字三选一，详细只写本档案。

## 一、模块定位与边界

- 覆盖：首页项目管理（新建/导入/列表）、工作区外壳（侧栏四大板块定位）、素材库页面与**联网采集管道**（任务卡 front matter 约定 + 本机管道 cron 76de7e949a3b 回填）、设置页（模型厂商/引擎开关/主题）、代码工程面（store/settings/slices 等数据层、路径约定、构建/打包、项目模板、依赖与依赖治理）。
- 权威源章节：模块设计-V2 §四（A）/§五（B）/§九（F）/§十一（H）/§十二（I）、§十四、架构评审 §二（store 上帝文件）。
- 边界外：编辑器与设定内容（创作层）、agent 能力（智能层）、视觉细节（体验层）。采集管道 cron 是采集系统本体，推进器只改织卷侧（表单/任务卡/回填逻辑）。

## 二、当前状态（截至 2026-09-16 01:5x）

- ✅ **smoke-ui 门禁判据回归资产化（2026-09-16 01:30 轮）**：`scripts/smoke-gate-check.mjs` 自检脚本落地（变异测试思想——自起「旧构建/过期构建」变异体，断言两级判据必拦、恢复后必过；4/4 实跑×2）；smoke-ui `--all` 收集排除该元测试；基线文档注记。
- ✅ **项目导出（2026-09-13 04:30 轮）**：首页卡片「更多操作」→「导出到…」——复制项目目录到选定位置（跳过 .git/.DS_Store/node_modules，与导入同 skip；同名不覆盖；目标不得是项目自身/内部；导出物可直接再导入，round-trip 冒烟 15/15 + UI 冒烟 5/5 + 截图）。设计 §四 A 菜单「打开目录 / 导出 / 删除」至此三项齐。
- ✅ **系统菜单（2026-09-14 01:30 第一刀 0fdad90 / 04:30 第二刀 f08c96b）**：mac 菜单栏六菜单（中文 label/role/accelerator 按 docs/系统菜单-设计口径.md）+ 10 个自定义动作通道：第一刀=主进程模板+分发+preload 桥；第二刀=渲染层单点分发（App.tsx MenuBridge switch→CustomEvent 派发）+ 启用态上报禁用（渲染层 route/editor → ipcMain 'menu:state' → applyMenuState 按口径灰显）+ 全局设置页路由 #/settings + 双触发时间窗去重；menu-template-smoke 20/20 + menu-actions-ui-smoke 9/9 + 截图两张。(g) 真机展开过目＝锁屏后（候选 3 ③）。
- ✅ **fs 事件消费原语（2026-09-12 第 20 轮，19b01d4）**：`features/fs/useFsEvents.ts` 新增 `useFsChanged(projectId, prefix, onChange)`——按事件引用追踪已消费位，批内全部新事件都会检查、已消费不重触发；Novel/Outline/DocSection/LibraryBrowser/CollectionBar 五处统一迁移，根治「events[events.length-1] 只看末条」在 React 18 批处理下的漏刷新（CollectionBar 18dda8c 同机制实锤；负向验证旧逻辑冒烟必挂）；新增 fs-batch-ui-smoke 7 断言。
- ✅ **devShim 口径收口（2026-09-12 第 21 轮，638e8f9）**：createProject/importProject 的 id 规则抽 `shared/projects.nextProjectId`（sanitizeFile+重名后缀）真机/devShim 双挂；devShim importProject 对齐 id=basename（去 demo- 前缀）、空路径报错、name=id/description=''；listSlices.updatedAt 从硬编码 0 改用 docsOf mtime（devMtime 模拟）；getPaths.defaultLibrary 核实渲染层零消费（保持 '' 并注释结论）。
- ✅ S1 项目骨架/首页；S5 素材库 + 采集管道（真实任务「校园图书馆场景」→ 素材卡《校园老图书馆》落库，文件系统实锤）；S6 检查阵容的素材升格助手（属智能层但落位素材库页）。
- ✅ **素材库页（2026-09-10，0d1b7f3）**：类别树（左列、自由建类别）+ 文件名/全文搜索（AND、排除采集池）+ 素材卡列表（预览/标签），替换原平铺 DocSection；新增 main/library.ts 域逻辑。
- ✅ **采集任务卡（2026-09-10，1b0a630）**：停滞提示（非终态且 mtime 超 2 天未动 → 列表「停滞 N 天」徽标 + 详情提示，文案指引删除重发）+ 发起采集提交查重（同需求进行中任务 → 表单警告不阻止）。
- ✅ **任务卡「删除/重发」入口（2026-09-10 22:32 轮）**：详情底部「删除该任务」（确认框，非终态警示文案；走系统废纸篓可恢复）＋「重发任务」（仅 pending/failed；重建全新 pending 卡，清旧结果/完成，mtime 随写盘更新 → 管道重新处理、停滞徽标随之消失）。
- ✅ D3：settings 独立成 main/settings.ts（settings/workspaceDir/libraryRoot 搬家），store 过渡期 re-export 保持 IPC 兼容。
- ✅ **数据层体检（2026-09-09 完成）**：store.ts 已从评审时的 369 行/17 export 降至 **269 行/14 export**——D3 拆出 settings 后，本轮再把「工作区文档域」拆到 `main/workspace.ts`（workspaceStatus/ensureWorkspaceDocs/listWorkspaceDocs/readWorkspaceDoc），store 只剩「项目 + 文件 + watcher」+ 骨架模板，对齐评审 §二.1 目标形态；IPC 通道名与行为不变（ipc.ts 改 import './workspace'）。后续扩展（厂商字段、路径规则）继续不碰 store。
- ✅ **⌘K 素材搜索细节（2026-09-11 本轮）**：q 为空时「最近素材」建议组（最近修改 5 条 + 相对时间，点击直达）；全文搜索命中行显示类别路径前缀；域逻辑 recentLibraryDocs（main/library.ts）与 shared 纯函数（libraryCategoryOf/formatRelativeTime）均可单测。
- ✅ **导入已有目录收尾（2026-09-11 本轮，dcea302）**：修复旧「只建同名空壳、外部内容丢失」假导入——现在真复制入库（cpSync 跳过 .git/.DS_Store/node_modules，原目录不动）+ 系统目录选择器（project:importPicker）+ 结构化结果反馈（ImportResult：成功/库内已有同名(info)/失败(error)）；单测 4 例 + 数据层冒烟 22 项 + 无头 UI 冒烟 4 项全绿。
- ✅ **快捷键三处同源体检与强制（2026-09-14 19:30 轮）**：menu mac 18 项 accelerator×docs/快捷键.md 逐项对表全一致（菜单零缺陷）；docs 措辞修正（「全部组合」精确化＋补 Milkdown 继承键位与 ⌘E 遮蔽注＋win 档说明）、ShortcutHelp 补 Esc 行（三处同源收齐）；menu-template-smoke §6 新增 menu↔docs **双向一致性检查**（漂移即 FAIL，负向验证实锤）——快捷键可发现性从口头约定升级为回归防线。提交 94f0f41。
- ✅ **编辑器快捷键真实按下生效实测（2026-09-14 22:30 轮 9ba8507 第一波 13 断言 + 2026-09-15 01:30 轮 30dd91c 第二波 31 断言，共 44 断言 ALL PASS）**：`scripts/shortcut-editor-live-smoke.mjs`＝CDP 浏览器级输入（isTrusted）驱动已挂载 Milkdown 编辑器——⌘B/⌘I/⌘Z/⌘Y/⇧⌘Z/⇧⌘B/⌥⌘1/⌥⌘2..6/⌥⌘0/⌘E 遮蔽/⌘F/⌘A/⌘G/⇧⌘G/Tab/⇧Tab 列表缩进全部「真实按下」生效；docs「Milkdown 内置」键位从文档承诺升级为实测回归资产。第二波新增 ProseApi.setCursor（测试用光标定位），断言面含 Tab 下沉 `* 甲\n  * 乙\n* 丙`/⇧Tab 回升、⌘G current 0→1→0 实跳、⌘A getSelected=全文；零 JS 异常。
- ✅ **冒烟环境契约收口（2026-09-15 07:30 轮，c39a02d）**：107 个冒烟脚本 BASE 硬编码端口统一支持 `ZJ_SMOKE_BASE` env 覆盖（8123/8899 字面量 + PORT + SPA 常量全收）；focus-a11y tab 查找跟随 BASE 端口；51 处过时头注释「python3 -m http.server 8123」+ 14 处 spa_server.py 注释归一为「node scripts/serve-renderer.mjs <port>」；新端口 8133 实跑 home-stats/collection-e2e/menu-actions/states 四冒烟全绿（换端口即挂根治）。
- ✅ **冒烟一键回归入口（2026-09-15 10:30 轮，115b9cd）**：`scripts/smoke-ui.mjs`——145 个 smoke 脚本统一运行器（`<name>` 子串匹配 / `--all` / `--live` 真模型 / `--list` 预检 / `--timeout` / `-x` 快停 / `-v` 透传）+ 逐脚本 BASE/CDP 探活预检（端口未起→清晰报错不执行，根治裸超时/假绿；8810 zj-bridge 为可选环境 SKIP 不计失败）；退出码=FAIL 数；`npm run smoke:ui` 入口；实跑 states-smoke 10.8s PASS + devshim-consistency 2.0s PASS + 负向探针/超时/-x 全实证；--all 预检 145 个 0.6s（143✓+2 SKIP）。全量首跑=下一步候选 1。
- 预留落点（未做，需要时再做）：`.zhijuan/index/`（全文检索索引）、`.zhijuan/tasks/`（采集任务状态机）、`.zhijuan/search/`、配置导出。

## 三、长远规划

- 工程红线：shared 纯约定（无 fs）、main 域逻辑、纯逻辑可单测；结构化可增长数据一律进 `.zhijuan/`；数据纯本地明文可入 git；不引重量级依赖。
- M3 菜单（体验与稳定，攒着别急）：项目模板、多章连写、正文版本历史、打包可分发的 .app；embedding 增强召回（可选，数据量上来再评估）。

## 四、迭代日志

（每轮按四阶段追加：日期时间 | 调研来源 | 规划要点 | 提交号 | 验证 | 下一步）

### 2026-09-15 10:30–11:0x（本轮：候选 1——冒烟一键回归入口 + BASE 探活预检；提交 115b9cd）

- **开工第 0 步**：`git status` 工作树干净；近 90 分钟提交＝f6dc1d8/228a0dd（创作层 09:45 docs+feat 批注接受 toast 建档）、5099627/0afee61（智能层 09:00）、ca4deec（体验层 08:15 docs），与本轮目标（scripts/smoke-ui.mjs 新增 / package.json / 档案）零交集；`docs/反馈清单.md` 无平台层「待落实/落实中」条目。环境核实：`pmset -g assertions` UserIsActive=0＝**仍锁屏**（候选 3 第 22 次核实受阻）；`hermes cron list` 采集管道 76de7e949a3b **第 22 次确认仍缺**（候选 2 维持待主会话）；CDP 9224（Chrome 152.0.7977.84）就绪；8123（python http.server→out/renderer，6:48 起）与 8899（`node scripts/serve-renderer.mjs 8899`）均在监听。
- **调研**（真实来源+仓库盘点）：① npm-run-all 官方 README（api.github.com/repos/mysticatea/npm-run-all/contents/README.md 直抓）：「官方 `npm run-script` 不能运行多个脚本…Let's shorten it by glob-like patterns」＋跨平台 `&` 在 cmd.exe 不可靠——**多个脚本聚合运行是生态公认缺口**，自研零依赖入口有其位置（不引 npm-run-all，工程红线）；② Playwright test-cli 文档（playwright.dev/docs/test-cli，curl 直抓）：`--list`＝收集报告所有测试但不运行（可发现性入口）；`--max-failures <N>`/`-x`＝失败即停（可选 fail-fast）——均被本轮采纳为 `--list`/`-x` 语义先例；③ 仓库盘点（候选定义即资产核验）：**scripts/ 实测 168 个 .mjs＝145 个文件名含 smoke ＋ 11 个 *-live（真模型长跑）＋ 12 个辅助**（serve-renderer/build-plugins/cdp-*/electron-stub/think-probe/zj-bridge/migrate-slice-state/nameform-shot/sync-retry-shots）；BASE 形态 5 种全收：`ZJ_SMOKE_BASE || 'http://localhost:8123'`×90、`127.0.0.1:8123`×9、`localhost:8899`×5、`127.0.0.1:8899`×3、`const PORT=N`+模板串 `${PORT}`×5（focus-a11y=8899，其余 8123）；另有 agent-cancel-live 两脚本依赖 8810（zj-bridge 真引擎桥）——需特判为可选环境；122/145 smoke 自带 `process.exit` 非 0 语义（退出码可作判据）。
- **产品规划**：为什么现在做＝候选 1（07:30 轮指定「下一轮主做」）；145 个冒烟零统一入口，「没跑」与「跑过」无法一眼区分，发布前/大改后回归全靠推进器手动挑脚本；端口坑已收口（c39a02d 支持 ZJ_SMOKE_BASE 全覆盖）但「该起什么服务、起了没有」仍靠人记——裸超时/假绿风险仍在。怎么做＝新建 `scripts/smoke-ui.mjs`（~210 行零依赖）批量执行器：参数=脚本名列表（省前缀/后缀、子串唯一匹配）或 `--all`（145 smoke，`--live` 追加 11 个真模型）＋ `--list`（仅预检不运行，Playwright 先例）＋ `--timeout <s>`（默认不限）＋ `-x`（首个失败即停，Playwright 先例；默认失败继续=回归要全览）＋ `-v`（透传）；逐脚本**预检**＝正则提取 BASE 默认端口/const PORT/localhost 字面量 → fetch 探活（2s 超时）＋引用 9224 则探 CDP /json/version；必选端口不通→标 FAIL＋清晰提示「请先 `node scripts/serve-renderer.mjs <port>`…＋CDP 9224 就绪」且**不执行**（不再裸超时）；8810＝可选环境（zj-bridge 专属依赖，缺失 SKIP 不计失败，否则 --all 恒红 2 个）；跑完输出 PASS/SKIP/FAIL 清单＋退出码=FAIL 数。**产品/创作视角**：织卷正文/设定是纯明文、价值随写作时长线性增长，任何 agent 链路/采集/批注改动若引入静默回归（采集回填错路径、切片同步漏触发），损坏的是章节数据的完整性——正文一旦被错误改写/丢失是**创作资产损失**而非 bug 账单；一键入口把「维护者手工纪律+端口记忆」变成「一条命令 145 脚本全检」的可复现契约，是平台层对创作内容可靠性的工程对价（验证资产与正文资产同权，延续 07:30 轮立场并有可执行形态）。**不动**：任何 src/ 产品代码、各脚本断言、ZJ_SMOKE_BASE 规格、采集管道、CDP 端口；无 UI 状态变化（CLI 工具，不触发截图契约，与 13:30 轮 relpath 同理）。验收口径＝三道门＋对既有冒烟实跑（正向 states-smoke/devshim-consistency-smoke 真实执行通过＋负向 9999 端口探针→预检 FAIL 清晰报错不执行＋--timeout 1 超时被杀→FAIL(超时)＋-x 首个失败停止＋--all/--live 集合与预检正确）。涉及文件：scripts/smoke-ui.mjs（新）、package.json（+smoke:ui 入口）、本档案。
- **技术落地**：`scripts/smoke-ui.mjs`（211 行，node 24 fetch/AbortSignal.timeout；spawn(process.execPath) 子进程、cwd=repoRoot、stdout/stderr 内存收尾（失败打印尾部 2.5k）、`--all` 串行+进度 `[i/N]`）；resolveName 支持 scripts/ 前缀省略/`.mjs` 省略/子串唯一匹配（歧义报错列全）；collectPorts 3 层正则（BASE 字面量→`const PORT`+`${PORT}` 组装→正文 localhost:PORT 兜底）；OPTIONAL_PORTS={'8810'}；`package.json` scripts +`"smoke:ui": "node scripts/smoke-ui.mjs"`（`npm run smoke:ui -- --all` 可发现入口）。**期间实踩**：①`new URL(import.meta.url).pathname` 会把中文路径 URL 编码（织卷→%E7%BB%87%E5%8D%B7）→ readdir ENOENT——改 `fileURLToPath`＋dirname（根因修）；②--all 首预检 2 FAIL＝8810 必选化（agent-cancel 两脚本依赖 zj-bridge）→ 特判可选环境 SKIP（否则 --all 恒红，违背「一键全绿」目标）；③`statSync/basename` 未用 import 清理。
- **测试验收**（真实跑通）：三道门 typecheck ✅ / build ✅（3014 模块）/ npm test **72 文件 725 例全绿**；`node --check` 通过；功能实测：`--list states` 预检 ✓；**正/负向**：`--list --all`＝145 脚本 0.6s 全预检（143 ✓＋2 SKIP(8810)＋0 FAIL、退出码 0）；`--list --all --live`＝156（145+11 去重正确）；真实执行 `states-smoke` **10.8s PASS**、`devshim-consistency-smoke` **2.0s PASS**（8899 探活+执行链路）；**负向探针**（临时 zz-probe-tmp.mjs 指向 9999）＝预检 FAIL「服务未起：http://localhost:9999」＋正确提示「请先启动：node scripts/serve-renderer.mjs 9999…」、**未执行**（无 SHOULD_NOT_RUN 输出）、退出码 1；`--timeout 1 -x states-smoke devshim-consistency-smoke`＝states 1s 超时被杀→FAIL(超时)＋-x 停止、devshim 未跑、退出码 1；脚本域残留核查：无 8133/8723 过时端口字面量（无误报）、8810 仅 agent-cancel 系与桥接文件出现。**限**：CK 探活为端口级（服务在≠内容对，bundle 新旧/页面内容靠各脚本自身断言，登记观察）；全量首跑（145 个约 30-60min）留作下一候选；真壳面归候选 3。
- **落档**：候选 1 **完成 ✅**（一键入口+探活预检落地并实证）；登记观察 3 项（探活粒度/带 smoke 的 live 系混跑/8810 常量）；候选 2 第 22 次核实仍缺（待主会话）、候选 3 第 22 次仍锁屏；下一步计划重写（候选 1＝全量首跑基线落档）。

**提交**：115b9cd — feat(平台层): 冒烟一键回归入口 smoke-ui.mjs——145 个 smoke 脚本统一运行器+逐脚本 BASE/CDP 探活预检（端口未起清晰报错不裸超时；8810 zj-bridge 为可选环境 SKIP 不计失败）；--list 预检/-x 快停/--timeout/--live 真模型；npm run smoke:ui 入口

### 2026-09-15 07:30–08:0x（本轮：候选 1——无头冒烟环境契约体检收口；提交 c39a02d）

- **开工第 0 步**：`git status` 工作树干净；近 90 分钟提交＝87bfca1/dc94b21（创作层 06:45）、3310494/761f24f（智能层 06:00——其中 761f24f 已把 history-empty-text/outline-history 两脚本 BASE 支持 ZJ_SMOKE_BASE，与本轮同模式，无文件交集）、de6cf99（体验层 docs），与本轮目标（scripts/*.mjs 环境契约）零交集；`docs/反馈清单.md` 无平台层「待落实/落实中」条目。环境核实：`pmset -g assertions` UserIsActive=0＝**仍锁屏**（候选 3 第 21 次核实受阻）；`hermes cron list` 采集管道 76de7e949a3b **第 21 次确认仍缺**（候选 2 维持待主会话）；本机 8123（python http.server 6:48 起）与 CDP 9224 就绪。
- **调研**（仓库内全量盘点，候选定义即资产核验；无外网项）：真实基线＝**107 个冒烟脚本**（scripts/ 共 163 个 .mjs）BASE 硬编码端口且无 env 覆盖——`const BASE = 'http://localhost:8123'`×82、`127.0.0.1:8123`×9、`localhost:8899`×3、`127.0.0.1:8899`×1、PORT 变体×5（focus-a11y=8899，icon-size/loading-indicator/modal-a11y/sidebar-v01=8123）——**远超 04:30 轮盘点的「13 个」**（当时低估；本轮按「完整模式集合」grep 实测 107，教训：同类盘点必须以模式全集 grep 而非抽查）；另有边角四类：home-singlebar 独立 `const SPA = 'http://127.0.0.1:8899'` 无覆盖；focus-a11y tab 查找硬编码 `includes(':8899')`（与 BASE 端口脱钩，换端口必找错 tab）；过时头注释「python3 -m http.server 8123 --directory out/renderer」**51 处**（本机常驻实际＝`node scripts/serve-renderer.mjs 8123`——SPA fallback＋线程化，python 对子路由 404）；spa_server.py 旧注释 **14 处**（仓库已无该文件，仅 /tmp 曾有）+ agent-cancel-live「python3 serve-renderer.mjs」笔误。
- **产品规划**：为什么现在做＝候选 1（04:30 轮自立）；「端口被占/换端口即挂」是推进器全体系多轮复踩坑（档案多轮「踩端口坑」记录），根因＝BASE 端口字面量硬编码＋头注释指引过时双份误导；本机同时存在 8123/8723/8899 历史服务器进程，任何端口起新服务旧脚本即挂。怎么做＝统一支持 `ZJ_SMOKE_BASE` env 覆盖（home-stats/libpath 先例规格：端口被占时用任意端口起 `node scripts/serve-renderer.mjs <port>` 后注入即可，零改脚本）＋头注释归一指向该权威脚本（含 SPA fallback 语义）；SPA 常量与 tab 查找跟随端口。**产品/创作视角**：这些冒烟资产是「agent 辅助写作」各链路（采集回填/批注/切片同步/菜单快捷键）的回归防线——防线自身不可盲跑（端口坑）时，「改动后一切如常」的主人信任就不成立；本轮把防线前置条件契约化，服务的是发布/迭代节奏下「每轮验证都可复现」的可靠性底座（创作项目长期积累，验证资产与正文资产同权）。**不动**：任何 src/ 产品代码、冒烟断言含义、采集管道、已有 8 个已支持 env 的脚本（保持先例规格）；无 UI 状态变化（不触发截图契约）。验收口径＝三道门＋新端口实跑 4 个代表性冒烟全绿＋零残留 grep。
- **技术落地**：批量替换（/tmp node 脚本，用完删）——5 种 BASE 字面量/PORT 变体/SPA 常量全部改 `process.env.ZJ_SMOKE_BASE || '<原值>'`；focus-a11y 第 11 行 `includes(':8899')`→`includes(':' + new URL(BASE).port)`；头注释「python3 -m http.server 8123 --directory out/renderer」→「node scripts/serve-renderer.mjs 8123」（51 处）＋spa_server.py 各写法归一（14 处）＋「python3 serve-renderer.mjs」笔误修；**107 文件 169 行，纯行内文本零逻辑改动**；残留核查＝旧注释 0 / 字面量 BASE 0 / spa_server 0 / includes(:8899) 0。
- **测试验收**（真实跑通）：`node --check` 107 文件全过；三道门 typecheck ✅ / build ✅（3012 模块）/ npm test **70 文件 703 例全绿**；**新端口实证**：`node scripts/serve-renderer.mjs 8133` 起服务后 `ZJ_SMOKE_BASE=http://localhost:8133` 实跑——**home-stats-ui-smoke 4/4**、**collection-e2e-ui-smoke 11 项 SMOKE ALL PASS**、**menu-actions-ui-smoke 16 项 ALL PASS（含 P6.5/P7.5 双触发）**、**states-smoke 11/11**——四冒烟在全新端口＋SPA fallback 服务下全绿，证明「换端口即挂」根治（此前仅 8123 可跑）；验证后 8133 临时服务已停。
- **落档**：候选 1 **完成 ✅**（实测范围 107 脚本；登记复盘：04:30 盘点「13 个」为低估，全量应为 107——后续同类盘点按模式集合 grep）；候选 2 第 21 次核实仍缺（待主会话）、候选 3 第 21 次仍锁屏；下一步计划重写（候选 1＝冒烟一键回归入口＋探活预检）。

### 2026-09-15 01:30–02:10（本轮：候选 1——编辑器快捷键实测第二波闭环；提交 30dd91c）

- **开工第 0 步**：`git status` 工作树干净；近 90 分钟提交＝d740756/e6e1072（创作层 00:45）、3432dc0/e9ccfad（智能层 00:00/03:00? 实为 00:00 轮）、e401e72（体验层 23:15 docs），与目标文件（scripts/shortcut-editor-live-smoke.mjs）零交集；`docs/反馈清单.md` 无平台层「待落实/落实中」条目；环境核实：`pmset -g assertions` UserIsActive=0＝**仍锁屏**（候选 3 第 19 次受阻）；`hermes cron list` 采集管道 76de7e949a3b **第 19 次仍缺**（候选 2 维持待主会话）；CDP 9224（Chrome 152.0.7977.84）就绪、`/tmp/spa_server.py`（8899）在跑、out/ 最新（index.html 01:14）。
- **调研**（主题=档案候选 1 第二波）：**外网真实来源**＝Apple HIG Keyboards 经 r.jina.ai 拿到原文（`r.jina.ai/https://developer.apple.com/design/human-interface-guidelines/keyboards`）：「Respect standard keyboard shortcuts…People expect each of the following standard keyboard shortcuts to perform the action listed in the table below」「Define custom keyboard shortcuts for only the most frequently used app-specific commands」——可预期性与克制定义，直接支撑「docs 承诺的按键必须逐键实测」；web_search 本次空返（既定）。**源码级键位确认**（node_modules/@milkdown/…，比 19:30 再进一层）：`preset-commonmark/lib/index.js`＝paragraphKeymap.TurnIntoText=**Mod-Alt-0**、headingKeymap.TurnIntoH1..H6=**Mod-Alt-1..6**、**listItemKeymap.SinkListItem=["Tab","Mod-]"] / LiftListItem=["Shift-Tab","Mod-["]**；`preset-gfm` 的 Tab/Shift-Tab＝**仅表格 NextCell/PrevCell**（priority 100）；commonmark＝[schema,inputRules,markInputRules,commands,keymap,plugins].flat()（keymap 聚合含 listItemKeymap，确认挂载）；**无 @milkdown/preset-list 包、无 plugin-indent use**（列表 schema/键位均由 commonmark 提供）。**产品/创作视角**：正文层级（标题 2-6/回正文）、全文定位（⌘A）、查找步进（⌘G/⇧⌘G）、列表结构（Tab 缩进）＝写作中手不离键盘的高频动作（HIG「惯用标准键行为预期」）；快捷键.md 对 Tab/⌥⌘N 的承诺此前零自动化实证，本轮逐键补齐，防「文档说支持、按下焦点跑掉」的心流断裂。
- **产品规划**：候选 1 落地＝扩 `scripts/shortcut-editor-live-smoke.mjs` 至 **31 断言**（T12/T13 ⌥⌘2..6、T14 ⌥⌘0、T15 ⌘A（keyDown 类型让浏览器默认全选生效）、T16 Tab/⇧Tab 列表缩进、T17 ⌘G/⇧⌘G 步进两处匹配）；**新增唯一源码点**＝ProseApi.setCursor(needle)（PM 层把光标钉到 doc 文本中 needle 起始处，测试/脚本用，生产零影响）；**不动**：编辑器行为、菜单、采集管道、win 模板、既有断言（T1-T11 原样保留=零回归验证）。
- **技术落地**：Prose.tsx 接口+实现各 1 处（setCursor 基于 doc.descendants + TextSelection.create，无匹配不动）；脚本 VK 表扩 a/Tab/0/2..6/ArrowDown/ArrowRight，press 支持 type='keyDown'；T16 断言兼容 Milkdown serializer 列表输出 `* `（非 `- `）。**期间实踩的测试驱动坑（已修）**：①setContent 注入后**光标在文档首（selFrom=0）**，End/ArrowDown 会落到 **trailing 空段**（`.use(trailing)` 补的），直接按 Tab 落在项外→keymap 不命中→**聚焦被浏览器移出编辑器**（一度误判为产品缺陷；Mod-]/Mod-[ 判别实验排除 keymap 未挂载）——用 setCursor('乙') 钉进列表项文本后 Tab 立即生效；②patch 双层转义把断言 regex 污染成 `\\n\\s`（码点 92,92,110）——python 按码点替换修复为 `\n\s`。
- **测试验收**（真实跑通）：**全部 31 断言 ALL PASS**（T12 ⌥⌘2..6 逐级 H2..H6、T14 ⌥⌘0 回正文、T15a/b ⌘A 全选（selFrom 0→selTo 11 + getSelected=全文）、T16 前置 setCursor 到「乙」项（selFrom=8）、**T16a Tab 下沉 `* 甲\n  * 乙\n* 丙` / T16b ⇧Tab 回升**、T17a total=2 → T17b ⌘G current 0→1（selFrom 1→8 实跳）→ T17c ⇧⌘G 回 1→0、T11 全程零 JS 异常）；三道门 typecheck ✅ / build ✅（3010 模块）/ npm test **68 文件 685 例全绿**（零回归）；ZJ_SHOT 截图 `~/Pictures/zhijuan/shortcut-editor-live-0206.png`（⌘F 查找条+高亮真实界面）。
- **落档**：候选 1 **完成 ✅**（快捷键实测两波闭环：13+31 断言，docs/快捷键.md 承诺的编辑器继承键位全部实证）；候选 2 第 19 次核实仍缺（待主会话）；候选 3 第 19 次仍锁屏；下一步计划重写（候选 1＝素材采集管道 e2e 资产履约性走查，候选 2 恢复 cron 维持）。

### 2026-09-14 22:30–22:5x（本轮：候选 1——编辑器内快捷键真实可用性无头实测闭环；提交 9ba8507）

- **开工第 0 步**：`git status` 工作树干净；近 90 分钟提交＝9d15867/39d4319（创作层 21:45）、94bf72b/bba5958（智能层 21:00）、ecfb939（体验层 20:15 docs），与本轮（仅新增冒烟脚本）零交集；`docs/反馈清单.md` 无归属平台层「待落实/落实中」条目；环境核实：`pmset -g assertions` UserIsActive=0＝**仍锁屏**（候选 3 第 18 次核实受阻）；`hermes cron list` **第 18 次确认**采集管道 76de7e949a3b 仍不在活动列表（6 个 active 无「素材库采集」；候选 2 维持待主会话）；`/tmp/spa_server.py`（8899）本轮首次发现未起（后台补起）、CDP 9224（Chrome 152.0.7977.84）就绪、out/ 为最新构建（index.html 22:03）。
- **调研**（主题=档案候选 1）：web_search 本次空返（后端无结果），沿 19:30 轮已归档真实来源（Apple HIG Keyboards URL／Electron #6731／安装源码）为权威；本轮再做**源码级命令面确认**（比 19:30 的「键位列表」更进一层）：`node_modules/@milkdown/preset-commonmark/lib/index.js`＝strong（Mod-b）/emphasis（Mod-i）绑定 `toggleMark`（空选区只置 storedMarks、零事务）、行内代码（Mod-e）命令体 `if (selection.empty) return false`、引用块（Mod-Shift-b）＝`wrapIn`、标题（Mod-Alt-1..6）＝`wrapInHeading`；`@milkdown/plugin-history/lib/index.js`＝undo `Mod-z`／redo `["Mod-y","Shift-Mod-z"]`；`node_modules/prosemirror-keymap` 依赖 `event.key+修饰符` 匹配（与键盘布局/code 无关）。**产品/创作视角**：docs/快捷键.md 声称的 Milkdown 继承键位此前**零自动化实证**——「文档承诺可用」与「实际按下生效」的差距是写作者信任面（误信 ⌘B 可用、按下无效=被迫切鼠标，打断心流；Apple HIG Keyboards 要求快捷键既可发现又行为可预期）——19:30 轮已闭环「可发现」（menu↔docs 双向），本轮补「可预期」（按下必生效）另一半。
- **产品规划**：候选 1（19:30 立）落地＝新增一个只用仓库既有无头套路的冒烟脚本，**零源码改动、零新依赖、零行为变更**；断言面＝docs 编辑节+备注继承键位中「Markdown 可检测」的组合（⌘B/⌘I 的 storedMark 包裹输入、⌘Z/⌘Y/⇧⌘Z 历史、⇧⌘B 引用块、⌥⌘1 标题）；⌘E 用「无选区/有选区两态」锁遮蔽行为（不产行内代码＋设查找词＋no-op）；输入走 CDP `Input.dispatchKeyEvent`/`Input.insertText`（浏览器真实输入管线、isTrusted=true，与 Puppeteer 同源）——若 PM keymap 不响应则按档案预案降级源码断言+登记观察，本轮实测**可行、无需降级**。**不动**：Prose.tsx/编辑器源码一行未动、menu.ts、采集管道、win 模板、既有 24+ 冒烟。
- **技术落地**：新增 `scripts/shortcut-editor-live-smoke.mjs`（206 行）——openTab/attach/evalUntil/ok 样板沿 menu-actions-ui-smoke；`press()`＝rawKeyDown+keyUp（modifiers 位掩码 Meta=4/Shift=8/Alt=1，key/code 手工指定——prosemirror-keymap 按 event.key 匹配，CDP 控制 key 参数绕开 mac ⌥ 组合出特殊字符的坑）；`insertText()`＝Input.insertText（PM beforeinput 路径）；断言面＝`api.getMarkdown()`（文档层可检测）+ `__ZJ_FIND.getState()`（selFrom/selTo/query/total，编辑器无选区/选区探针）。
- **测试验收**（真实跑通）：首跑 **13 项 ALL PASS**（T3/T4 storedMark 包裹实锤、T5 三键撤销重做、T6/T7 块级命令、T8/T9 ⌘E 两态遮蔽、T9c ⌘E 后 total=1 高亮、T10 ⌘F 实开查找条、T11 零 JS 异常）；T5 旁证：`setContent` 事务也入历史、⌘Z 可回编辑器初始文档（测试注入 API 行为，非缺陷）；ZJ_SHOT=1 二跑同 ALL PASS＋截图 `~/Pictures/zhijuan/shortcut-editor-live-2245.png`；三道门 typecheck ✅ / build ✅（3009 模块）/ npm test **66 文件 667 例全绿**；提交 9ba8507（仅 scripts 1 文件）。未测/观察：⌘A 全选、⌥⌘2..6 与 ⌥⌘0、⌘G/⇧⌘G 步进、Tab/⇧Tab 列表缩进——列为下轮候选 1。
- **落档**：候选 1 **完成 ✅**；「二、当前状态」补一行；候选 2 第 18 次核实仍缺（待主会话）、候选 3 第 18 次仍锁屏；下一步计划重写（候选 1＝实测第二波）。

### 2026-09-14 19:30–20:0x（本轮：候选 1——菜单快捷键与 docs/快捷键.md 一致性体检 + 一致性冒烟强制；提交 94f0f41）

- **开工第 0 步**：`git status` 工作树干净；近 90 分钟提交＝553048d/1439d9d（创作层 18:45，守卫冒烟/toast）、3122c0d/e392a7b（智能层 18:00 docs），与本轮目标（docs/快捷键.md、ShortcutHelp.tsx、scripts 两冒烟）零交集；`docs/反馈清单.md` 无归属平台层「待落实/落实中」条目。环境核实：`pmset -g assertions` UserIsActive=0＝**仍锁屏**（候选 3 第 17 次核实受阻；本轮无头/数据层可做）；`hermes cron list` **第 17 次确认**采集管道 76de7e949a3b 仍不在活动列表（6 个 active 无「素材库采集」；候选 2 维持待主会话）；`/tmp/spa_server.py`（8899）与 CDP 9224（Chrome 152.0.7977.84）就绪。
- **调研**（主题=档案候选 1；真实来源）：① 外网 web_search 两次查询（Milkdown keymap 定制/HIG 菜单发现性）后端返回空——退而取**安装源码为权威**（比在线文档更贴近本仓库实际运行版本）：`node_modules/@milkdown/plugin-history/src/index.ts`＝redo `Mod-y`/`Shift-Mod-z`、undo `Mod-z`；`@milkdown/preset-commonmark/lib/index.js`＝行内代码 `Mod-e`、strong `Mod-b`、emphasis `Mod-i`、引用块 `Mod-Shift-b`、正文段落 `Mod-Alt-0`、标题 `Mod-Alt-1..6`、列表缩进 `Tab`/`Mod-]`、反缩进 `Shift-Tab`/`Mod-[`；② 外网佐证 1 条命中：Electron issue #6731（github.com/electron/electron/issues/6731「Ctrl+Plus accelerator ends up as Shift+Ctrl+=」）——「**Cmd+Plus does work correctly on osx**」→ mac 侧 zoomIn `CmdOrCtrl+Plus` 注册正常（问题仅 win/linux，且 win 走查已锁 role 默认）；③ **决定性代码事实**：`node_modules/prosemirror-view/src/input.ts` `eventBelongsToView`（L91-99）＝`if (event.defaultPrevented) return false` —— Prose 的 window 捕获 keydown 对 ⌘E `preventDefault()` 后 PM keymap 不再执行 → 应用「用选区设置查找词」稳定遮蔽 Milkdown 行内代码 `Mod-e`（无双重触发，行为与 docs 一致）；④ 逐项对表：menu mac 分支 18 个显式 accelerator **全部**在 docs/快捷键.md 有记录（⌘, / ⌘W / ⌃⌘F / ⌘M / ⌘0 / ⌘+ / ⌘− / ⌘H / ⌥⌘H / ⌘Q / ⌘S / ⌘Z / ⇧⌘Z / ⌘X / ⌘C / ⌘V / ⌘A / ⌘F / ⌘E / ⌘G / ⇧⌘G）；docs 表格组合 ⊆ 菜单 ∪ 应用内处理（⌘K 命令面板 / ⌘B ⌘I ⌘Y 编辑器 Milkdown / Esc 浮层体系）；**真实差异四项**＝(a) docs 备注「菜单承载本表全部组合」措辞过强（⌘K/⌘B/⌘I/⌘Y/Esc 无菜单项，属设计口径 §三-5「不新增、只承载已有+系统标准」）；(b) Milkdown 继承键位（⇧⌘B/⌥⌘0/⌥⌘1-6/⌘[ ⌘]/Tab）与 ⌘E 遮蔽均未在 docs 登记；(c) ShortcutHelp 速查面板（三处同源之一）缺「全应用 Esc」行；(d) docs 无 win 档说明（菜单 win 分支已 CmdOrCtrl 化）。
- **产品规划**：为什么现在做＝候选 1 已立（16:30 轮），且发现「三处同源契约」（设计口径 §三-5）目前只是口头约定——本轮把它**升级为冒烟强制**（menu↔docs 双向一致性检查，漂移即 FAIL）；同时把用户最关心的「快捷键可发现性=文档与实现一致」固化成回归防线。怎么做＝docs/快捷键.md（措辞修正 + 编辑节补 Milkdown 继承键位注 + win 档说明）；ShortcutHelp.tsx（全应用补 Esc 行）；menu-template-smoke.mjs 加 §6（`accToDoc` 归一＝按 '+' 拆 token 映射 CmdOrCtrl/Cmd/Shift/Alt/Ctrl/Plus → ⌘/⇧/⌥/⌃/+；方向 A=菜单显式 accelerator ⊆ docs 文本；方向 B=docs 表格第一格键盘组合 ⊆ 菜单 ∪ 应用内 allowlist〔⌘K/⌘B/⌘I/⌘Y/Esc，可 env 覆盖做负向〕）；menu-actions-ui-smoke.mjs P8 补 P8.1（速查面板含 Esc 行断言）+ 截图；**不动**：menu.ts 一行未动（菜单本身零缺陷——体检结论）、编辑器 ⌘E 行为（与 docs 一致，行内代码有工具栏按钮）、体验层文案口径表、采集管道、win 模板。验收口径＝三道门 + menu-template-smoke 29/29（含新 2 项）+ 负向验证（docs ⌘S→⌘P 必抓）+ menu-actions-ui 全过 + 截图。
- **技术落地**：4 文件——docs/快捷键.md（备注「全部组合」→「全部键盘快捷键组合＋macOS 标准键」并注明 ⌘K/⌘B/⌘I/⌘Y/Esc 属应用内处理、新增 win 档说明；编辑表下补 Milkdown 继承键位与 ⌘E 遮蔽注，冲突以本文档为准）；ShortcutHelp.tsx（全应用组 +「关闭当前浮层（Esc）」行）；menu-template-smoke.mjs（§6 约 45 行：accToDoc 拆分归一、A/B 双向、ZJ_SMOKE_ALLOW 负向口）；menu-actions-ui-smoke.mjs（P8.1 断言 + ZJ_SHOT 截图段沿用既有规格）。
- **测试验收**（真实跑通）：首跑 §6 **2 FAIL**——accToDoc 首版用顺序 replace 把分隔符 '+' 残留（`Cmd+H`→`⌘+H`）→ 根因式修法＝按 '+' 拆 token 逐 token 映射，修后 **29/29 PASS**（原 27 项零回归）；**负向验证**：临时把 docs「⌘S」批量→「⌘P」→ 方向 A FAIL（菜单 ⌘S 漏档）+ 方向 B FAIL（文档 ⌘P 空头）＝双向都真实可抓 → 还原后 29/29（restore 后 git diff 确认 docs 无残留）；三道门 typecheck ✅ / build ✅（3007 模块）/ npm test **64 文件 647 例全绿**；`node scripts/menu-actions-ui-smoke.mjs` **10 项 ALL PASS**（原 9 项无回归 + P8.1 速查含 Esc 行）+ 截图 `~/Pictures/zhijuan/shortcut-help-1953.png`（1280×713，DOM 文本断言「关闭当前浮层」客观通过）。
- **落档**：候选 1 **完成 ✅**；「二、当前状态」补一行；候选 2 第 17 次核实仍缺（待主会话）、候选 3 第 17 次仍锁屏；下一步计划重写（候选 1＝编辑器内快捷键真实可用性无头实测闭环——B 方向目前只证明「文档↔菜单或 allowlist 存在」，Milkdown 键位在编辑器里「确实按下生效」尚无自动化实证）。

### 2026-09-12 16:30–17:00（第 21 轮：候选 2 devShim 剩余口径差异收口；提交 638e8f9）

**① 调研**（真实来源）：
- 并发检查：工作树干净；近 90min 提交 e64ea67（创作层）/3216b10（智能层）均动过 `devShim.ts` 但函数面=adoptActs/auditDoc，本轮目标函数面（createProject/importProject/listSlices/getPaths）无交集；反馈清单无平台层「待落实/落实中」条。
- Martin Fowler《Test Double》（https://martinfowler.com/bliki/TestDouble.html，经 r.jina.ai 抓取）：devShim 属 **Fake** 类测试替身（有工作实现但走捷径），风险=与生产行为漂移；同作者《InMemoryTestDatabase》（https://martinfowler.com/bliki/InMemoryTestDatabase.html）主张让测试替身与生产部署尽量靠近——织卷对策即第 19 轮红线「shared 纯函数双挂」，本轮延续。
- 真机实现逐字段比对（改前重读）：`store.importProject`（空白路径 trim 校验→resolve 后 **basename 即 id**（不 sanitize）→无 project.md 时 name=id/description=''）；`ipc app:getPaths`（defaultLibrary=process.env.HOME）；`main/slices.listSlices`（updatedAt=statSync mtimeMs）；`shared/paths.sanitizeFile`（保留中文）。另核实：渲染层 `defaultLibrary` **零消费**（仅 devShim 定义处）、Timeline 页不消费 SliceEntry.updatedAt。

**② 产品规划**：候选 2 攒批到期，收口三处：① createProject/importProject 的 id 规则——按红线抽 `shared/projects.nextProjectId`（sanitizeFile＋重名时间戳后缀），真机 store.createProject 与 devShim 双挂；devShim importProject 对齐 id=basename（去 demo- 前缀与 slice(0,4)）、空路径报错、name=id/description=''；② getPaths.defaultLibrary：已核实零消费且 renderer 无 node 环境取不到 HOME → 保持 '' 并注释结论（非代码差异，仅记录）；③ listSlices.updatedAt：硬编码 0 → docsOf 的 mtime（devMtime 稳定模拟，语义=真机 statSync）。为什么现在：mock 与真机口径差异是「无头冒烟假绿」源头之一（冒烟跑在残缺 mock 上会掩盖真机问题），攒批到期即收。不动：真机 importProject 复制入库逻辑、采集管道、UI 布局。验收口径：三道门＋import-project/template/devshim-parity 冒烟＋临时行为检查。

**③ 技术落地**：`shared/projects.ts` +nextProjectId；`store.ts` createProject 改用（行为等价：sanitize+existsSync 后缀）；`devShim.ts` createProject/importProject/listSlices 三处＋import；`scripts/import-project-ui-smoke.mjs` 既有断言 `/^#\/project\/demo-/`（依赖旧前缀）改宽松（decodeURIComponent 含「旧稿」）；`tests/unit/projects.test.ts` +4 例。

**④ 测试验收**（真实跑通）：
- 三道门：typecheck ✅ / build ✅ / npm test **47 文件 483 例全绿**（+4 nextProjectId）。
- import-project-ui-smoke **4/4 PASS**（跳转 hash=`#/project/%E6%97%A7%E7%A8%BF/novel`、解码含「旧稿」；失败路径 toast 正常）；**template-ui-smoke 7/7 PASS**（createProject mock 改动回归）；**devshim-parity-ui-smoke 11/11 PASS**（listSlices 改动回归）。
- 临时行为检查 **6/6 PASS**（CDP 直调 window.zhijuan：首建 id=清洗名、重名加后缀、import id=basename 无 demo- 前缀、尾斜杠取 basename、空白路径报错与真机同、listSlices.updatedAt>0 不再为 0）。
- 踩坑：① patch 工具对正则反斜杠报「escape-drift」（该行改用 python 脚本替换）；② 冒烟断言首版 `/^#\/project\/[^/]+$/` 漏了项目页 hash 带的 `/novel` 尾段（首次 FAIL，改 startsWith+includes 后过）——**断言先对着真实 hash 写**。

**⑤ 落档**：本条目；「二、当前状态」补一行；下一步计划重写（候选 2 完成，候选 3 转正为候选 2 且扩为冒烟环境加固收口；候选 1 不变）。

### 2026-09-11 16:30–16:55（第 13 轮：候选 1 ⌘K 面板再体检——打开章节切片状态 + 最近素材键盘可达；提交 bcd11ba）

**① 调研**（真实来源）：
- 并发检查：`git log` 近 5 笔均为创作层/智能层/体验层（3167844/0c5e764/c6c7673/8611e3a/d3d4cae），与本模块文件无交集；工作树干净；反馈清单无平台层「待落实/落实中」条（F-20260911-01 属体验层落实中，非本模块）。
- 04-体验层.md「五、下一步计划」：下轮候选为候选 0 质感专项（V-03 侧栏密度/V-02 清尾/V-05 空态插画），**未占用 CommandPalette**——⌘K 体检无撞车，安全开工。
- 代码体检（CommandPalette.tsx 现状）：打开章节项已显示字数；未显示切片状态；「最近素材」组 DOM 序在「打开章节」之后——q 空态键盘 ↓ 需过 7 页面 + N 章节才可达（20 章项目 ≈ 28 步），键盘优先用户痛点；q 空时最近素材是唯一「内容性」建议（Obsidian 命令面板惯例=最近使用置顶）。
- 既有冒烟（cmdk-ui/recent/material）断言全用 includes/group 定位，**不依赖组顺序** → 组重排安全。devShim `listChapters` 已解析 `fm['切片']`（与真机 extractFrontMatter 同口径），字段可达；`recentLibraryDocs` 只扫 `素材库/`（注入正文章不影响）。

**② 产品规划**：候选 1 落地两点（均 P3、纯渲染层、零新依赖）：
- **打开章节组显示切片状态**：有切片=「切片：<名>」低调徽章（与素材搜索「文件名/正文」徽章同款 bg-surface-2/10px）；无切片=「未设切片」弱灰文本——发现性（新建章忘设切片是常见疏漏，面板即见）。
- **最近素材组前移至「页面」组后**：键盘到达从 ~28 步降到 7 步；页面导航仍居首不压主路径。
- 不动：域逻辑（searchDocs/listChapters/Library 跳转/采集池/设置）；不引依赖。验收口径：三道门 + 无头冒烟（组顺序、徽章、纯 ↓ 7 步达最近素材回车跳转、零 JS 异常）+ 三回归。

**③ 技术落地**（提交 bcd11ba）：
- `CommandPalette.tsx`：打开章节 item 加切片徽章/未设切片（ml-auto，字数 ml-2 靠右保持）；「最近素材」组 JSX 前移至「打开章节」组之前。
- 新增 `scripts/cmdk-chapter-state-smoke.mjs`：经 `window.zhijuan.writeDoc` 在**独立测试 tab** 注入无切片「第05章_雾堤.md」（devShim 内存，不污染演示种子与其他冒烟）→ 断言切片徽章/未设切片/组顺序/纯 ↓≤9 步选中最近素材/回车跳 library？doc=/零 JS 异常。

**④ 测试验收**（真实跑通）：
- 三道门：typecheck ✅ / build ✅ / npm test **39 文件 395 例全绿** ✅。
- 无头真机 `node scripts/cmdk-chapter-state-smoke.mjs` **11 项全 PASS**：组顺序=页面→最近素材→打开章节→打开项目→帮助；第1章「切片：第一幕_雾港之夜」徽章；注入第5章「未设切片」；字数仍显；**纯 ↓ 键 7 步**选中最近素材条目（环境/采集_演示图书馆）、回车跳 `library?doc=`；零 JS 异常。
- **回归全绿**：cmdk-ui-smoke 15 项 PASS、cmdk-recent-smoke 14 项 PASS、cmdk-material-smoke 19 项 PASS（「雾港」章节项 now 带切片徽章，文本包含断言兼容未破坏）。
- 踩坑记录：① patch 模糊匹配曾把「打开章节」字数行误替换为搜索组 snippet（引入 h 未定义）→ 重读文件用唯一上下文修复；② write_file 写入 `\d` 变 `\\d` 导致正则 SyntaxError——冒烟脚本含正则先用 `node --check` 验证；③ devShim `writeDoc` 返回非 true，注入断言用 `.then/.catch` 形态而非返回值。

**⑤ 落档**：本条目；下一步计划重写（候选 1 完成，候选 2 转正）。

### 2026-09-11 13:30–13:55（第 12 轮：候选 1 导入已有目录收尾——修复假导入 + 目录选择器）

**① 调研**（真实上网，来源记录）：
- Obsidian 官方帮助「Create a vault」（https://help.obsidian.md/getting+started/create+a+vault，经 r.jina.ai 抓取正文）：Obsidian 的 vault 模型是「本地文件夹即库」，打开已有文件夹＝**原位使用**；这一点与织卷不同——织卷已拍板 D-V2-8「项目库默认位＝工作区/项目库」，listProjects/projectDir/watcher 全部基于 libraryRoot，若照 Obsidian 做「外挂项目」要引入台账解析、多位置 watcher、删除/统计/recents 全链路改造（不可逆一刀）。写作类软件（Scrivener/Manuskript）「Import」则是把外部文件复制进项目。**结论：织卷走「复制入库」语义**——把已有目录复制进项目库补骨架，原目录不动（可逆、零架构侵入、兼容全部现有路径假设）。
- Electron 官方文档 dialog（https://www.electronjs.org/docs/latest/api/dialog）：目录选择标准姿势 `dialog.showOpenDialog({ properties: ['openDirectory'] })`（未给 defaultPath 时回落 Downloads/家目录），macOS 系统选择器是「打开目录型应用」通行交互（Obsidian/VS Code 同）。
- 关键 bug（代码审读实锤）：旧 `store.importProject(dir)` 对传入 dir 只做 existsSync 检查，随后 `ensureSkeleton(id)`/`projectFile(id)` 全部按 `projectDir(id)=libraryRoot()/basename` 走——**传入目录内容根本没进库**，等于在库内建了个同名空壳（假导入）；且 Home 侧 `if (p) navigate` 对返回 null 静默无提示。
- 并发检查：开工 `git status` 有智能层固定命令 WIP（AgentPanel/CommandMenu/ChapterCheckDrawer/Novel/commands.ts + fixed-command-ui-smoke.mjs），属智能层在途，本轮只 `git add` 自己文件不整树提交；13:07–13:17 提交均为创作层/体验层（83d5bd9/8950a66/a0eb7cf），与本模块文件无交集；反馈清单暂无本模块待落实条目（体验质感 F-20260911-01 属体验层，14:15 轮接手）。

**② 规划**：候选 1 一句话＝**把「导入已有目录」从假导入修成真导入**，顺手补系统目录选择器。为什么现在：导入是首页级入口（§四 A 交互 2），当前行为与文案承诺完全不符（文案说「补全骨架不覆盖已有内容」，实际外部内容全部丢失），是 P1 级数据语义 bug，早晚要修，且修复面小（store 一个函数 + IPC 一个通道 + 对话框）。怎么做：① store.importProject 重写为「复制入库」——`resolve` 归一、`cpSync` 递归（filter 跳过 `.git`/`.DS_Store`/`node_modules`）、库内已有同名项目幂等不覆盖、project.md 不存在才补、返回结构化 `ImportResult { ok, summary?, error?, copied? }`；② ipc 加 `project:importPicker`（系统目录选择器）；③ Home 对话框加「选择文件夹…」按钮回填输入框 + 按结构化结果提示（成功包含「原目录保留不动」、已有同名提示 info、失败展示 error）；④ devShim 同口径 mock（importProject 结构化 + importPicker=null，zj-fail 可注入）。不动：listProjects/recents/骨架模板/templates 逻辑；不引依赖（fs.cpSync 为 Node 20 内建）。

**③ 落地**：`shared/types.ts` +ImportResult；`store.ts` importProject 重写 + import 增补（resolve/statSync/cpSync）；`ipc.ts` +project:importPicker；`preload` +importPicker、importProject 返回类型改 ImportResult；`Home.tsx` importDir/pickImportDir/对话框（按钮 shrink-0 whitespace-nowrap 按约定）；`devShim.ts` importProject mock 对齐真机结构。单测 store.test.ts +2 describe（复制入库/跳过杂物/幂等/不存在/库内自身）。冒烟：新增 `scripts/import-project-smoke.mjs`（数据层真文件系统 22 断言）+ `scripts/import-project-ui-smoke.mjs`（无头 UI 4 项）。

**④ 测试验收**（真实跑通）：
- 三道门：typecheck ✅ / build ✅ / npm test **392/392** ✅（含本轮新增 4 例）。
- 数据层冒烟 `node scripts/import-project-smoke.mjs`：**22 项全 PASS**——内容拷入、.git/.DS_Store/node_modules 跳过、骨架补全、project.md 生成、listProjects 可见、重复导入 copied=false 且不清内容、不存在目录结构化错误、库内项目自身幂等。
- 无头 UI 冒烟 `node scripts/import-project-ui-smoke.mjs`（CDP 9224 + 8123 + 缓存破坏参数）：**4 项全 PASS**——对话框新文案与「选择文件夹…」按钮、成功导入 toast + 跳转 `#/project/demo-旧稿/novel`、`?zj-fail=importProject` 注入失败 toast「导入目录失败」/页面不崩。
- 踩坑：zj-fail 注入参数必须放在路径 query（`/?zj-fail=…`），放 hash 后（`/#/?zj-fail=…`）location.search 读不到、注入静默失效（states-smoke 是 `/?…&zj-fail=…#/` 惯例，本轮首跑假 FAIL 一次，实踩）。

**⑤ 落档**：本条目 + 当前状态补一行；模块设计 §四 A 交互 2 补注；下一步计划已重写（候选 1 完成）。

**提交**：dcea302 — fix(平台层): 导入已有目录收尾——真复制入库（跳过 .git/.DS_Store/node_modules）、系统目录选择器、结构化结果反馈；修复原「只建同名空壳、外部内容丢失」假导入 bug

### 2026-09-11 10:30–11:00（第 11 轮：候选 1 ⌘K 素材搜索细节——最近素材 + 命中类别路径）

**① 调研**（web_search 实搜，来源记录）：
- Obsidian Forum「Dataview list of 25 most recently modified files in vault」（https://forum.obsidian.md/t/dataview-list-of-25-most-recently-modified-files-in-vault/23771）与 positioniseverything「How to Show Recently Edited Notes in Obsidian」（https://www.positioniseverything.net/how-to-show-recently-edited-notes-in-obsidian/）：**按 file.mtime 降序列「最近修改」文档**是 Obsidian 生态成熟先例（用户用 `SORT file.mtime DESC` 表达），即「最近素材 = 素材库下按 mtime 取最近 N 条」；recent-files 插件（上轮已引）是「最近打开」语义，二者区分：本候选按「最近修改」实现（写作中刚采集/刚编辑的素材最可能再开）。
- 结合第 9 轮已引 Apple HIG Search fields「可考虑显示最近/建议搜索」：q 为空时 show recent 是搜索建议的标准形式（第 9 轮「q 空不预载」指不预载全文搜索组；最近建议组是本轮新增，不冲突）。
- 并发检查：开工 `git status` 干净；90 分钟内提交均为智能层（1bdc86f/33b64d0）与创作层（ce49226/903eb05）+ 体系 docs（8ce93aa 仅 README），与本模块候选无文件交集；反馈清单无本模块待落实条目（仅已解决 F-20260910-01）。

**② 规划**：候选 1 两个 P3 细节一起做——① 命中行只有文件名，无法区分「人物/旧茶楼账房」与「环境/旧茶楼账房」，显示**类别路径**是辨识关键；② q 为空面板只显示导航组，浪费写作中「再开刚编辑素材」场景，新增**最近素材**建议组（最近修改 5 条 + 相对时间）。为什么现在：零新依赖（域函数 + cmdk item 即可），闭环键盘优先工作流；不动：searchDocs 域逻辑、LibraryBrowser、采集池、recent-files（项目置顶那套）。验收口径：三道门 + 单测（域函数/纯函数）+ 无头 UI 冒烟（真实点击跳转实锤）+ 更新既有 cmdk-material 回归断言。

**③ 落地**：
- `shared/relativeTime.ts`（新建）：`formatRelativeTime`（刚刚/N 分钟前/N 小时前/N 天前/MM-DD，Apple HIG relative date 口径）。`shared/libraryTree.ts`：`libraryCategoryOf(file)` 解析「素材库/<类别>/…」→ 类别名（根级/非素材库 → ''）。`shared/types.ts`：`RecentLibraryDoc { file/name/mtime }`。
- `main/library.ts`：`recentLibraryDocs(id, n=5)`——同 searchDocs 枚举口径（排除采集池/隐藏、递归子目录），按 mtime 新→旧稳定排序，取前 n；`ipc.ts` 加 `library:recentDocs`；preload/devShim 同步 mock（devShim 用 docsOf 现成 mtime 口径）。
- `CommandPalette.tsx`：q 为空且项目内时显示「最近素材」组（打开面板时一次性拉取；item = 类别前缀灰字 + 文件名 + 右侧相对时间，点击直达 `?doc=`）；全文搜索命中行加类别前缀 + keywords 含类别。
- 冒烟：新增 `scripts/cmdk-recent-smoke.mjs`（17 项断言）；`cmdk-material-smoke.mjs` 第 ② 步断言同步（「空输入不显示素材组」→「不显示全文搜索组 + 显示最近素材组」）。

**④ 测试验收**（真实跑通）：
- 三道门：typecheck ✅ / build ✅ / npm test **371/371** ✅（本轮新增 8 例：relativeTime 4 + libraryCategoryOf 3 + recentLibraryDocs 1）。
- 无头 CDP 冒烟（9224 + out/renderer 带缓存破坏）：`node scripts/cmdk-recent-smoke.mjs` **17 项全 PASS**——组出现、4 条（采集池任务卡未入列）、类别前缀 + 「刚刚」相对时间、点击第一条 → `__ZJ_DOC.rel` 实锤打开正确素材、全文搜索「旧物」命中行带「桥段/」前缀不回归、无 JS 异常；`node scripts/cmdk-material-smoke.mjs` **19 项全 PASS**（含新断言「空输入显示最近素材组」）。
- 踩坑：①devShim mock 素材 mtime 全为 now → 最近列表排序按 file 拼音序（环<桥<人<索），冒烟断言须动态解析条目路径而非写死首条；②CDP 注入表达式里 `'\n'` 经双层传输转义被打乱——改用 `String.fromCharCode(10)`（存坑）。
- 截图：`/tmp/cmdk-recent.png`（⌘K 面板「最近素材」组，视觉留主人过目）。

**⑤ 落档**：本条目 + 当前状态补一行；下一步计划已重写。

**提交**：ab3cc4e — feat(平台层): ⌘K 素材搜索细节——q 空显示最近素材（最近修改 5 条 + 相对时间）并可点开，命中行显示类别路径前缀；新增 recentLibraryDocs 域函数/shared 纯函数，cmdk-recent-smoke 冒烟 17 项 + material 冒烟回归 19 项全绿

### 2026-09-11 06:30–07:00（第 10 轮：候选 1 项目首页增强——搜索过滤 + 最近打开置顶）

**① 调研**（真实来源；本机 web_search 返回不相关结果、web_extract（含 r.jina.ai 前缀）全部「private or internal network」被拦 → 用本机无头 Chrome CDP 9224 抓正文）：
- Obsidian Recent Files 插件 README（https://github.com/tgrosinger/recent-files-obsidian，CDP 抓 innerText）：「displays a list of most recently opened files in the sidebar」，点击打开、可排除路径/标签/书签——**「最近打开」作为写作工具入口且按打开动作记录**的成熟实践；织卷映射：Home 默认排序 = 最近打开优先。
- 尝试 VS Code `docs/getstarted/userinterface` 与 `getting-started`、Apple HIG `menus` 抓取：页面均无「Open Recent」细节，**不作为依据**，仅 Obsidian 插件页一条实测来源 + macOS/VS Code「最近使用/Open Recent」系统级常识（不硬编细节）。
- 并发检查：开工 `git status` 干净；90 分钟内提交均为智能层/创作层 docs 落档与 feat（1ca3116/b6f5c08/918b11d/9fd1359），与本模块候选无文件交集；反馈清单仅 F-20260910-01（已解决），无本模块待落实条目。

**② 规划**：Home 现只有卡片网格（按 updatedAt 排序），无搜索/无「最近打开」；⌘K 是瞬态跳转，Home 才是管理面（模块设计 §四 A：名称/简介/统计/更新/最近章节 + 删除/导出）。优化点＝**多项目时按名称/简介即时过滤 + 最近打开置顶**。为什么现在做：项目多了管理面必须能找、能按「手头在写哪本」排序；零新依赖（recents 记 userData 小 JSON，排序/过滤是纯函数）。怎么做：`shared/projects.ts`（orderProjects 纯函数，无 fs 可单测）＋ `main/recent.ts`（独立状态域，**不混进 AppSettings**——settings 是用户可编辑设置，recents 是内部行为记录；userData/zhijuan-recents.json，上限 12、去重置顶、损坏容错）＋ 挂点用**现成 `project:open`**（Workspace mount 必调，覆盖卡片/⌘K/导入后所有入口，零新增渲染层调用）＋ 删除项目时 `removeRecent` 对偶清理 ＋ Home 工具条（搜索框 + 命中计数 + 排序说明，窄窗口不换行）。**不动**：store.listProjects 排序（渲染层重排，别处引用不受影响）、卡片布局、CommandPalette、devShim 数据口径（openProject/getRecentEntries 与真机同语义：打开即记录、返回按打开时间降序）。验收口径：三道门 + orderProjects/recent 单测（13 例）+ 无头 UI 冒烟（搜索过滤/空态/打开后返回置顶/无 JS 异常）。

**③ 落地**：
- `src/shared/projects.ts`：RecentEntry 类型 + orderProjects（query trim 后名称/简介子串过滤、大小写不敏感；recents 中项目按 openedAt 降序在前，其余按 updatedAt 降序；recents 非法项防御）。
- `src/main/recent.ts`：getRecentEntries/recordOpen/removeRecent（safeParse 校验、mkdir+write 幂等、写失败静默）。
- `src/main/ipc.ts`：`project:open` handler 加 recordOpen；新增 `project:recents`；`project:remove` ok 后 removeRecent。
- `src/preload/index.ts`：getRecentEntries。
- `Home.tsx`：工具条（Search 图标输入框/命中计数「共 N 个项目 · 命中 M」/排序说明「最近打开优先 · 未打开的按最近编辑」）；渲染用 orderProjects；无匹配空态文案。
- devShim.ts：recents 内存数组 + openProject 记录 + getRecentEntries（与真机同口径）。
- 新增 tests/unit/projects.test.ts（7 例）与 tests/unit/recent.test.ts（6 例，真实落盘 tmp）。

**④ 测试验收**（真实跑通）：
- 三道门：typecheck ✅ / build ✅ / npm test **342/342** ✅（新增 13 例；两处测试校正：query 用中文描述子串、同毫秒 openedAt 不断言严格大于）。
- 无头 UI 冒烟 `/tmp/home-recents-smoke.mjs`（CDP 9224 + serve-renderer 8123 + 缓存破坏参数）**12/12 PASS**：首页渲染/搜索框与排序说明存在/初始按最近编辑（余烬的灯在前）/搜「云山」过滤到 1 卡/命中计数/无匹配空态/清空恢复/点击进项目/返回首页后「云山驿事」置顶/无 JS 异常。
- 修复过程：recent.ts 写盘前补 mkdirSync（否则 userData 目录缺失时写失败被静默吞掉，测试暴露）。

**⑤ 落档**：本条目；模块设计 §四 A 交互补注（Home 支持搜索与最近打开置顶）；下一步计划已重写（候选 1 完成，候选 2/3 前移）。

**提交**：9dcaf0d feat(平台层): 首页搜索过滤+最近打开置顶——shared/projects 纯函数排序、main/recent 独立记录(上限12去重)、project:open 时记录/删除时清理、Home 工具条与空态；+13 单测与无头冒烟 12/12

### 2026-09-09 21:32–21:45（第 1 轮）
- **调研**：①读档案「五」候选 1「数据层体检」+ 评审 §二.1（store 上帝文件目标形态：只留项目+文件+watcher）；②真实上网：Electron 官方 Process Model（https://www.electronjs.org/docs/latest/tutorial/process-model，r.jina.ai 抓取）、Performance（…/tutorial/performance）、electron-vite 官方 Getting Started（https://electron-vite.org/guide/，main/preload/renderer 分域组织）；③本机 searxng 全引擎挂起（CAPTCHA/限流），web_search 空转，已确证 r.jina.ai 直连可抓（环境记录）。
- **规划**：体检结论——store.ts 实测 307 行/18 export（评审时 369 行/17），settings 已拆（D3）但仍含「工作区文档域」4 函数（workspaceStatus/ensureWorkspaceDocs/listWorkspaceDocs/readWorkspaceDoc），未达评审目标形态；本轮最小一刀＝纯搬移拆出 `main/workspace.ts`，store 不再 re-export（ipc.ts 是唯一引用方，直接改 import），不动逻辑/通道名/共享类型/settings。
- **落地**：新增 src/main/workspace.ts（工作区落档文档幂等 + 列表/读取防穿越 basename）；store.ts 删 41 行并清 import；ipc.ts import 改 './workspace'；新增 tests/unit/workspace.test.ts 4 例（幂等/status/列表排序/读取与防穿越）。**改动后 store.ts = 269 行/14 export，干净落在「项目+文件+watcher」域。**
- **验证**：npm run typecheck ✅ / build ✅（主进程+preload+renderer 全过）/ npm test 174/174 ✅（新增 4 例）；数据层冒烟 `WORKSPACE SMOKE OK`（esbuild bundle workspace.ts + electron 桩，真临时目录：首建落档两文档、二次 created=[] 幂等、列表/读取
/防穿越全对）。
- **提交**：0a13192 refactor(平台层): 工作区文档域拆出 main/workspace.ts。
- **下一步**：数据层体检闭环，下一轮按「五」候选 1 项目模板（M3）开工。

### 2026-09-09 22:33–22:55（第 2 轮：候选 1 项目模板 M3）

**① 调研**（真实来源，r.jina.ai 直连抓取原文；web_extract 仍判「私有网络」被拦）：
- Scrivener 官方博客《Create Custom Templates for Your Scrivener Projects》https://www.literatureandlatte.com/blog/create-custom-templates-for-your-scrivener-projects ：内置 20+ 分类模板；Novel 模板＝稿件目录 + 示例首章 + 示例场景文本 + 角色/设定/参考分区；**任意项目可 File > Save as Template**（标题/分类/图标/是否存样式）；模板可导入/导出/设默认/「Reveal in Finder」——模板≈项目骨架快照，可社区分享。
- Obsidian 官方 Templates 核心插件 https://help.obsidian.md/plugins/templates ：模板＝**普通 markdown 文件**放在指定「模板文件夹」，插入时替换变量 `{{title}}/{{date}}/{{time}}`（可带 Moment 格式串），模板属性可与目标合并——模板＝纯文件、用户自定义、可分享。
- Manuskript README https://github.com/olivierkes/manuskript ：新建项目选 fiction/nonfiction 等「writing modes」模板，改变初始结构。
- 共性：模板＝可复用初始文档集（纯文件）；选择发生在「新建项目」时；用户可自定义；可分享。织卷取这一形态——**学 Obsidian「模板＝目录下的文件」，不学 Scrivener 的项目快照导入导出**（后者重，非当下价值）。

**② 规划**（本轮收敛，以下为设计定稿）：
- 形态：模板＝`<工作区>/模板/项目模板/<模板名>/` 下的文件目录，与项目内结构同构；纯本地明文、可入 git、分享＝拷目录；**零新依赖**。
- 内建「示例」模板：主进程幂等落档（与 `文档/` 同模式，只落空档不覆盖用户改动）；内容＝示例角色档案（基础设定＋切片小节）+ 示例世界观切片文件 + 带约定头的示例正文章——教 front matter/切片/目录约定，**中性示例，非任何作品的种子**。
- 新建项目对话框加「初始内容」：空白（默认＝现有行为）/ 示例 / 用户模板（扫描模板目录）；`createProject` 加**可选** template 参数（IPC 向后兼容）。
- 应用规则：先 `ensureSkeleton` + project.md（顺序不变），再从模板**补充复制**——跳过已存在文件、跳过 project.md 与 `.zhijuan/`，绝不覆盖已有内容。
- **不做**：`{{}}` 变量替换（无真实需求）；「另存为模板」＝把项目目录拷进模板目录（纯文件操作，文档里说明即可）；导入导出＝文件系统。
- 不动：`ensureSkeleton` 骨架默认文件、project.md 正文模板、proposals/slices/settings、其他层功能。
- 与「初始化引导」边界：模板＝**静态文件样例**（平台层）；ProjectGuide＝**交互填写引导**（体验层，`?guide=1` 链路不变）；两者正交，交叉登记到 04-体验层档案。
- 验收口径：三道门绿 + templates 单测（幂等落档/列表/复制跳过规则）＋ 数据层冒烟（新建项目选「示例」→ 三个示例文件落盘、project.md 未被模板覆盖）。

**③ 技术落地**（本轮实现）：
- 新增 `src/main/templates.ts`：`ensureBuiltinTemplates`（内建「示例」幂等落档：示例角色档案 / 示例世界切片 / 带约定头的示例正文章，与 `文档/` 同「只落空档」模式）、`listTemplates`（先落档再扫描 `模板/项目模板/` 子目录，内建排最前，隐藏项/单文件不列）、`applyTemplate`（补充复制：只复制 .md、跳过已存在文件、防御性跳过 project.md 与 `.zhijuan/`；模板不存在返回 ok:false）。
- `store.createProject` 加**可选** `template?: string`：骨架与 project.md 照旧后再补充应用模板。
- 接线：`ipc.ts`（`project:create` 透传第三参 + 新 `project:templates`）、`preload/index.ts`、`devShim.ts`（listTemplates mock ＋ createProject 第三参）。
- Home 新建项目对话框加「初始内容（可选）」选择（Select）：空白（默认）/ 示例 / 用户模板，说明文案指向模板目录位置。
- `shared/types.ts` 增 `ProjectTemplate`（preload/renderer 共用，不跨进程拉 main 类型）。
- 新增 `tests/unit/templates.test.ts` 6 例；新增仓库内无头 UI 冒烟 `scripts/template-ui-smoke.mjs`。
- 顺带发现（不在本轮修）：项目路由 index `<Navigate to="novel" replace />` 会丢掉 `?guide=1` search 参数——Workspace 挂载时已捕获 guide 状态，引导仍会弹，仅 URL 不残留；登记给体验层候选 3 留意。

**④ 测试验收**（真实跑通）：
- 三道门：`npm run typecheck` ✅ / `npm run build` ✅ / `npm test` **180/180** ✅（新增 templates 6 例，真 fs ＋ electron 桩）。
- 数据链路＝单测即真实分支：createProject(name, desc, '示例') 走真 store + 真模板落档 + 真文件系统，断言示例三文件落盘、project.md 仍为 store 生成、骨架默认文件在、stats.chapters=1。
- 无头 UI 冒烟 `scripts/template-ui-smoke.mjs`（CDP 9224 + out/renderer 带缓存破坏）：9 项全过——首页→新建项目对话框→「初始内容」下拉含 空白/示例 → 选「示例」→ 填名创建 → 跳 `/project/:id/novel` 项目页完好。

**⑤ 落档**：本条目 + 模块设计-V2 §四 交互 1 标注 + ROADMAP M3「项目模板 ✅ v1」+ 04-体验层候选 3 交叉登记。
- **提交**：b097b09 feat(平台层): 项目模板 v1——模板目录+新建项目可选初始内容（示例/用户模板），补充复制不覆盖骨架。
- **下一步**：本轮闭环；下一轮按「五」候选 1（打包 .app 可行性规划）或候选 3（别扭点随手修）开工。

### 2026-09-10 02:32–03:05（第 3 轮：候选 1 打包分发 .app 可行性规划）

**① 调研**（r.jina.ai 直连抓取；本机 web_search/searxng 全引擎仍挂起）：
- electron-vite Distribution https://electron-vite.org/guide/distribution ：ASAR 限制——`child_process.exec/spawn` **不能执行作为参数出现的 asar 内路径**；原生模块（*.node/.app/.exe）不应打进 asar；官方 electron-builder.yml 示例（files 白名单 + asarUnpack + afterSign）。
- electron-builder File Patterns https://www.electron.build/docs/contents ：files/extraResources/asar/npmRebuild 语义；**production dependencies 默认自动进包**（靠移 devDependencies 规避）。
- Electron 官方 ASAR tutorial https://www.electronjs.org/docs/latest/tutorial/asar-archives ：asar 只读；**cwd 不能设为 asar 内目录**；execFile/fs.open/process.dlopen 走临时解包；spawn/exec 仅支持包外真路径；Electron 31.x 已 EOL。
- Electron 31 release blog https://www.electronjs.org/blog/electron-31-0 ：31.x = Chromium 126 / V8 12.6 / **Node 20.14.0**（决定兜底 node 的版本口径）。
- 本机实证（只读命令）：`out/main/index.js` 仅 128KB、require 只有内建模块+electron ⇒ **主进程被 electron-vite 全量 bundle，运行时零 npm 依赖**；out/preload 仅 require('electron')（同理）；`dsh-runtime` 273MB（200 个 @deepseek-ai 包；原生模块：node-pty 跨平台 prebuilds 23MB 其中 darwin-arm64 仅 140KB、sharp 仅 darwin-arm64、node-addon-require-builtin 仅 darwin-arm64）；宿主机 arm64、系统 node v24.19.0；dshhome 8.6MB（profiles/*.yml 被 git 跟踪）、run/ 4KB（patch 运行期写入）。

**② 规划**（结论：**可行，无不可逆；定位=内部自用分发，不做签名/商店/自动更新**）：
- **形态**：electron-builder（devDep，构建期工具不违红线）+ 单包结构，`asar: true` 保留（主/预/渲染全 bundle、无外部资源）；**dsh-runtime 必须走 `extraResources` 出 asar**——子进程是独立真 node、无 Electron 的 asar fs patch，且含 .node 原生模块（dlopen 需真路径）、cwd 需设为运行时根；`asarUnpack` 不够（路径仍在 app.asar 命名空间内，子进程解析不到 unpacked 真路径）。访问侧 `runtimeDir()` 加 `app.isPackaged` 分支 → `join(process.resourcesPath,'dsh-runtime')`（dev 行为不变）。
- **节点宿主（无 node 的目标机）**：方案 A=extraResources 带官方 node 二进制（+~60MB 解压）；**方案 B（推荐起步）= `process.execPath` + env `ELECTRON_RUN_AS_NODE=1`**（0 额外体积；Electron 31 内嵌 Node 20.14.0；需真机冒烟确认 dsh 依赖树（按 node 24 安装）在 20.14 下可跑——sharp 0.35 要求 ≥18.17、node-pty 为 N-API，预期兼容，属实施轮判据）。现有 nodeBin() 的“Electron 兜底吞输出”坑＝缺 RUN_AS_NODE env 所致，实施时一起修。
- **可写状态分离**：打包后 `dsh-runtime/dshhome`（8.6MB，含生成物）与 `run/*.patch.yml`（主进程每启动重写）若留在 .app 内既不干净也不抗重装/升级 → 实施轮拍板：仅 `app.isPackaged` 时将 dshHome 与 run 迁 `app.getPath('userData')` 下（`<userData>/dshhome`、`<userData>/run`），dev 保持现状；首启幂等（现有 mkdir/写入逻辑已幂等，只需移目录常量）。
- **包体与裁剪**：files 白名单仅 `out/**`+`package.json`；**dependencies 全部移 devDependencies**（构建期需要，运行期 bundler 已内联；dsh-runtime 是独立 vendored 树不受影响）；extraResources 的 filter 剔 `node-pty/prebuilds/{win32,linux}*`（省 23MB）与 `dshhome` 内生成物、`.git`、tsconfig 等；`electronLanguages` 只留 zh-CN/en。预估 .app ≈ 500MB、zip≈250MB。
- **不做**：notarization/签名（内部自用，首启右键打开+xattr -cr 足够）、Windows/Linux 目标、自动更新、asar 内嵌 dsh。
- **验收口径（实施轮）**：三道门绿；`electron-builder --mac` 出 .app；**解锁后**真机启动（打包 app 与 dev 同为 Electron 渲染，锁屏建不出窗口，无头验收不覆盖打包产物）→ 写作区真模型对话链路（引擎经 RUN_AS_NODE 拉起、读 `userData` patch、连 127.0.0.1:8888）。

**③ 落地清单（实施轮按此执行，本轮不动实现）**：①package.json（deps→devDeps、+electron-builder、scripts:dist）②electron-builder.yml（上列全部）③runtime.ts 三处小改（runtimeDir isPackaged / nodeBin 兜底+RUN_AS_NODE（launch.env 注入）/ dshHome·run 迁 userData isPackaged 分支）④buildResources 图标（先默认占位，图标设计属体验层）⑤真机验收脚本。

**④ 验证**：本轮为调研/规划轮，无代码改动；档案与 ROADMAP 标注随本轮一并提交（docs(平台层)，迭代日志与 ROADMAP 两文件）。

**⑤ 落档**：本条目；ROADMAP M3 打包行标「可行性规划 ✅（2026-09-10）」。

### 2026-09-10 06:32–06:52（第 4 轮：候选 3 别扭点随手修——采集任务卡结果不可见）

**① 调研**：读本档案「五」候选 3 + 权威源模块设计 §四/五/九/十一/十二/十四（§十一 交互 3 明确「任务卡点击可见结果」，实际点不开）；实跑真数据（`素材库验收` 项目任务卡）确证管道回填格式＝front matter 追加 `结果:`/`完成:` + 正文摘要，且任务卡 md 是唯一载体（实现未落 `.zhijuan/tasks`，与 §十一 通道 1 有出入）。本轮为自发现修复，无外网新信息需求（不编造 URL），依据＝模块设计 + 真机样本。

**② 规划**：集成后真实用户侧完全看不见管道回填结果——排查出两层问题：(a) **status 恒显 pending 的 bug**：CollectionBar `readDoc(id, d.file)` 漏拼 `素材库/采集池/` 前缀（listDocs 返回相对 relDir 路径的已知坑），任务卡实际永远读空，S5 验收只看文件未看 UI 所以漏网；(b) §十一 交互 3「点击可见结果」根本没实现（任务卡块是普通 div）。本轮：readDoc 补前缀 + 任务卡块可点击 → 详情 Dialog（状态/类别/需求/关键词/来源/创建/结果/完成/正文）；新增 shared/taskCard.parseTaskCard（复用 extractFrontMatter，纯函数可单测）；**不动**采集管道 cron、任务卡格式、采集池列表/watcher 逻辑；devShim 补演示任务卡（无头可验）。

**③ 落地**：`a259bca`（src/shared/taskCard.ts + CollectionBar.tsx + devShim.ts + tests/unit/taskCard.test.ts + scripts/cdp-taskcard-ui-smoke.mjs + scripts/taskcard-real-smoke.mts）。

**④ 验证**：typecheck ✅ / build ✅ / npm test **217/217** ✅（+4）；无头 UI 冒烟 `cdp-taskcard-ui-smoke.mjs` PASS（devShim 演示项目：任务卡渲染成 `done + 摘要`（修前恒 pending）→ 点击 → 详情含结果路径/完成时间/关键词/正文）；真数据冒烟 `taskcard-real-smoke.mts` PASS（真实管道回填卡：done / 结果=素材库/环境/采集_先一条试试.md / 完成=2026-09-03 12:25 / 关键词解析正确）。

**⑤ 落档**：本条目；模块设计 §十一 交互 3 标 ✅（并注明实际载体＝任务卡 md front matter，未落 `.zhijuan/tasks`）。

### 2026-09-10 10:37–10:55（第 5 轮：候选 2/3 交汇——任务卡详情「结果」直达素材 + markdown 渲染）

**① 调研**：重读本档案「五」候选 2/3 + 模块设计 §十一（通道 1/3）与 §九（F 素材库：采集回填是「整理后草稿需主人过目」）；读现网代码——CollectionBar 详情 Dialog（结果=纯文本、正文=`<pre>` 裸源码）、DocSection/Library/SectionNav（评估候选 2 原方案「跨板块跳转+选中态」的成本与边界）、AgentPanel（项目已依赖 react-markdown@10 + remark-gfm，.prose 样式在 tokens.css 已有）；确认主进程 `readDoc` 只做 `join(projectDir, rel)` **不防穿越**（防御必须落在调用侧）。本轮为本地自发现，无外网调研需求（沿用模块设计 + 上轮真数据结论，不编造 URL）。

**② 规划**：候选 2 原方案（跳到素材库对应类别并定位）需跨板块跳转 + Library 选中态，属体验层 §十三 边界；而采集栏本身就在素材库页顶部——**详情内只读预览**一步到位（少一跳、不碰 DocSection/路由/体验层），价值等同甚至更优。收敛为本轮：①结果行可点 → 读该素材 → 详情内 ReactMarkdown 渲染预览（可收起；文件不存在显示提示）；②详情正文 `<pre>` 改 `.prose` markdown 渲染（回填是整理后草稿，源码裸显违背「主人过目」产品红线）；③新增 `shared/taskCard.isLibraryResultPath` 纯函数（素材库前缀 + .md + 不含 `..`），防管道坏值把「结果」当任意文件读（readDoc 无防穿越，此处是唯一防线）。**不动**：采集管道 cron、任务卡格式、采集池列表/watcher、DocSection/Library、其余功能；不引新依赖（react-markdown 已在）。验收：三道门 + 无头 UI 冒烟（devShim 补演示素材）。

**③ 落地**：`d877d0a`（src/shared/taskCard.ts + CollectionBar.tsx + devShim.ts（补 采集_演示图书馆.md 演示素材）+ tests/unit/taskCard.test.ts + scripts/cdp-taskcard-ui-smoke.mjs（扩到 6 项断言））。

**④ 验证**：typecheck ✅ / build ✅ / npm test **235/235** ✅（+3）；无头 UI 冒烟 `cdp-taskcard-ui-smoke.mjs` [3][4][5][6] 全 PASS——详情正文 h1 渲染无源码残留、点结果预览出「校园老图书馆/借书卡/可复用的感官细节」、可收起。

⑤ 落档：本条目；模块设计 §十一 交互 3 补注（结果可在详情内预览）；下一步计划已重写。

### 2026-09-10 14:32–（第 6 轮：候选 2 素材库页类别树 + 搜索）

**① 调研**（真实来源，r.jina.ai 直连抓取原文；本机 web_search/searxng 全引擎挂起已确认轮）：
- Obsidian 官方搜索帮助 https://obsidian.md/help/search：搜索默认**文件名 + 正文全文**，默认大小写不敏感；空格分词 = AND；`file:`/`content:` 可限定字段；结果默认按文件名排序。
- Obsidian 官方标签帮助 https://obsidian.md/help/tags：标签命名规则（无空格、`/` 嵌套、大小写不敏感）；素材模板沿用此约定（front matter `标签:`/`tags:`）。
- 结论：织卷素材库搜索取「文件名 + 全文、空格分词 AND、大小写不敏感」语义；本期不做运算符/布尔表达式（小库线性扫盘足够，不引索引依赖）；类别组织沿用「目录=类别」现状（采集管道回填 `素材库/<类别>/` 已按此工作），类别树 UI 补上，不引入标签体系。

**② 规划**：如果说模块设计 §九 布局（左列类别树自由建 + 采集池固定入口；顶部搜索文件名+全文），现状 DocSection 平铺列表差距大——素材与采集池任务卡同列混排（listDocs 递归含采集池）、无类别层级、无搜索、无「新类别」。
本轮收敛为：新组件 `features/library/LibraryBrowser` 替换素材库页中部（CollectionBar/升格助手不动）：左列**类别树**（一级类别 + 数量 + 底部「＋ 新类别」；顶部固定「索引」项）；右列**搜索框（文件名+全文）+ 素材卡列表（预览/标签/时间）+ 点开进 DocEditor（返回按钮）**。对应新增：`main/library.ts`（listLibraryCategories / createLibraryCategory / searchDocs——域逻辑，store 保持减负后不动）、IPC `library:categories`/`library:createCategory`/`docs:search`、shared 纯函数 buildLibraryTree（组装+排序，可单测）、devShim 三 mock + 补第二类别演示素材。
**与设计 §九 的偏差（记档）**：「采集池固定入口」由**顶部常驻 CollectionBar 承担**（比左树更显眼、已验收），类别树不再重复放采集池项——避免同一任务列表双份渲染与回归；如主人认为不妥再调整。
**不动**：DocSection（人物/世界观共用）、CollectionBar 内部逻辑、采集管道 cron、任务卡格式、DocEditor、主题/设计系统；不引新依赖（react-markdown 已有）。
**验收口径**：三道门 + `main/library.ts` 真临时目录冒烟（建类别/枚举/搜索命中）+ 无头 UI 冒烟（devShim：树出现类别并计数、点类别出素材卡、「+新类别」落树、搜索「借书卡」出命中且点击进编辑器）。

**③ 落地**：
- `src/main/library.ts`（域逻辑：listLibraryCategories / createLibraryCategory / searchDocs——store 保持减负后不动）、IPC `library:categories`/`library:createCategory`/`docs:search`、preload 三方法、`shared/types.ts` 增 `LibraryCategory`/`SearchHit`、`shared/libraryTree.ts`（树组装纯函数）。
- `features/library/LibraryBrowser.tsx`（新组件，替换素材库页中部 DocSection）：左列类别树（一级目录+数量+底部「＋ 新类别」+ 顶部「索引」固定项）+ 右列搜索框（文件名+全文、空格分词 AND、排除采集池、350ms 防抖）/素材卡列表（首行预览+标签+更新时间）/点开进 DocEditor（返回按钮、⌘S 保存）。
- `devShim.ts`：三 mock（类别由 docs 推导+extraCats 空类别登记）+ 补「人物/旧茶楼账房.md」演示素材；`Library.tsx` 只换中部组件。
- 顺带修：`createLibraryCategory` 空白名先 trim 再 sanitize（否则「    」会建成「未命名」类别）。

**④ 测试验收**（真实跑通）：
- 三道门：typecheck ✅ / build ✅ / npm test **247/247** ✅（新增 library.test 3 例 + libraryTree.test 2 例，真 fs + electron 桩）。
- 数据层冒烟 `scripts/lib-smoke.tmp.mjs`（临时，用完已删）→ `LIBRARY SMOKE OK`：真临时目录 + 真设置，类别枚举（采集池排除/空目录 count 0）、建类别落盘、重名/空白报错、搜索正文命中带 snippet、文件名命中、排除采集池、AND 无命中，8 项全 PASS。
- 无头 UI 冒烟 `scripts/library-cat-ui-smoke.mjs`（CDP 9224 + devShim）7 项全 PASS：树含索引/人物/环境（计数）→ 点类别出素材卡（预览+标签）→ 「+新类别」建「场景」落树 → 搜索「借书卡」出命中（正文徽章+snippet）→ 点击命中进编辑器（`__ZJ_DOC.rel` 实锤）→ 返回保留搜索态。
- 过程中修的两处：①素材显示名去掉 `.md` 后缀；②预览取首个非空行（含标题行，标题最有辨识度）。

**⑤ 落档**：本条目；模块设计 §九 布局三行标 ✅（含「采集池入口由顶部采集栏承担」偏差说明）；下一步计划已重写。

### 2026-09-10 18:32–19:05（第 7 轮：候选 3 采集任务卡小修——停滞提示 + 提交查重）

**① 调研**（真实来源；本机 web_search/searxng 仍全引擎空返，走 r.jina.ai 直连）：
- GitHub actions/stale README（https://github.com/actions/stale，经 r.jina.ai 抓原文）：任务「停滞/过期」标记的通用惯例＝**按「最后更新时间」做 idle 判定**（默认 60 天标 stale、7 天后关），标记提示但**不删数据**，任何回写/评论都重置计时。织卷映射：任务卡 mtime=管道最后触碰时刻（回填/状态变更都会更新），管道每 20m 应处理一次 → 2 天未动即可判「停滞」；终态（done/failed）恒不算。
- 查重惯例（idempotency）：重复采集的根因＝同一需求被登记两次且都处于非终态；判据用 front matter `需求:` 归一化后比较（去空白），提示不阻止（复采是合法诉求，主人知情即可）。

**② 规划**：候选 3 前半（「列表刷新/去重、过期提示」中后可做部分）＝本次落地：**停滞提示**（列表徽标「停滞 N 天」+ 详情提示行，文案指引删除重发起）+ **提交查重**（发起采集时已有同需求进行中任务 → 表单内警告，不阻止）。搜索命中片段关键词高亮属体验层细节，已登记不排期（本轮不碰）。**不动**：任务卡文件格式、管道 cron、LibraryBrowser/搜索、模块设计 §九 布局。

**③ 落地**：
- `src/shared/taskCard.ts`：新增 `STALE_TASK_MS`（2 天）+ `isTaskStale(status, lastActivityMs, nowMs)` 纯函数（终态恒 false、其余按最后活动 idle 判定——参照 stale-bot）。
- `features/collection/CollectionBar.tsx`：`TaskInfo` 增 `demand/stale`；**统一用 parseTaskCard 解析、删除旧 parseStatus 正则**（重复实现，曾与详情解析分叉）；列表卡片 pending/running 停滞时出「停滞 N 天」徽标（warn 色 + title 说明）；详情状态行下出停滞提示；发起采集表单对同需求（归一化比较、只看 pending/running）弹「已有进行中的需求相同任务」警告，不阻止提交。
- `devShim.ts`：新增演示停滞任务卡（pending、需求「雨夜的码头描写…」、mtime 模拟 3 天前——docsOf 特判，与 `_导演.md` 先例同风格），供无头演示/冒烟。
- 新增 `scripts/collection-stale-ui-smoke.mjs`（无头 CDP 冒烟脚本，入库）。

**④ 测试验收**（真实跑通）：
- 三道门：typecheck ✅ / build ✅ / npm test **266/266** ✅（taskCard 新增 6 例：阈值边界=不算、超 1ms 算、刚登记不算、done/failed 恒不算、running 停顿算、未知状态算）。
- 无头 UI 冒烟 `scripts/collection-stale-ui-smoke.mjs`（CDP 9224 + devShim）**6 项全 PASS**：①列表含「雨夜码头+停滞 3 天」徽标 ②已完成图书馆卡无停滞徽标（不误报）③详情出「任务已停滞 3 天」提示 ④发起采集输相同需求 → 查重警告（含任务名）⑤改文本 → 警告消失 ⑥查重警告下提交按钮未被禁用。

**⑤ 落档**：本条目；模块设计 §十一 补注（停滞提示/查重约定，任务卡载体不变）；下一步计划已重写。

### 2026-09-10 22:32–22:55（第 8 轮：候选 1 任务卡「删除/重发」入口）——候选 3 收尾完成

**① 调研**（真实来源）：
- Obsidian 官方帮助（https://help.obsidian.md/files-and-folders，原文仓库 https://github.com/obsidianmd/obsidian-help/blob/master/en/Files%20and%20folders/Manage%20notes.md，经 r.jina.ai 抓原文）：删除文件**默认进系统废纸篓**（可恢复），另提供 Obsidian trash（库内 .trash）/ 永久删除两个选项——主流惯例＝删除可恢复 + 不强制每次确认。
- VS Code 官方界面文档（https://code.visualstudio.com/docs/getstarted/userinterface）：资源管理器可删除/重命名，误删可从 Local History 恢复。
- 织卷内部先例：项目删除 removeProject 已走 `shell.trashItem`（mac 废纸篓，可恢复）——删除任务卡沿用同一语义。
- 结论：删除＝**废纸篓（可恢复）＋ 确认框**；非终态提示「管道不再处理」、终态仅提示可找回（不烦人）；本机 web_search/searxng 仍空返，信任 r.jina.ai 直连。

**② 规划**：停滞提示文案已指引「删除重发起」，但 store 只有 read/write/list、无删除通道——本轮补全闭环：
- `store.deleteDoc(id, rel)`：走废纸篓（与 removeProject 同先例），失败兜底硬删；防御路径校验（非 md / 空 / 绝对 / `..` 拒绝——readDoc 只 join 不防穿越，删除同样不能开口子）。
- IPC `doc:delete` + preload `deleteDoc`（渲染层唯一入口）。
- CollectionBar 详情 footer：「删除该任务」（all 状态；确认框：非终态红字警示「管道不再处理，如需重采请重发/重新发起」，终态提示可找回；取消不误删）＋「重发任务」（仅 pending/failed；`shared/rebuildTaskCardForRetry` 纯函数重建全新 pending 卡，清结果/完成、保留需求/关键词/类别/来源/创建/正文——管道只认 status: pending，重发即重新入队）。
- **不动**：任务卡文件格式、管道 cron（76de7e949a3b）、LibraryBrowser/搜索、发起采集表单、模块设计 §九/§十一 布局。
- 验收口径：三道门 + 无头 UI 冒烟（删除确认/取消/删除后消失/done 卡差异/重发后停滞徽标消失）+ 真机《素材库验收》卡重发格式校验。

**③ 落地**：
- `src/shared/taskCard.ts`：`rebuildTaskCardForRetry(v: TaskCardView): string`（纯函数；body 空时补标题占位）。
- `src/main/store.ts`：`deleteDoc`（await shell.trashItem → catch 兜底 rmSync；路径校验四连）。
- `src/main/ipc.ts` + `src/preload/index.ts`：`doc:delete` 通道/方法。
- `features/collection/CollectionBar.tsx`：详情 footer（关闭 + spacer + 重发/删除）；确认 Dialog 独立 state（点删除先关详情再开确认，避免嵌套 Dialog 焦点问题）；doDelete/doRetry 后关详情 + refresh（fs 事件双保险）。
- `devShim.ts`：deleteDoc mock（docs.delete + fsEmit）；「任务_演示停滞」被重发（writeDoc 触碰）后从 docsOf 的「3 天前」特判名单移除——否则静态模拟让「重发后停滞徽标消失」永远验不过（真机行为＝写盘更新 mtime）。
- 新增 `scripts/collection-delete-ui-smoke.mjs`（无头 CDP 冒烟脚本，入库）。

**④ 测试验收**（真实跑通）：
- 三道门：typecheck ✅ / build ✅ / npm test **282/282** ✅（taskCard +3 例：done 重建/可逆重建/空正文占位；store +4 例：废纸篓正常路径/兜底硬删文件消失/路径校验不调废纸篓/不存在返回错误）。
- 无头 UI 冒烟 `scripts/collection-delete-ui-smoke.mjs`（CDP 9224 + devShim）**7 项全 PASS**：①详情含重发/删除按钮 ②重发后卡仍在列表且停滞徽标消失 ③删除确认非终态文案（任务尚未完成/管道不再处理）④取消不误删、无确认框残留 ⑤确认删除后卡从列表消失（只剩图书馆卡）⑥done 卡无重发按钮 ⑦done 卡确认文案为已终态。
- 真机数据层校验（临时 bundle + node）：真实《素材库验收/任务_先一条试试.md》parse（done/结果/完成）→ `isTaskStale(done)`=false（**停滞判定不误报实锤**）→ 重发重建后 status=pending、结果清空、需求/关键词/类别/创建保留、front matter 与管道约定一致（管道只认 status: pending，会正常处理）。

**⑤ 落档**：本条目；模块设计 §十一 补注（任务卡删除/重发交互约定）；下一步计划已重写。

### 2026-09-11 02:32–02:55（第 9 轮：候选 1 全局 ⌘K 面板接入素材全文搜索）

**① 调研**（真实来源，r.jina.ai 直连抓取原文；本机 web_search/searxng 全引擎空返为既定轮况）：
- Apple HIG Search fields（https://developer.apple.com/design/human-interface-guidelines/search-fields）：「输入即搜（start search immediately when a person types）」「最相关优先、结果**分组**以助查找（consider categorizing）」「可考虑显示最近/建议搜索」——织卷映射：⌘K 内素材结果独立分组；q 为空不预载（面板是瞬态层，避免噪点）。
- Obsidian 官方 Search 帮助（https://help.obsidian.md/plugins/search）：搜索默认**文件名 + 正文全文**、空格分词 AND、大小写不敏感（与织卷 searchDocs 口径完全一致）；Obsidian 把搜索做成 ⌘⇧F 独立入口，但织卷**不另开独立搜索入口**——素材页内搜索 + ⌘K 面板搜索共用同一 docs:search 域逻辑（一套口径两入口，避免漂移；独立搜索页与命令面板并存是 Obsidian 的重型做法，对织卷是重复建设）。
- 并发检查：开工前读 04-体验层.md——体验层当前候选为 I2 快捷键总表收尾/侧栏体检/空态质感，未占用 CommandPalette；相关文件近期修订均属本模块（0d1b7f3）与已提交功能，无冲突。

**② 规划**：素材库页内搜索只能在素材页用，「写作中按 ⌘K 搜素材再点击打开」是键盘优先闭环（D-V2-12 agent-first + 素材积累）缺的一环，且实现零新依赖（docs:search IPC、cmdk 1.1.1 onValueChange、素材页 DocEditor 均在）。收敛为：⌘K 面板加「素材 · 全文搜索」分组——输入即搜（防抖 300ms、AND、排除采集池、limit 8 防噪）、命中显示「文件名/正文」方式徽章 + 片段（truncate）、点击/回车直达素材页 `?doc=<相对项目根路径>` 打开编辑器。**不动**：docs:search/searchDocs 域逻辑、LibraryBrowser 现有搜索与列表、采集池、设置；不引新依赖。验收口径：三道门 + 无头 UI 冒烟（真跳转 + 编辑器实锤 + 回归）。

**③ 落地**：
- `CommandPalette.tsx`：受控搜索词 q（onValueChange）+ 防抖搜索 effect（seq 竞态防护）+ 「素材 · 全文搜索」分组（空输入不显示；loading 占位「正在搜索素材…」；item = 文件名 + 命中方式徽章 + snippet）；打开面板时重置 q，不残留。
- `Library.tsx`：`?doc=` 消费——读参数传 LibraryBrowser 后 `setSp({}, {replace:true})` 清参（与 novel?ch= 同策略，刷新不残留）。
- `LibraryBrowser.tsx`：新增 `openDoc` prop——消费时 setEditorRel + 退出搜索态（openDoc 非 null 才触发，清参后变 null 不重复）。
- devShim 无需改（searchDocs mock + 演示素材已齐）；新增 `scripts/cmdk-material-smoke.mjs`（无头 CDP 冒烟，入库）。

**④ 测试验收**（真实跑通）：
- 三道门：typecheck ✅ / build ✅ / npm test **308/308** ✅（纯 UI 接线，复用既有域函数与 novel?ch= 先例，无新增单测）。
- 无头 UI 冒烟 `scripts/cmdk-material-smoke.mjs`（CDP 9224 + out/renderer 带缓存破坏）**19 项全 PASS**：正文页 ⌘K 面板打开→页面/打开章节/打开项目三组仍在→空输入不显示素材组→搜「旧物」命中「追忆型开头」（正文徽章 + snippet）→点击直达素材页→`__ZJ_DOC.rel` 实锤打开正确文件→编辑器显示标题→doc 参数被消费清除→搜「茶楼」文件名命中→点击打开→无结果不显示素材组 + 空态文案含素材提示→无 JS 异常→回归「打开章节 雾港」仍可用。
- 调试踩坑（记录）：①断言时机——素材组 heading 在「正在搜索…」占位阶段就出现，过早读 items 会拿到占位 disabled 项，必须等真实命中项渲染；②跳转断言——Library mount 后**立即**清参（replace），带 `doc=` 的 hash 是瞬态，断言应等 `library` 页 + `__ZJ_DOC.rel` 而非 URL 参数。

**⑤ 落档**：本条目；下一步计划已重写（候选 1 换为项目首页增强）。

**提交**：b2d4971 feat(平台层): ⌘K 面板接入素材全文搜索——输入即搜（防抖300ms/AND/排除采集池/取前8条），点击直达素材页 ?doc= 打开编辑器（消费后清参）；LibraryBrowser 支持外部打开；新增 cmdk-material-ui-smoke 冒烟 19 项

### 2026-09-11 19:30–19:50（本轮：候选 1 导入落点可见性落地——设置页显示生效库根）

**① 调研**（真实来源，curl r.jina.ai 抓原文；web_search/searxng 空返或跑偏为既定轮况）：
- Apple HIG Settings（https://developer.apple.com/design/human-interface-guidelines/settings）：「Avoid using settings to ask for setup information you can get in other ways」「生效结果别藏着，别让用户暂停任务去猜」——映射：库根输入框显示的是**配置值**，留空 ≠ 无效（D-V2-8 决策落默认位/老目录保持原地），设置页应展示**生效值**。
- Obsidian 官方 Create a vault（https://help.obsidian.md/Getting+started/Create+a+vault）：vault 即本地文件夹，官方反复强调用户要能知道库在哪；Obsidian 设置/关于区展示当前库路径为同类先例。
- 并发检查：开工 git status 干净；最近 90 分钟提交均为创作层/智能层（add88f1 等），与本模块文件无交集；反馈清单无平台层待落实条目，安全开工。

**② 规划**：为什么现在做——库根留空时项目实际落到位（老目录/工作区/项目库）对用户不可见，迁移排查易误；工作区区已有「当前：<dir>」只读先例（workspaceStatus），项目库侧缺失。怎么做——preload 暴露**既有** `app:getPaths` IPC（main 侧早已实现 `documents: libraryRoot()`，**零新增 IPC**），Settings 项目库 Card 输入框下加「当前：<生效库根>」只读行（样式同工作区先例，保存后刷新）。涉及文件：preload/index.ts（+3）、devShim.ts（getPaths mock 与真机字段对齐：原 mock 返回 `{documents, libraryRoot}` 与真机 `{documents, defaultLibrary}` 口径不一致）、Settings.tsx（state/refreshLibrary/卡片布局）。**不动**：settings.ts 库根决策、app:getPaths handler、存储结构；不引依赖。验收口径：三道门 + 无头冒烟（默认展示 → 改路径保存刷新 → 清空回默认位）。

**③ 落地**：见上；新增 `scripts/libpath-ui-smoke.mjs`（入库，3 步）。

**④ 测试验收**（真实跑通）：
- 三道门：typecheck ✅ / build ✅ / npm test **414/414** ✅（settings.test.ts 已覆盖 libraryRoot 三态决策，无需新单测）。
- 无头 UI 冒烟 `scripts/libpath-ui-smoke.mjs`（CDP 9224 + out/renderer 带缓存破坏）**3 项全 PASS**：设置页默认显示「当前：~/Documents/织卷工作区/项目库」→ 填 /tmp/zj-lib-test 保存 → 当前行刷新 → 清空保存 → 回默认位。
- 踩坑（记录）：断言「当前：」行不能 `find` 首个 p——工作区卡与项目库卡各有一行「当前：」，必须按目标路径串（含「/项目库」）区分，否则 3 步全假红（DOM 实际正确）。

**⑤ 落档**：本条目；下一步计划已重写（候选 1 完成，候选 2 转正）。

**提交**：d0fd64e feat(平台层): 设置页项目库卡显示生效库根——preload 暴露既有 app:getPaths（零新增IPC），devShim mock 与真机字段对齐，保存后刷新；新增 libpath-ui-smoke 冒烟3项

### 2026-09-11 22:30–22:45（第 15 轮：候选 2 冒烟脚本环境加固收口 + 候选 1 ⌘K 复核关闭；提交 ad96407）

**① 调研**（真实来源，curl r.jina.ai 抓原文；web_search/searxng 空返为既定轮况，仅搜到 CSDN/Azure 现场外内容未采信）：
- Electron 官方 app API（https://www.electronjs.org/docs/latest/api/app，r.jina.ai 抓取）：`userData`＝「存放应用配置文件」的目录（默认 appData/应用名）；官方提供 `app.setPath(name, path)` 运行时覆盖指定目录（目录不存在会抛错，须先 mkdir）——**测试/冒烟把 userData 指到临时目录是官方认可的隔离口**；织卷 `electron-stub.mjs` 以 `process.env.ZJ_USERDATA` 模拟同语义（getPath('userData') 优先读环境变量），冒烟显式干净目录＝对齐官方做法，非本地 hack。
- 代码体检：引用 electron-stub 的冒烟脚本共 21 个，其中 16 个已设 ZJ_USERDATA（at-refs-smoke 先例＝固定路径+rmSync 清空；import-project-smoke＝mkdtemp 动建），**缺 5 个**（engine-smoke / engine-sync-smoke / skill-command-live / think-probe / zj-edit-smoke——全部真模型脚本）；`src/main/agent/engine.ts:6` 经 `projectDir`→`libraryRoot()`（settings）定位项目库，**残留 userData 的坏 settings 会直接让引擎冒烟指向空路径**（2026-09-11 已踩：libraryRoot 定位空路径、readDoc 全 null）。
- 并发检查：开工 git status 干净；90 分钟内提交为创作/智能层（df213c8 等），与本模块文件无交集；反馈清单无平台层待落实条目（F-20260911-01 属体验层落实中），安全开工。

**② 产品规划**：候选 2 收口——5 个真模型脚本统一补「显式 `ZJ_USERDATA=<固定干净目录>` + rmSync 清空」（沿用 at-refs-smoke 先例，不用 mkdtemp：固定路径可复现、便于排查）；为什么现在做：这 5 个是「引擎/命令/编辑」链路的核心回归脚本，残留库根会造**假绿（读到空库照样跑）或假失败（找不到项目）**，浪费时间且掩盖真实问题；顺带完成候选 1（⌘K 章节组复核＝重跑 cmdk-ui-smoke，无问题则按计划关闭该项）。**不动**：electron-stub 默认值（保留 `/tmp/zj-smoke-userdata` 兜底，防未来新脚本忘设）、已加固 16 个脚本、src/ 任何代码、采集管道。验收口径：①对照探针实证（污染 settings 下 dirty→坏路径、clean→回退真实库）；②加固后的真模型脚本端到端跑通（证明隔离不破坏链路）；③三道门；④⌘K 冒烟回归。

**③ 技术落地**（5 文件，各 +4~5 行，纯脚本头部加固，零 src 改动）：`engine-smoke.mjs` / `engine-sync-smoke.mjs` / `skill-command-live.mjs` / `think-probe.mjs` / `zj-edit-smoke.mjs`——import `rmSync`，`process.env.ZJ_USERDATA='/tmp/zj-smoke-<工具名>'` + `rmSync(force)`；目录名各自独立（engine/engine-sync/skill-live/think-probe/zjedit），互不共享；注释写明缘由。

**④ 测试验收**（真实跑通）：
- 对照实证（临时探针 bundle settings.ts + electron-stub，已删）：**dirty**（无 ZJ_USERDATA，`/tmp/zj-smoke-userdata` 预写 `{"libraryRoot":"/tmp/已删除的临时库"}`）→ `libraryRoot()=/tmp/已删除的临时库`（坏路径实锤）；**clean**（ZJ_USERDATA=/tmp/zj-smoke-engine 干净）→ `/Users/USER/Documents/织卷项目库`（回退正确）——加固价值实证。
- 三道门：typecheck ✅ / build ✅ / npm test 42 文件 **421 例全绿** ✅（2026-09-11 22:35 实测）。
- 真模型端到端（加固后 think-probe）：`LOCAL_LLM_KEY=local node scripts/think-probe.mjs` → think 事件 **124 段 / 994 字**、工具流 `["zj_read_doc"]`、读真实项目「agent冒烟」正文并正确总结——加固未破坏引擎链路（LLM 127.0.0.1:8888 在线）。
- 候选 1 复核：`scripts/cmdk-ui-smoke.mjs` 重跑 **SMOKE PASS**——组序 `[页面,最近素材,打开章节,打开项目,帮助]`，章节组在最近素材后未被削弱，过滤「雾港」→ 直达 `novel?ch=` + `__ZJ_DOC.rel=正文/第01章_雾港.md` 写入编辑器实锤，无 JS 异常。**该项确认无问题，按计划关闭**。
- 收尾：清除 `/tmp/zj-smoke-userdata` 预写的坏 settings（所有脚本现已显式隔离，该目录不再被读，防未来新脚本复踩）。

**⑤ 落档**：本条目；候选 1/2 均完成，下一步计划重写（候选 3 转正为候选 1 调研项）。

**提交**：ad96407 fix(平台层): 5个真模型冒烟脚本显式干净userData，防残留settings污染libraryRoot

### 2026-09-12 01:30–02:00（第 16 轮：候选 1 素材检索性能摸底 + 索引立项调研；顺带修 devShim 搜索口径；提交 <待填>）

**① 调研**（真实来源 + 实测）：
- 本地摸底（临时脚本 esbuild bundle `main/library.searchDocs` + electron-stub，已删）：真实库素材规模——`素材库验收` 3 卡 6.3KB（最大 5.7KB 采集卡）、`织卷smoke` 2 卡 353B、agent冒烟 1 卡 131B，各项目全量搜索 **0.1ms** 无感；**合成放大**（2.5KB/卡）：N=200→3.1ms、N=500→7.3ms、N=1000→13.8ms、N=2000（≈5MB）→**29.0ms**（无命中=全扫最坏口径，≈6ms/MB）；命中词场景因 `limit=50` 早退，2000 卡仅 1.6ms（但结果为目录遍历序、无相关度排序——模块设计「全文关键词检索即可」口径下不追）。调用侧防抖已有：LibraryBrowser 350ms、CommandPalette 300ms，高频全扫焦虑不存在。
- 联网（web_search 走 searxng）：FlexSearch（https://gitcode.com/Cangjie-TPC/flexSearch4cj/overview，「快速、零依赖」）与 MiniSearch（https://ai.so.com/search/…，「轻量级、零依赖、内存索引、毫秒级」）均确认为纯 JS 零依赖内存索引，规模上万文档才显优势；**SQLite 车道被否**：endoflife.date/electron 支持矩阵（r.jina.ai 抓取）确认 Electron 31（v31.7.7）内嵌 Node 20（https://endoflife.date/electron）→ 无 `node:sqlite`（22.5+），要 SQLite 得引 better-sqlite3 原生依赖，违背「零重量依赖」约束。
- **结论：`.zhijuan/index` 索引立项暂缓**——全扫 29ms@5MB（≈4000 张素材卡量级），当前与数年可预期规模内完全够用；触发条件（谁先到先立项）：单项目素材库 **md 数 >3000 或总字节 >20MB**，届时首选 minisearch/flexsearch（纯 JS 零依赖），不选 SQLite。D-V2 一期「零结构化存储」立场保持。
- 代码体检顺带发现（2026-09-11 已踩两次的同类教训）：**devShim.searchDocs 与真机 main/library.searchDocs 不同口径**——① dev 签名 `opts` 无 `limit`（真机默认 50；CommandPalette 传 `limit:8` 在 dev/无头下被忽略→全量返回，与真机行为不一致，冒烟可能假绿/假红）；② snippet 起点 dev 取 `terms[0]` 位置，真机 `snippetOf` 取**各 term 最早出现位置**（min）。

**② 产品规划**：候选 1（性能摸底+索引立项）调研完成、结论=暂缓（含触发条件，落档）；与此同时把「devShim 搜索口径对齐」作为本轮小步——为什么现在：mock 口径漂移已两次实踩（listDocs/涉及人物），这次是第三次（searchDocs limit），一次修一处太零碎，本轮先修 searchDocs（真机侧本来就有 limit，dev 侧缺实现），其余 mock 字段（listDocs/readDoc/recentLibraryDocs 等）登记为候选 2 一次性体检。**不动**：真机 searchDocs 域逻辑、CommandPalette/LibraryBrowser 调用侧、采集管道；不引依赖。验收口径：三道门 + 无头冒烟（limit 生效/默认 50/排除采集池/空 query/snippet 逐字一致/多词 AND，10 步）。

**③ 技术落地**：`src/renderer/src/lib/devShim.ts` searchDocs——opts 补 `limit?: number`、`const limit = opts?.limit ?? 50`（同真机）、循环头 `if (out.length >= limit) break`（同真机早退语义）；snippet 改 min 口径（照搬真机 snippetOf 的 idx 循环）。新增 `scripts/devshim-search-smoke.mjs`（无头 CDP 9224 + out/renderer 缓存破坏，入库可复用）。

**④ 测试验收**（真实跑通）：
- 三道门：typecheck ✅ / build ✅ / npm test 42 文件 **427 例全绿** ✅（纯 mock 修复，无新增单测）。
- 无头 UI 冒烟 `scripts/devshim-search-smoke.mjs` **10 项全 PASS**：devShim 就绪→探测词命中数（灯 3/演示 3）→limit:2 返回 ≤2 且=全量前 2 条→limit:1 ≤1→默认 50 与显式 50 一致→excludePrefix 排除采集池（无任务卡）→空 query []→内容命中 snippet 与真机 snippetOf 算法（readDoc 原文重算）逐字一致→多词 AND ≤单词数。

**⑤ 落档**：本条目；下一步计划重写（候选 1 完成；候选 2 转正为候选 1）。

**提交**：cdeb6e7 fix(平台层): devShim 搜索口径对齐真机——补 limit（默认50）+snippet 取最早词位置，冒烟10步；落档检索性能摸底与索引暂缓结论

### 2026-09-12 04:30–05:10（第 17 轮：候选 1 采集任务卡状态机体检；提交 ddd22f0）

**① 调研**（真实来源+实测）：
- 权威源核对：模块设计 §11（任务卡 front matter 约定/通道）＋ ROADMAP/README（管道 cron 常驻声明）。
- **体检最大发现＝管道侧失联实锤**：采集管道 cron「织卷素材库采集」(76de7e949a3b) **不在活动任务列表**——`hermes cron list` 仅 7 个活动任务（四模块推进器＋日报/批注/批注日报），无 76de；executions.db 中 76de 仅 3 条 completed（2026-09-03 12:11/12:16/12:17，`source=direct`＝S5 验收时手工触发），此后 9 天零执行；`cron/output/` 无该任务目录；2026-09-11 14:18 curator 备份 cron-jobs.json（7 任务）同样没有。**推论：2026-09-09 推进器体系重构重写 jobs.json 时被连带丢弃**（README 明言它属系统本体、不在推进器体系内）。影响：App 侧新登记任务卡将永远 pending，2 天后出「停滞 N 天」徽标（App 信号正确，但消费方不存在）。
- 重建处方（推进器边界=不改 .hermes，故只提供处方，执行者=主会话/主人）：`hermes cron create '20m' --name 织卷素材库采集 --workdir ~/Desktop/织卷 --deliver origin '扫描各项目库 素材库/采集池 status:pending 卡 → web_search/web_extract/专用浏览器采集 → 清洗成素材草稿写 素材库/<类别>/采集_<任务名>.md → 回填任务卡 status:done/failed+结果/完成'`；monitor gate 用 `--monitor-script`（输出 pending 清单，无活不唤醒，与老门卫同语义）。`hermes cron create --help`（本机 CLI）已确认 --monitor-script/--skill/--name 参数存在。
- 既有冒烟覆盖核对：unit taskCard 21 例（parse/rebuild/stale/isLibraryResultPath）+ UI cdp-taskcard 6 步 / collection-stale 6 步 / collection-delete 7 步 + 真盘 taskcard-real-smoke（素材库验收 done 卡解析 PASS）。**App 侧状态机覆盖完整，无缺口**。

**② 产品规划**：体检结论＝App 侧健康、管道侧失联（见上，登记为 P1 跨模块事项）；小修一项：任务卡 FM `需求` 旧代码 `slice(0,120)` 静默截断——管道按 FM 需求当采集规格（§11 约定），长需求被砍=规格丢失；且提交查重按**已截断**需求比较，>120 字相同需求**漏警**（会重复采集）。修：FM 需求不截断（正文标题仍截 20 字）。**不动**：任务卡格式、管道 cron 本体、查重/停滞/预览既定行为；不引依赖。验收口径：三道门＋无头冒烟（135 字需求→详情未截断；同需求复提→查重警告出现）。

**③ 技术落地**：`CollectionBar.submit`：`'需求: ' + demand.replace(/\n/g,' ')`（删 `.slice(0,120)`）；新增 `scripts/taskcard-spec-ui-smoke.mjs`（无头 CDP 9224 + devShim，6 步：前提>120字→提交→详情含尾标未截断→复提出查重警告）。

**④ 测试验收**（真实跑通）：三道门 typecheck ✅ / build ✅ / npm test 42 文件 **432 例全绿** ✅；无头 UI：taskcard-spec **6/6** ✅、collection-stale 6/6 ✅（回归）、cdp-taskcard 6/6 ✅（回归）、collection-delete 7/7 ✅（回归）；真盘 taskcard-real-smoke PASS ✅。

**⑤ 落档**：本条目；下一步计划重写（候选 1 完成；管道 cron 恢复登记为 P1 跨模块事项，重建处方见本条）。

**提交**：ddd22f0 fix(平台层): 采集任务卡需求不再截断120字（FM需求=完整规格+长需求查重不漏警）；新增taskcard-spec-ui-smoke 6断言

### 第 18 轮（2026-09-12 07:30–08:35，候选 2：采集管道端到端复验 + 意外修掉真竞态）

**① 调研**（真实来源+实测）：
- 权威源核对：模块设计 §11（任务卡/通道约定）、taskCard.ts（parse/isLibraryResultPath/isTaskStale）、library.ts（类别树/searchDocs 排除采集池）、store.ts（writeDoc 自动 ensureDir）、CollectionBar.tsx（submit FM/refresh/详情预览）——全部重读确认现状。
- 既有套路复用：数据层 bundle 模式见 history-smoke.mjs（esbuild + electron-stub + 临时 ZJ_USERDATA 干净目录）；无头 UI 模式见 cdp-taskcard/collection-stale（CDP 9224 + devShim + 缓存破坏参数）。
- 上网佐证 React 18 自动批处理机制（多 setState 同一 tick 合并渲染）：https://juejin.cn/post/7102344887852335112 、 https://juejin.cn/post/7473891873191919642 ——本轮修复的机制依据（配合实证）。
- **实测发现真 bug（e2e 冒烟走出来的）**：模拟管道回填＝任务卡+素材**两次连续 writeDoc**（真机 chokidar 会同样连发）时，CollectionBar 任务卡列表**不自动刷新**——fs 事件广播本身正常（探针：onFsEvent 收到 2 条）、useFsEvents 也入列，但列表 TaskInfo.status 停在 pending。逐步定位（fsprobe4/5 对照）：**React 18 自动批处理把同 tick 两条 fs 事件并入一次渲染，`useEffect` 读 `events[events.length-1]` 只见最后一条 `素材库/环境/…`，任务卡（采集池）广播被「跳过」→ 条件不匹配不 refresh**。只写任务卡（单事件）则正常刷新——复现链完整。

**② 产品规划**：注册候选 2 本体（采集管道端到端复验脚本）+ 顺手修上述竞态（一行，属 F 采集域）：
- 为什么现在做：候选 1（恢复管道 cron）需主会话执行、未落地，App 侧「观察外部回填」闭环长期没有自动验证资产生成——先补织卷侧闭环（不依赖 cron），并顺手把复验逼出的刷新竞态修掉（正是「真端到端复验」的价值）。
- 怎么做：数据层真盘复验（真 store/library/taskCard + 临时库，16 断言：建项目→发起采集 pending→管道回填 done+素材→类别树/搜索/最近素材闭环→失败卡/坏路径防线）+ 无头 UI 复验（发起采集→模拟管道两写→断言**无需手动刷新**状态流转 done→详情结果/完成→结果预览 markdown→收起，11 断言）+ CollectionBar useEffect 修一行。
- **不动**：任务卡格式、管道 cron 本体、查重/停滞/预览行为、其它页面；Novel/Outline/DocSection/LibraryBrowser 存在同一「只看最后一条」模式，但属其它模块域（正文/大纲/文档区）或前缀宽风险低，登记给对应层，本轮不发散。
- 验收口径：三道门 + 数据层 16 断言 + 无头 UI 11 断言。

**③ 技术落地**：
- 新增 `scripts/collection-e2e-smoke.mjs`（数据层 bundle 冒烟，见上）。
- 新增 `scripts/collection-e2e-ui-smoke.mjs`（无头 UI 冒烟；提交前注入 writeDoc 记录钩子拿**真实任务卡路径**——不猜文件名；回填用页面 `window.zhijuan.writeDoc` 两写，devShim 自动广播 fs 事件＝真机 watcher 语义）。
- `CollectionBar.tsx` 事件监听修复：`events[events.length-1]` 判定 → `events.some((ev) => ev.path.startsWith('素材库/采集池'))`（扫描窗口内任一采集池事件即刷新；幂等，无重复副作用）。

**④ 测试验收**（真实跑通）：三道门 typecheck ✅ / build ✅ / npm test 42 文件 **433 例全绿** ✅；数据层 `node scripts/collection-e2e-smoke.mjs` **SMOKE OK（16/16）**✅；无头 UI `node scripts/collection-e2e-ui-smoke.mjs` **SMOKE ALL PASS（11/11）**✅（修复前 10/11、仅「状态流转 done」断言失败＝bug 复现；修复后全绿）；截图 `~/Pictures/zhijuan/collection-e2e-0830.png`（回填后列表 done + 详情结果预览）。

**⑤ 落档**：本条目；下一步计划重写（候选 2 完成；其余页面同模式排查登记为候选 3）。

**提交**：18dda8c feat(平台层): 采集管道端到端复验（数据层16断言+无头UI 11断言）并修 CollectionBar fs 事件批处理竞态（连续写盘只刷最后一条漏触发）

### 第 19 轮（2026-09-12 10:30–12:00，候选 2：devShim 与真机口径一次性体检——抽共享纯函数根治分叉）

**① 调研**：
- 前提检查：git status 干净；创作层 38311a8（10:08，shared/outline 三端同口径 + devShim 索引重建）在 90 分钟内但与本轮改动面（devShim 解析类 mock / shared 新增）不同区；反馈清单无平台层「待落实/落实中」条目。
- 逐字段比对 devShim vs 真机（store.ts / library.ts / slices.ts / settings.ts / fmatter.ts / paths.ts / history.ts / ipc.ts），实锤差异 9 处：
  - **P1** devShim.listChapters 手写正则全文匹配（`章号:\s*(\d+)` 无视 front matter 边界、不剥 `#` 注释、空数组产生 `涉及人物: []` 键），且排序按 fm 章号而非文件名 numOf——与真机 extractFrontMatter + ok 判据（任一关键字段）+ Number 归一 + numOf 排序全不同（2026-09-10 已实踩同源「mock 假绿」，本轮 P5/P8 之祸根）。
  - **P2** devShim.listSlices 手写正则解析块、缺 `时间` 字段、排序口径与真机（extractFrontMatter + noOf + localeCompare）不同。
  - **P2** devShim.getPaths.documents 只走「设置非空→工作区/项目库」，**漏老默认位 legacy 分支**；本机实况 `~/Documents/织卷项目库` 存在且非空 → 真机 libraryRoot() 默认走 legacy，mock 却显示 工作区/项目库（libpath-ui-smoke 原断言「与真机同口径」实际从未对过真机）。
  - **P2** devShim.createLibraryCategory / renameChapter 内联复制 sanitizeFile 逻辑（无 import，易漂移）。
  - **P3** listDocs 未按 mtime 排序（真机 mtime desc）；searchDocs.mtime=now（与 docsOf 的「导演板/停滞卡特判」不一致）；deleteDoc 无路径校验（真机防穿越）；writeDoc 版本化仅 正文/（真机 isVersionedRel＝正文/ ∪ 大纲/审读_*）。
- 上网佐证（来源记档）：mock 复制生产逻辑必然漂移，单源真相/DRY 是通行工程原则 https://en.wikipedia.org/wiki/Don%27t_repeat_yourself ；仓库先例=38311a8 shared/outline 三端同口径（真机/索引/devShim）已确立同模式；本地 searxng 两次返回噪音（与主题无关），不再纠缠。
- 结论：与其在 devShim 里再抄一遍逐字段修，不如把「章节列表/切片枚举/库根决策」抽成 shared 纯函数，真机与 devShim **共用同一实现**（根治分叉），单测直证口径。

**② 产品规划**：
- 为什么现在做：候选 2 到期；2026-09-11 三次实踩（listDocs 未剥 .md、涉及人物解析缺字段、searchDocs 缺 limit/起点）全是「mock 与真机分叉」同源事故，逐字段修补治标；本轮把三个易分叉的「解析/决策」逻辑抽 shared 纯函数（真机 store/slices/settings 与 devShim 共用），并顺手对齐其余 6 处小口径，一次收口。
- 怎么做（涉及文件）：
  - 新增 `src/shared/chapters.ts`：`listChapterEntries(sources)`（移植 store.listChapters 全逻辑：extractFrontMatter / ok 判据 / 章号 Number 归一 / 文件名 numOf 排序 / countWords）。
  - 新增 `src/shared/slices.ts`：`listSliceEntries(sources)`（移植 main/slices.listSlices：extractFrontMatter / 切片 / 时间 / 涉及人物 / 章号排序）。
  - 新增 `src/shared/settingsLogic.ts`：`resolveLibraryRoot(opts)`（移植 settings.libraryRoot 决策链：设置非空→legacy 存在且非空→工作区/项目库）。
  - `src/main/store.ts`、`src/main/slices.ts`、`src/main/settings.ts` 改用共享函数（**行为不变**，重构等价）。
  - `src/renderer/src/lib/devShim.ts`：listChapters / listSlices / getPaths 改调共享函数；createLibraryCategory / renameChapter 用 sanitizeFile；listDocs 补 mtime desc 排序；searchDocs.mtime 抽 docsOf 同口径 helper；deleteDoc 补路径校验；writeDoc 版本化条件加 大纲/审读_。
  - 更新 `scripts/libpath-ui-smoke.mjs`（默认/清空断言改 legacy 位，与真机实况一致）。
  - 新增单测 `tests/unit/chapters.test.ts`、`tests/unit/sliceEntries.test.ts`（切片纯函数；真机集成测试 `tests/unit/slices.test.ts` 已有、保持不动）、`tests/unit/settingsLogic.test.ts`；新增无头 UI 冒烟 `scripts/devshim-parity-ui-smoke.mjs`（页面直调 window.zhijuan 断言口径）。
- 不动：真机行为语义（重构等价、既有 433 例单测护栏）、任务卡格式、管道 cron 本体、其它页面；候选 3（其余页面「只看最后一条 fs 事件」）仍留给下一轮。
- 验收口径：三道门（typecheck / build / 全量单测）＋ 真机数据层行为等价（既有单测全绿＋新增共享函数单测）＋ 无头 UI 冒烟（libpath-ui-smoke 改后、devshim-parity-ui-smoke）真实跑通。

**③ 技术落地**：
- 新增 `src/shared/chapters.ts`（listChapterEntries：extractFrontMatter / ok 判据 / 章号 Number 归一 / 文件名 numOf 排序 / countWords）、`src/shared/slices.ts`（listSliceEntries：切片/时间/涉及人物/章号排序）、`src/shared/settingsLogic.ts`（resolveLibraryRoot：设置非空→legacy 存在且非空→工作区/项目库）。
- `store.listChapters` / `main/slices.listSlices` / `settings.libraryRoot` 改为收集 fs 实况后调用共享函数（行为等价、原逻辑仅搬移）。
- `devShim`：listChapters/listSlices/getPaths 改调共享函数；createLibraryCategory/renameChapter 用 sanitizeFile（含「docs 已有类别」重复检测）；listDocs 补 mtime desc 排序；searchDocs/listDocs 的 mtime 统一到 docsOf 特判 helper（devMtime）；deleteDoc 补真机同路径校验；writeDoc 版本化条件=正文/ ∪ 大纲/审读_*；**冒烟揪出第 10 处差异并顺手修**：历史版本文件名缺 `.md`（真机 writeSnapshot 带扩展名）——snapNameDev + '.md'。
- 新增单测 `tests/unit/chapters.test.ts`（6 例）/ `tests/unit/sliceEntries.test.ts`（4 例；真机集成测试 `tests/unit/slices.test.ts` 保持不动）/ `tests/unit/settingsLogic.test.ts`（5 例）；新增无头冒烟 `scripts/devshim-parity-ui-smoke.mjs`（页面直调 window.zhijuan，11 步）。
- 更新 `scripts/libpath-ui-smoke.mjs`：默认/清空断言从 工作区/项目库 改为 老默认位（与真机实况一致——原断言语义从未对过真机）。

**④ 测试验收**（真实跑通）：三道门 typecheck ✅ / build ✅ / npm test **46 文件 459 例全绿** ✅（真机集成与新增纯函数用例都在）；无头冒烟 `node scripts/devshim-parity-ui-smoke.mjs` **11/11 ALL PASS**（其中「正文含章号:9 但无约定头 → fm=null」「涉及人物: [] 不产生键」「审读历史带 .md」三步直接区分新老口径，修复前会挂）；`node scripts/libpath-ui-smoke.mjs` **3/3 PASS**（设置页真实路径显示 legacy 位）；回归 `at-mention-ui-smoke`（ALL OK）/ `history-ui-smoke`（SMOKE PASS）/ `chapter-prefill-ui-smoke`（OK，涉及人物预填「林晚, 陈默」）全过。

**⑤ 落档**：本条目；下一步计划重写（候选 2 完成；候选 3「其余页面 fs 事件同模式排查」升级为候选 2；本轮体检尾巴登记候选 3）。

**提交**：5d41d0f feat(平台层): devShim 与真机口径一次性体检收口——章节列表/切片枚举/库根决策抽 shared 纯函数（store·slices·settings 与 devShim 共用同一实现，根治 mock 分叉）；顺带对齐 7 处小口径（getPaths legacy 分支、sanitizeFile、listDocs 排序、searchDocs mtime、deleteDoc 校验、writeDoc 版本化含审读、历史文件名带 .md）；单测 19 例 + 无头冒烟 11 步 + libpath 断言语义修正

### 第 20 轮（2026-09-12 13:30–14:05，候选 2：其余页面「只看最后一条 fs 事件」排查与根治）

**① 调研**（真实来源+实测）：
- 前提检查：git status 干净；创作层 48fa49c（12:45，文档落档）与本轮改动面无交集；反馈清单无平台层「待落实/落实中」条目（F-20260912-01/02 均归体验层且已解决）。
- 五个消费端全部核实：`useFsEvents`（features/fs，60 行内，50 条窗口）与四页 `events[events.length-1]` 判定原文（Novel.tsx L187-191 前缀「正文/」、Outline.tsx L62-65「大纲/」、DocSection.tsx L59-62「relDir+'/'」、LibraryBrowser.tsx L131-134「素材库/」）+ CollectionBar L100-105 `.some` 修复（18dda8c）。
- 主进程链路核实：store.ts `watchProject` 用原生 fs.watch recursive，逐事件 `broadcastToAll`→`webContents.send('fs:event')`（无去抖/合并）；devShim `fsEmit` 在 writeDoc 体内同步广播——两者都可能在**同一 JS tick 内产生多条事件**（React 18 自动批处理并入一次渲染；机制佐证 https://segmentfault.com/a/1190000040908767 、https://cloud.tencent.com/developer/article/1843055 ），此时 `events[events.length-1]` 只看到末条：混合前缀批量（如 正文/ + .zhijuan/proposals/、素材库/ + 人物/）会漏刷新（CollectionBar 实锤同机制，e2e 2026-09-12）。
- 逐页风险判定：**LibraryBrowser**——采集管道回填（采集池任务卡+类别素材）两事件同属「素材库/」前缀，末条也匹配→典型场景不漏；但「素材库/ + 人物/ 等混合批」会漏（可复现）；**Outline**——章卡/导演板/索引全在「大纲/」下，同前缀安全，混合批低概率；**Novel/DocSection**——保存+切片同步连续写盘含 正文/ + .zhijuan/ 混合批，丢的是「非必需」列表刷新（自身写后列表本就无需重载），实际影响小。**结论：一类畸形模式四页并存，机制与 CollectionBar 同一个，风险等级低但真实（素材库页可复现必挂），且旧 `.some` 修复会随 50 窗口重扫、非匹配事件也连带多刷——收口为共享原语，非「一刀切」**。

**② 产品规划**：
- 为什么现在做：候选 2 到期；同类竞态已在 CollectionBar 浪费一轮（18dda8c），剩余四页是同模式潜在雷；根因=消费端无法区分「新旧事件」，用「末条」当代理。一句话修复=新增共享原语（工程域基础设施），五处统一迁移，顺带消掉 `.some` 的窗口重扫副作用。
- 怎么做（涉及文件）：`features/fs/useFsEvents.ts` 新增 `useFsChanged(projectId, prefix, onChange)`——按事件对象引用追踪已消费位：批内全部新事件都会检查；prev 被 50 条窗口挤出时降级全量检查（宁多勿漏，刷新读取幂等）；已消费旧事件不重复触发。五处消费端各一行替换（CollectionBar 去 events 变量；Novel/Outline/DocSection/LibraryBrowser 保留 useFsEvents 供 extVersion 用）。
- 不动：useFsEvents 本身（50 窗口语义）、主进程 watcher、devShim、任务卡/管道；Novel/Outline 改动严格等于原效果（跨域文件但候选 2 定义即含此四页，且工作树干净、创作层/智能层无在途 WIP 冲突）。
- 验收口径：三道门全绿 + 新增无头冒烟 `scripts/fs-batch-ui-smoke.mjs`（同 tick 连写「素材库/ + 人物/」混合批，断言素材库列表自动刷新出现新素材、类别计数+1）真实跑通，且**负向验证**（stash 掉本修复重跑→冒烟必挂，证明测试有效）；回归 collection-e2e-ui-smoke（CollectionBar 链路）/ chapter-prefill / audit-history-entry。

**③ 技术落地**：
- `useFsEvents.ts` 新增 `useFsChanged`（按引用追踪 prevLastRef；indexOf 定位起点；-1=窗口挤出→从 0 全查；事件对象引用稳定，无内存增长）。
- CollectionBar：`.some` effect → `useFsChanged(id, '素材库/采集池', ...)`，删除 `const events`；LibraryBrowser/Novel/Outline/DocSection：`events[events.length-1]` effect → `useFsChanged(id, '<前缀>', ...)`。
- 新增 `scripts/fs-batch-ui-smoke.mjs`（8 断言：渲染/选类/基线与计数/批量写盘/自动刷新出现/计数+1/清理）。

**④ 测试验收**（真实跑通）：三道门 typecheck ✅ / build ✅ / npm test **46 文件 465 例全绿** ✅；`node scripts/fs-batch-ui-smoke.mjs` **7/7 ALL PASS**（修复后）；**负向验证**：`git stash push -- src/` + rebuild + 重跑冒烟 = **SMOKE FAIL 2**（「新素材未出现」「计数 1→1」），stash pop 后恢复 ALL PASS——证明冒烟真实区分新旧逻辑；回归 `collection-e2e-ui-smoke` 11/11、`chapter-prefill-ui-smoke` OK、`audit-history-entry-smoke` PASS。

**⑤ 落档**：本条目；下一步计划重写（候选 2 完成；devShim 剩余差异升级为候选 2；本轮观察项登记候选 3）。

**提交**：19b01d4 fix(平台层): fs 事件消费统一到 useFsChanged 原语——批内全查不再只看最后一条（五处迁移，修混合前缀批量事件漏刷新；旧逻辑负向验证必挂）

### 2026-09-12 19:30–20:10（本轮：候选 2「冒烟脚本 ZJ_USERDATA 加固收口」→ 落地；提交 fa7762d）

- **开工第 0 步**：`git status` 工作树干净（最近提交＝创作层 2423dd2/e532d0a、智能层 87a8c25，均未触 scripts/ 与平台层域，无在途冲突）；`docs/反馈清单.md` 无归属平台层待落实/落实中条目。
- **调研**：主题为仓库内工程加固，无需外网；全仓扫描 `grep -rl electron-stub scripts/`＝**24 个**数据层/真模型冒烟脚本，按 ZJ_USERDATA 形态三分类：① 固定独立目录＋启动 rmSync 清空＝12 个（含第 15 轮已改 5 个与 at-refs 先例）；② mkdtemp 全新临时目录＋自写 settings＝8 个（context-* / comments-context / history / collection-e2e / import-project，每次运行天然干净，安全）；③ **fallback 到共享 `/tmp/zj-smoke-userdata` 且不清洗＝4 个（acts-smoke / director-smoke / director-check-smoke / subtasks-smoke）**——正是候选 2 要收口的残留。现状确认：共享目录仍存在（9-11 22:37 建）但 settings 已被第 15 轮清空，隐患＝任何 fallback 脚本下一次写入即复污。
- **产品规划**：风险机理＝共享目录残留 `zhijuan-settings.json` → `libraryRoot()` 指向已删目录 → `readDoc`/projectDir 全 null → 真模型冒烟「链路跑通却没有内容」假绿。怎么做＝4 脚本改为「各自独立固定目录＋启动 rmSync 清空」（at-refs 同款），不改任何业务逻辑；不动：zj-bridge（固定 `/tmp/zj-bridge-userdata` 是 answerDir() 与边车插件 ZJ_USER_ANSWER_DIR 同目录的设计要求，且从不写 settings）、mkdtemp 系、electron-stub 本身、其余 20 个。验收＝三道门＋新环境下 libraryRoot/readDoc 探针＋真模型冒烟真实跑通。
- **技术落地**：4 个 .mjs：import 增 `rmSync`；`process.env.ZJ_USERDATA = '/tmp/zj-smoke-<名>'` ＋ 紧接 `rmSync(…, { recursive:true, force:true })`（子进程经 `env: {…, ZJ_USERDATA}` 继承）；subtasks-smoke 用法注释去掉 `ZJ_USERDATA=…` 传参说明。
- **测试验收**：三道门 typecheck ✅ / build ✅ / npm test **47 文件 488 例全绿** ✅；探针（bundle settings+store，ZJ_USERDATA=/tmp/zj-smoke-director-check）实证 `libraryRoot=/Users/USER/Documents/织卷项目库`、`readDoc('织卷smoke','正文/第02章_灯下.md')=true`——新环境路径解析正确；真模型冒烟 `scripts/director-check-smoke.mjs` 实跑两遍：**首遍 492.7s 返回 ok 但空结构**（summary/arcs/axes/redlines/hooks 全空，脚本判 3）＝模型侧波动（与智能层档案候选 2① 已登记「vLLM reasoning 模型 content 可能为 null」同因；directorCheck 已有空结果自动重试一次仍空）；**二遍 455.8s 返回完整结果**（arcs=4/axes=2/redlines=5/hooks=3＋实质 summary，脚本退出 0）——新环境下边车+真模型+提取链路真实跑通。另：直连探针（max_tokens=80）显示模型回复含 reasoning 字段、content 为 null——佐证智能层已登记项（reasoning 可能吃掉预算），非本轮引入。
- **落档**：候选 2 收口 ✅（24 脚本全核查、无一残留共享目录形态；剩余安全形态 20 个不动）；下一步计划重写。

### 2026-09-12 22:30–23:1x（本轮：候选 2「真模型冒烟空 JSON 波动诊断增强」→ 落地；提交 8b07021）

- **开工第 0 步**：`git status` 首查干净（22:30）；近 90 分钟提交＝创作层 1b104a7/9877ad1、智能层 c5d24e7/bab0b40、体验层 85d7af6，与 scripts/、src/main/agent 域无文件交集；`docs/反馈清单.md` 无归属平台层待落实/落实中条目。
- **调研**（仓库内工程现状，架构级，无需外网）：通读 subtask 骨架（subtask.ts runSubtask/runOnce + subtask-types.ts）与四个真模型冒烟脚本（acts/director/director-check/subtasks-smoke.mjs）及全部高层入口（runDirector/runDirectorCheck/runAudit/runChapterCheck/runMaterialTriage/runOutlineRebuild/runActs）。现状确认：各检查器 extract 已自动重试一次，但**重试后仍空时只能看到空结构，无法区分「模型空输出（vLLM reasoning content null，智能层已登记同因）/ 解析失败 / 模型说人话绕圈」**；acts/director/director-check 三脚本已有 exit 非 0 判据但无回包，**subtasks-smoke 完全无判空**（七步打印后恒 exit 0＝假绿风险，2026-09-06 曾以「回复恰好显示在 meta 卡」为假通过的教训同型）。
- **产品规划**：为什么现在做＝上轮 director-check 首遍空/二遍过（492s/455s），诊断盲区只能靠盲目重跑复现；怎么做＝**骨架层单点留存**「最后一次模型原始回复」（runOnceInner），runSubtask 仅在 `retry.check` 判定结果仍无效（weak）时附 `lastRaw`（正常路径不带，避免跨 IPC 白传大文本）；acts/outline 循环侧用 runOnceInner 按需留存（failedRaw/emptyRaw）；四脚本空/弱结果时打印原始回包并**退出非 0**。不动：各 def 行为与重试策略（零行为变化）、UI 消费路径、renderer、devShim、其余冒烟脚本；验收＝node 侧三道门 + 单测 2 例 + 真模型 director-check 跑通。
- **技术落地**：`subtask-types.ts` SubtaskOutcome ok 分支加 `lastRaw?`；`subtask.ts` 新增 `runOnceInner`（恒带回 lastRaw），`runOnce`＝薄包装（outline/acts 调用面不变），`runSubtask` 用 inner 且 weak 时附 lastRaw；director / director-check / audit（runAudit＋runChapterCheck）/ triage 返回类型加 `lastRaw?` 并透传；acts ActsResult 加 `failedRaw?`（失败段号→原始回复，genSeg 改走 runOnceInner）；outline OutlineRebuildResult 加 `emptyRaw?`（空卡章→原始回复）；四脚本空/弱分支打印 `[DIAG]` 原始回包前 2000–3000 字并置非 0 退出码（subtasks-smoke 增 EMPTY 判空表＋bad 标记，六步全判空）。
- **测试验收**：node 侧 `tsc -p tsconfig.node.json` EXIT=0 ✅；`npm run build` ✅（2989 模块，main/preload/renderer 全过）；`subtask.test.ts` **12/12 绿**（新增 2 例：重试仍空→lastRaw 含散文原文、重试后有效→不带 lastRaw 省跨 IPC）；全量 npm test 512 例 509 绿／**3 红**——三红全部来自 **22:58 并发出现的「批注功能」WIP**（tests/unit/annotations.test.ts parseAnnotationCsv 空行占位 3 例），且该 WIP 的 web typecheck 还有 devAnnoResolve/doScan/RefreshCw 3 处 TS2304 断链；WIP＝新增 src/main/agent/annotations.ts、src/shared/annotations.ts、tests/unit/annotations.test.ts ＋改 ipc/proposals/preload/ProposalDrawer/devShim/Workspace/shared/types，**与本轮改动零文件重叠**，按并发契约不碰、登记留主会话/对应推进器收尾（列入下一步候选）。四脚本生成的 entry 语法与链路由真模型 director-check-smoke 实跑验证（见下）。
- **真模型验证**：`scripts/director-check-smoke.mjs` 后台实跑一遍（LOCAL_LLM_KEY=local）**PASS**——完整核对结果（arcs=4/axes=2/redlines=5/hooks=3＋实质 summary，脚本退出 0），生成 entry 含新 [DIAG] 代码语法正常、runDirectorCheck→runSubtask→lastRaw 链路不回归（正常路径不带 lastRaw，行为与旧版一致；诊断分支仅在重试仍空时触发，本遍未触发＝符合设计）。**四脚本空结果诊断分支的触发侧由单测 2 例锁定**（重试仍空→lastRaw 含散文原文）。「批注功能」WIP 与真模型链路零交集（不动 src/main/agent 域）。
- **落档**：候选 2 完成 ✅；WIP 断链登记（候选 2 收口观察）；下一步计划重写。

### 2026-09-13 01:30–01:5x（本轮：候选 2「批注 WIP 收口观察 + CSV 行号口径核对」→ 核对待完成、发现并修复记账误记；提交 9d468bd）

- **开工第 0 步**：`git status` 工作树干净；近 90 分钟提交＝创作层 eea71a3/186cfec/4b7e096、智能层 f4fd0ac、体验层 45a2904，与平台层域（src/main/agent/annotations.ts / scripts / 测试）零文件交集；`docs/反馈清单.md` 无归属平台层待落实条目（F-20260912-04 归属创作层+体验层，批注显示 UI 已在体验层档案候选）。
- **调研**：① 批注 WIP 收尾核实——`git log --grep=批注` 证实 cb9cee3（定时优化）/0569a74（设置开关二次口径）/4b7e096（划词入口）/186cfec（loc 留空修正）均落地，且 `src/main/agent/annotations.ts`、`src/shared/annotations.ts`、`tests/unit/annotations.test.ts` 三文件已在 git 树——**22:58 发现的未提交 WIP 已被作者收尾**，观察项可关闭；② 采集管道 cron 复核：`hermes cron list` 无 76de7e949a3b（末次 2026-09-03），候选 1 仍待主会话执行（推进器不改 .hermes，仅登记）；③ 批注口径核对（候选 2 后半）：devShim scanAnnotations/annotationAdd/devAnnoResolve 三处已复用 shared 三函数（parse/segment/escape）且定位优先级（before 原文精确匹配 > loc 区间）与真机 `findAnnotationTargets` 一致、行删除倒序 splice + 空文件删除语义与主进程 `resolveAnnotationRows` 一致——无 devShim/主进程分叉（仅 dev 防重用「pending 存在即停」，系 mock 简化与本地无 done 账本的合理差异）。
- **产品规划**：核对中在 `scanAnnotations` 记账段发现真 bug——记账循环遍历**全部 targets**（`if (!itemsBy.has(t.mdRel)) continue` 只按章节挡），引擎只匹配部分批注时，**未匹配行也会被 push 进 `annotations.done.json`**，下轮扫描 `doneRows.includes` 直接跳过 → 该批注永久不再处理（与返回的「未匹配 N 条，可稍后重试」提示自相矛盾；csv 被编辑 mtime 变化才可能重扫，通常不会发生）。为什么现在做＝候选 2 明确的工程点，且是批注闭环的可靠性缺陷（引擎偶发未匹配是常态，8 部检查器均有空结果波动案例）；怎么做＝记账改为**按行判断是否命中 result**（与提案生成同判据），只记实际生成提案的行；不动＝devShim（其 mock 无 done 账本、不共享该逻辑）、proposals、findAnnotationTargets、resolveAnnotationRows、csv 约定。验收＝新单测 2 例（修复前红/修复后绿）＋三道门＋既有批注 UI 冒烟回归。
- **技术落地**：`src/main/agent/annotations.ts` 记账段重写——新增 `toRecord: Map<csvRel, number[]>`，仅当 `result.find(before 精确匹配)` 命中才收集行号，再按 csv 合并写入（保留 mtime 语义：csv 变更时 rec 重建、未变更时追加）；`tests/unit/annotations-scan.test.ts` 新建（vi.mock electron/runtime，mkdtemp 真盘项目 + 真 csv/md，driveSession 返回部分/全量 JSON）。
- **测试验收**：新单测首跑 **2/2 红**（found=0 实锤旧逻辑永久跳过未匹配行）→ 修复后 **2/2 绿**（部分匹配 done=[1,2]；下轮 found=2 重试成功断言 done=[1,2,3]）；三道门 typecheck ✅ / build ✅（2990 模块）/ `npm test` **51 文件 522 例全绿**（含新增 2 例）；回归 `scripts/annotations-ui-smoke.mjs` **10/10 ALL PASS**（devShim 链路未受影响）。
- **落档**：候选 2 关闭（WIP 已收尾 + 口径核对完成 + 发现并修复一个真实缺陷）；候选 1 维持待主会话；下一步计划重写。

### 2026-09-13 04:30–05:0x（本轮：自发现「项目导出」——模块设计 §四 A 菜单「打开目录 / 导出 / 删除」缺导出；落地 + round-trip 冒烟实证；提交见下）

- **开工第 0 步**：`git status` 工作树干净（仅本轮 6 个文件）；近 90 分钟提交＝b095129（创作层 docs）/6383efd（创作层 fix）/83cecdb、6600e0c（智能层）/090f7cc、b997633（体验层）/4d8e40f（平台层 01:30 docs），与本轮目标文件（Home.tsx / store.ts / ipc.ts / preload / shared/types / devShim 导出面 / scripts）零交集；`docs/反馈清单.md` 无归属平台层「待落实/落实中」条目（F-20260912-04 已由体验层勾销）。
- **调研**（真实来源）：① 本机 `hermes cron list`（04:31）**再次确认**采集管道 cron 76de7e949a3b 仍不在活动任务列表（候选 1 维持登记，推进器不改 .hermes）；② Node.js 官方文档 fs.cpSync（https://nodejs.org/api/fs.html#fscpsyncsrc-dest-options，v16.7+；recursive/filter/force/errorOnExist/preserveTimestamps 语义逐条核对——**默认 force=true 会覆盖已存在目标**，故冲突必须显式检查，不能依赖 errorOnExist；filter 忽略目录时其内容一并跳过，正合「跳过 .git 时其内部 config 等不拷」）；仓库既有 `importProject` 已用 cpSync+同 filter（2026-09-11 dcea302），本轮导出复用同一形态=导入/导出互为镜像；③ 模块设计 §四 A 布局「右键菜单（打开目录 / 导出 / 删除）」——现状 Home 卡片菜单只有 打开所在文件夹/移到废纸篓，**导出缺失**（产品设计内已拍板项，非新功能堆叠）。
- **产品规划**：为什么现在做＝设计 §四 A 既定三菜单项差「导出」；数据纯本地明文可入 git，导出＝「作品永不锁死」（跨机迁移/备份/分享），且与导入（复制入库）天然 round-trip；怎么做＝`store.exportProject(id, destParent)`（校验 project 存在；目标=父目录/项目目录名；已存在同名→报错不覆盖；防呆：目标=项目自身或其内部→拒绝（cpSync 边抄边抄自身）；cpSync 跳过 .git/.DS_Store/node_modules）+ `project:export` IPC（系统目录选择器 [openDirectory, createDirectory] 选父目录，取消返回 cancelled）+ Home 卡片菜单「导出到…」（FolderOutput 图标，成功 toast「已导出」带目标路径/失败「导出失败」，取消静默）+ devShim 同口径（ok+dest 示意路径）；**不动**：导入语义、删除/废纸篓、卡片布局、recents、采集管道、设置页；新术语「导出」按红线先改 文案口径表 再写 UI。验收口径：三道门＋数据层冒烟（真实 fs：完整复制/内容一致/skip 三杂物/同名拒绝/项目内部拒绝/换库根再导入内容一致/不存在报错）＋无头 UI 冒烟（菜单项→点击→toast 出现）。
- **技术落地**：`shared/types.ts` +ExportResult；`main/store.ts` +exportProject（30 行，复用既有 cpSync/ensureDir）；`main/ipc.ts` +project:export（showOpenDialog 父窗口形态与 chapter:export 同款）；`preload/index.ts` +exportProject 桥；`pages/Home.tsx` 菜单项 + exportTo（toast）；`lib/devShim.ts` +exportProject mock（注明口径）；`docs/文案口径表.md` +「导出」术语行；`docs/模块设计-V2.md` §四 A +4. 导出 ✅；新增 `scripts/export-project-smoke.mjs`（数据层）+ `scripts/export-project-ui-smoke.mjs`（UI）。
- **测试验收**：三道门 typecheck ✅ / build ✅（2990 模块）/ npm test **51 文件 529 例全绿**；数据层冒烟 `node scripts/export-project-smoke.mjs` **15/15 PASS**（真 fs：正文/素材/project.md/.zhijuan 全复制、.git/.DS_Store/node_modules 全跳过、同名拒绝不覆盖、目标在项目内部拒绝、导出物换新库根 importProject 再导入后 readDoc 内容与源一致——round-trip 实证、不存在项目报错）；无头 UI 冒烟 `scripts/export-project-ui-smoke.mjs` **5/5 PASS**（首页卡「更多操作」→「导出到…」→「已导出」toast 含目标路径；**坑：Radix DropdownMenu Trigger 在 pointerdown 打开，合成 `.click()` 打不开菜单——冒烟须 dispatchEvent(new PointerEvent('pointerdown'))**，已写进脚本注释）；截图 `~/Pictures/zhijuan/project-export-menu-0447.png`。
- **落档**：项目导出 ✅（模块设计 §四 A 标 ✅）；候选 1 维持待主会话（本轮再次核实 cron 仍缺失）；下一步计划重写。

### 2026-09-13 07:30–08:0x（本轮：候选 1/2 再核实均受阻 → 自发现「Agent 面板宽度记忆」（模块设计 §十二 明列未落地）；落地；提交见下）

- **开工第 0 步**：`git status` 工作树干净；近 90 分钟提交＝创作层 2bcc0aa、智能层 86e1f8a/1608d1b，与本轮目标文件零交集；`docs/反馈清单.md` 无归属平台层「待落实/落实中」条目（F-20260912-04 已闭环）。环境核实：`hermes cron list` **第 4 次确认**采集管道 76de7e949a3b 仍不在（候选 1 维持登记）；`pmset -g assertions` UserIsActive=0＝**锁屏**，候选 2（导出真壳验收）仍无法执行。
- **调研**（真实来源，URL 记档）：① W3C WAI-ARIA APG **Window Splitter**（https://www.w3.org/WAI/ARIA/apg/patterns/windowsplitter/，经本机无头 Chrome 抓正文）——可聚焦分隔条必须 `role="separator"`＋`aria-orientation`＋`aria-label/aria-labelledby`（主面板名）＋`aria-controls`＋`aria-valuenow/min/max`；键盘：←/→ 移动垂直分隔条、Enter 折叠、Home/End 可选（最小/最大）、F6 可选循环；② shadcn/ui **Resizable**（https://ui.shadcn.com/docs/components/resizable）＝基于 react-resizable-panels（bvaughn）——需引新依赖，且其模型是「百分比双面板」，本项目只要「单面板 px 宽度＋记忆」，评估后**不引**（工程红线：不引重量级依赖；手写 ~60 行即可，仓库亦有自研拖拽先例）；③ 仓库现状核对：AgentPanel 根节点硬编码 `w-80`（320px）不可调；模块设计 §十二「面板宽度记忆」在设置清单里但**从未落地**（§13.3 注记当时记为「Resizable 暂无现成需求」，与 §十二 自相矛盾——§十二为权威：有需求）；④ 口径表核对：「Agent 面板」已是既有用户层术语（口径表 61 行⑥），无新术语。
- **产品规划**：为什么现在做＝模块设计 §十二 已拍板项欠账；agent-first 主通道固定 320px 对工具活动卡/EditCard 行号对比偏窄，宽度不可调且不记忆；怎么做＝Agent 面板左缘加**自研分隔条**（WAI-ARIA 齐全：role/aria 值/tabindex/方向键/Home/End/双击复位）＋拖拽 pointer 事件（window 级 move/up 跟手）＋松手/按键即写 `AppSettings.agentPanelWidth`（默认 320，钳 [280, 560]）；纯逻辑（取值域与钳制）抽 `shared/uiPrefs.ts` 纯函数，真机/devShim 共用（工程红线）；涉及文件：shared/uiPrefs.ts（新）、shared/types.ts、renderer AgentPanel.tsx、devShim.ts（settings 字面量补齐口径）、tests/unit/uiPrefs.test.ts（新）、scripts/agent-panel-width-ui-smoke.mjs（新）；**不动**：编辑器、其他面板/抽屉宽度、设置页布局、Chapters 相关、采集管道；**放弃**：引 react-resizable-panels、百分比布局、左右双面板（agent 面板是固定侧栏语义）。验收口径：三道门＋单测 4 例＋无头 UI 冒烟（CDP 真鼠标拖拽/真键盘事件 13 项断言）＋截图。
- **技术落地**：`shared/uiPrefs.ts`（AGENT_PANEL_DEFAULT_WIDTH=320/MIN=280/MAX=560/STEP=16 + clampAgentWidth：非有限值→默认、小数取整、越界钳制）；`types.ts` AppSettings+`agentPanelWidth`（DEFAULT_SETTINGS 用常量，老配置读盘自动补 320）；`AgentPanel.tsx`：mount 读设置→clamp→生效；拖拽=pointerdown 记起点→window pointermove（clamp 后 setState）→pointerup 持久化（body cursor/userSelect 临时态）；键盘 ArrowLeft/Right ±16、Home/End 最窄/最宽、双击复位 320；分隔条 JSX 全 aria 字段＋focus-visible 环＋hover 青黛细线（tokens 语义色）；`devShim.ts` settings 字面量补 `agentPanelWidth`（口径与 DEFAULT_SETTINGS 一致）。
- **测试验收**（真实跑通）：三道门 typecheck ✅ / build ✅（2990 模块）/ npm test **52 文件 536 例全绿**（含新增 uiPrefs 4 例）；无头冒烟 `node scripts/agent-panel-width-ui-smoke.mjs` **13 项全 OK**：默认 320、分隔条 role/aria-label/aria-controls/tabindex/valuemin/max 齐全、**CDP 真实鼠标拖拽**左移 120→440 且 `getSettings().agentPanelWidth`=440（写通）、右拖 300→钳 280、**CDP 真键盘** ArrowRight×20→钳 560、ArrowLeft→544、Home→280、End→560、**双击复位 320**＋设置回写、全程零 JS 异常；截图 `~/Pictures/zhijuan/agent-panel-width-0749.png`（加宽 480＋hover 态）。限制：devShim 内存态无法验证「重启后恢复」（真机由 settings 文件负责，主进程 setSettings 写盘路径自有测试面覆盖），真壳端到端并入候选 2 的解锁验收批。
- **落档**：模块设计 §十二「面板宽度记忆」标 ✅、§13.3 注记更新（Resizable 以自研 separator 落地，未引 react-resizable-panels）；候选 1 维持待主会话（第 4 次核实）；候选 2 叠加本项目目待解锁验收；下一步计划重写。

### 2026-09-13 10:30–11:1x（本轮：自发现「首页项目库可见性」——模块设计 §四 A 布局欠账；提交 5344d96）

- **开工第 0 步**：`git status` 工作树干净（仅本轮 3 文件）；近 90 分钟提交＝创作层 2aa0a5b/b1fd5dc、智能层 6450eae/b5d229a、体验层 771068b，与本轮目标文件（Home.tsx / 文案口径表 / 模块设计）零交集；`docs/反馈清单.md` 无归属平台层「待落实/落实中」条目（F-20260912-04 已闭环）。环境核实：`pmset -g assertions` UserIsActive=0＝**仍锁屏**（候选 2 真壳验收第 6 次核实受阻）；`hermes cron list` 第 5 次确认采集管道 76de7e949a3b 仍不在活动列表（候选 1 维持登记待主会话）。
- **调研**（真实来源，URL 记档）：① Obsidian 官方帮助「Create a vault」（https://help.obsidian.md/Getting+started/Create+a+vault，经本机无头 Chrome CDP 9224 抓正文——web_extract/r.jina.ai 均被判私有网络拦截）："A vault is a folder on your local file system where Obsidian stores your notes. You can keep all your notes in one vault, or create several vaults for each of your different projects."——多库（vault）是官方一等场景、库位置=文件系统路径为产品级可见信息；② 仓库权威源核对：模块设计 **§四 A 布局明列「顶部：应用名 + 「＋ 新建项目」主按钮 + 库根路径（可改）」与「底栏：当前库根、右键菜单（打开目录 / 导出 / 删除）」**——当前 Home.tsx 顶部只有徽标＋导入/新建（无库根），底部无状态栏，**首页库根可见性从未落地**（打开目录/导出/删除已落地，库根展示是 §四 A 唯一残留欠账）；③ D-V2-8 决策链（settingsLogic.resolveLibraryRoot：设置非空→老默认位 ~/Documents/织卷项目库 非空保持原地→工作区/项目库）＝真实存在多条库根路径，首页项目列表来自哪个库用户无从得知（本机即 legacy 库 + 工作区库双场景）；④ 现状核实：`app:getPaths` IPC 已存在（preload 47 行），devShim 已有同口径 mock（678-686 行，与真机共用 settingsLogic），设置页「项目库」卡已用「库根路径/生效库根」术语（Settings.tsx 268-278）；**但注意**：设置页路由挂在 `/project/:id/settings`（App.tsx 60 行）＝首页无项目上下文不可达（EngineBadge 须从 hash 解析项目 id）——故「可改」不跳设置页，落**新 IPC `settings:pickLibrary`**（系统目录选择器→setSettings({libraryRoot})→返回生效库根，主进程 +11 行、preload +1 行、devShim 同口径 mock），首页就地改库根并刷新列表，更贴模块设计「库根路径（可改）」原意。
- **产品规划**：为什么现在做＝§四 A 设计欠账（模块设计为权威源，已拍板项）+ 多库根场景认知盲区（平台层项目管理核心：用户必须知道自己在浏览哪个库）；怎么做＝Home 底部加轻量状态栏（footer）：左侧「当前项目库：<生效库根>」（truncate＋title 全量，窄窗口不换行），右侧 ghost 按钮「更改库根路径…」→ 系统目录选择器 → 写入设置 → 刷新项目列表（就地「可改」，不跳设置页——设置页首页不可达，见调研④）；不动：卡片布局、新建/导入/删除/导出菜单、设置页、采集管道；术语按口径表「项目库/库根路径」登记（设置页既有用法，非新词，补表登记）。验收口径：三道门 + 无头 UI 冒烟（footer 出现、路径与 getPaths().documents 动态一致、点击后路径随动 mock 新库、toast「已更改项目库」、truncate 抗窄窗、零 JS 异常）+ 截图。
- **技术落地**：`src/main/ipc.ts` 加 `settings:pickLibrary`（showOpenDialog「选择项目库位置…」→cancel 返回 null；否则 setSettings({libraryRoot})→返回 libraryRoot()）；`src/preload/index.ts` 加 `pickLibrary`；`src/renderer/src/lib/devShim.ts` 加同口径 mock（写 settings.libraryRoot='/tmp/织卷-dev-项目库' 并返回——getPaths 决策链随动）；`Home.tsx`：libPath state（refresh 里 Promise.all 增 getPaths().catch(()=>null)，失败不阻塞列表）、changeLibRoot（pick→setLibPath→refresh→toast「已更改项目库」/「更改项目库失败」）、footer（`data-testid="home-libroot"`，truncate+title，按钮 whitespace-nowrap）；`docs/文案口径表.md` 一、术语表补「项目库/库根路径」；`docs/模块设计-V2.md` §四 A 布局两行标 ✅；新 `scripts/home-libroot-ui-smoke.mjs`（footer 文本与 getPaths().documents 动态相等——devShim 返回 '~/Documents/织卷项目库' 字面量不硬编）。
- **测试验收**（真实跑通）：三道门全绿——typecheck ✅、build ✅（2994 模块）、npm test **53 文件 553 例**；无头冒烟 `node scripts/home-libroot-ui-smoke.mjs` **5 项全 PASS**（①底栏就绪+路径与 getPaths().documents 动态一致（~/Documents/织卷项目库）② truncate/whitespace-nowrap 抗窄窗 ③ 点击「更改库根路径…」→ footer 更新为 dev 新库 + toast「已更改项目库」④ getPaths 随动 /tmp/织卷-dev-项目库 ⑤ 零 JS 异常）；截图 `~/Pictures/zhijuan/home-libroot-1045.png`。
- **落档**：§四 A 欠账完成 ✅（模块设计布局两行标 ✅）；口径表补「项目库/库根路径」术语；候选 1 第 5 次核实仍缺（待主会话）、候选 2 新增 ③首页库根真壳链路、第 6 次核实仍锁屏；下一步计划重写（新增第 5 条「平台层已拍板项全部落地」现状记录）。

### 2026-09-13 13:30–14:0x（本轮：自发现「首页项目卡片元信息缺素材数」——模块设计 §四 A「每卡：章节数·人物数·素材数」欠账；落地；提交见下）

- **开工第 0 步**：`git status` 工作树干净（仅本轮 5 文件）；近 90 分钟提交＝创作层 2f9ea5c/0754760、智能层 d04c291、体验层 4f0233f/561e344，与本轮目标文件（Home.tsx / scripts / docs）零交集；`docs/反馈清单.md` 无归属平台层「待落实/落实中」条目。环境核实：`pmset -g assertions` UserIsActive=0＝**仍锁屏**（候选 2 真壳验收第 7 次核实受阻）；`hermes cron list` **第 6 次确认**采集管道 76de7e949a3b 仍不在活动列表（候选 1 维持登记待主会话）。
- **调研**（来源记档）：① 权威源模块设计 **§四 A 布局明列「每卡：名称 / 简介 / 章节数·人物数·素材数 / 更新时间 / 最近章节」**——现状核对 Home.tsx：信息区有简介/最近章节/日期，封面小字只有「N 章 · 人物 N」（293 行），**素材数从未显示**；② 数据层核实 store.ts 130-136 行：ProjectStats 已含 `materials`（countFiles('素材库') − 采集池）且 devShim listProjects 同口径（materials:1/2/0）——**数据已备，纯 UI 漏显示**，不是功能盲区；③ 外网佐证（Notion gallery/board 卡片属性可自选显示，https://www.notion.com/help/intro-to-databases；同类写作软件 misenous.com、folienta.com 项目卡均以角色/元素等统计组织长篇项目）——卡片元信息为通行实践，素材数是织卷 V2「素材库积累」核心支柱的可视信号，首页看不到积累量是认知盲区。
- **产品规划**：为什么现在做＝§四 A 设计欠账（与上两轮 agentPanelWidth / 首页库根可见性同型：**设计明列、实现漏项**），平台层已拍板项收口系列；怎么做＝Home 封面小字补「· 素材 N」（`p.stats.materials`，保持 11px truncate 纯文本封面样式，不破坏主人 F-20260912-01 终版）+ `data-testid="zj-card-stats"` 供冒烟；涉及文件：Home.tsx（1 行）、scripts/home-stats-ui-smoke.mjs（新）；**不动**：信息区布局、封面版式/色板、store/devShim（数据已备）、导入导出、toast、其他页；无新术语（「素材/素材库」口径表已有）。验收口径：三道门＋新无头冒烟（断言文本与 `listProjects().stats` 集合动态一致，不依赖「最近打开」排序）＋旧冒烟 home-libroot 回归＋截图。
- **技术落地**：`Home.tsx` 封面小字一处（`{p.stats.chapters} 章 · 人物 {p.stats.characters} · 素材 {p.stats.materials}`）；新 `scripts/home-stats-ui-smoke.mjs` 4 断言（testid 出现 / 每卡文本与 stats 集合一致含素材数 / 回归含「章」「人物」/ 零 JS 异常）＋ `ZJ_SHOT` 环境变量可选截图；顺带修 `home-libroot-ui-smoke.mjs` 的 BASE 硬编码 8123（支持 `ZJ_SMOKE_BASE` 覆盖）——本轮实踩：**仓库 scripts/spa_server.py 不存在（样板注释过时）**，本机无头验收服务器为 `/tmp/spa_server.py` 常驻 **8899**（ROOT 硬编码织卷 out/renderer），改脚本支持 BASE 覆盖后不再踩端口坑。
- **测试验收**（真实跑通）：三道门 typecheck ✅ / build ✅（2994 模块）/ npm test **53 文件 553 例全绿**；无头冒烟 `node scripts/home-stats-ui-smoke.mjs` **4/4 ALL PASS**（样本「3 章 · 人物 2 · 素材 1 / 0 章 · 人物 0 · 素材 0 / 5 章 · 人物 4 · 素材 2」，与 stats 集合动态一致）；回归 `home-libroot-ui-smoke` **5 项 PASS**；截图 `~/Pictures/zhijuan/home-card-stats-1342.png`。
- **落档**：模块设计 §四 A「主体：每卡字段集」标 ✅——**平台层模块设计欠账至此全部收口**（§四 A 布局/数据/交互行全 ✅；§五 B 尚发现一处残留见下）；候选 1 第 6 次核实仍缺（待主会话）、候选 2 第 7 次核实仍锁屏；下一步计划重写（新增候选：§五 B 侧边栏「采集入口快捷方式」——本轮盘点发现仍在设计清单未落地）。

### 2026-09-13 16:30–16:5x（本轮：候选 3——模块设计 §五 B 侧边栏「采集入口快捷方式」落地；提交 c1b5658）

- **开工第 0 步**：`git status` 工作树干净（本轮 4 文件）；近 90 分钟提交＝创作层 6c2fb6e/388f838（15:45 轮，docs＋项目骨架）、智能层 e75a3f7（15:00 轮）、体验层 b55d124/8560bec，与本轮目标（SectionNav/Library/CollectionBar）零交集；`docs/反馈清单.md` 无归属平台层「待落实/落实中」条目；候选 3 已由 13:30 轮盘点立项「下一轮主做」，本轮即它。环境核实：`pmset -g assertions` UserIsActive=0＝**仍锁屏**（候选 2 第 8 次核实受阻）；`hermes cron list` 第 8 次确认采集管道 76de7e949a3b 仍不在活动列表（候选 1 维持登记待主会话）。
- **调研**（真实来源记 URL）：① Obsidian 官方帮助 Quickadd（https://help.obsidian.md/Plugins/Quickadd，r.jina.ai 抓正文——quickadd.obsidian.md 域名解析失败换官方 help 站）与 Command palette（https://help.obsidian.md/Plugins/Command+palette）——QuickAdd「从任意位置一键发起捕获动作」（热键/命令面板常驻可达）为成熟先例，与本设计意图同构；② 权威源模块设计 **§五 B 明列「底部：采集入口快捷方式、设置入口」**——SectionNav.tsx 底部现状只有「设置」＋「返回项目列表」（最近核对 84-104 行），采集入口缺失；③ 现状核实：素材库页 CollectionBar 已有「＋ 发起采集」按钮（open state 组件内自持，189 行），采集是 V2 核心动作（S5）但**跨页无快捷入口**——写作/阅读途中要采素材须先导航进素材库页再点按钮（两步）；④ 消费模式核实：App.tsx `library` 子路由、Library.tsx 已有 `?doc=` 参数消费＋清参先例（13-16 行，与 novel?ch= 同策略），hash 路由下 useSearchParams 可用，直接复用同策略，无需新 IPC/devShim 改动。
- **产品规划**：为什么现在做＝**平台层模块设计唯一残留欠账**（13:30 轮盘点结论）＋核心动作（采集）缺常驻入口；怎么做＝SectionNav 底部（设置上方）加「发起采集」NavLink（绝对路径 `/project/:id/library?collect=1`，projectId prop 直接拼，避开相对解析语义）→ Library 页消费 `?collect=1`（清参同 ?doc= 策略）→ tick 信号传 CollectionBar 新 prop `requestOpen`（tick 递增避免同值被 React 忽略、同页重复点击仍弹）→ 自动打开采集表单 Dialog；涉及：`features/nav/SectionNav.tsx`、`pages/Library.tsx`、`features/collection/CollectionBar.tsx`、新 `scripts/nav-collect-ui-smoke.mjs`；**不动**：采集管道、任务卡格式、提交逻辑、devShim（纯渲染层导航，无新 IPC/API）；术语「发起采集」口径表已有（素材行：采集动作「发起采集」「采集素材」）。验收口径：三道门＋无头冒烟 5 点（入口存在／点击跳库页且表单自动打开／关闭后 URL 无 collect 残留／无参进入不自动弹／同页重复点击再弹）＋截图。
- **技术落地**：三文件小改共 32 行（SectionNav 底部入口＋CloudDownload 图标；Library 消费参数＋tick 传参；CollectionBar requestOpen effect）；冒烟脚本按 states-smoke 样板（/json/new 用 PUT、evalUntil、**NavLink 渲染为 `<a>` 不是 button**——clickText 只查 button，脚本内自写 clickNav 查 `aside.w-60 a`）；BASE 支持 `ZJ_SMOKE_BASE` 覆盖（同 home-stats 冒烟规格，防端口坑）。
- **测试验收**（真实跑通）：三道门全绿——typecheck ✅ / build ✅（2994 模块）/ npm test **54 文件 557 例**；无头冒烟 `node scripts/nav-collect-ui-smoke.mjs` **5/5 PASS**（① 侧栏存在「发起采集」入口（domain 断言）② 点击→library 路由＋「需求描述」表单自动打开 ③ 关闭后 hash 无 collect 残留 ④ 直接进 library 不自动弹 ⑤ 同页重复点击仍弹）；截图 `~/Pictures/zhijuan/nav-collect-entry-1638.png`（侧栏入口态）＋ `nav-collect-form-1638.png`（表单打开态）。
- **落档**：模块设计-V2 **§五 B 侧边栏行标 ✅**（c1b5658）——**平台层模块设计已拍板项至此全部落地**（§四 A/§五 B/§九 F/§十一 H/§十二 全 ✅）；候选 1 第 8 次核实仍缺（待主会话）、候选 2 第 8 次仍锁屏；下一步计划重写（候选 1/2 维持、候选 3 收口，新增候选 3＝devShim 与真机口径对表审计）。

### 2026-09-13 19:30–20:0x（本轮：候选 3「devShim 与真机口径对表审计」第一波 → 修复 8 处口径；提交见下）

- **开工第 0 步**：`git status` 工作树干净（仅本轮文件）；近 90 分钟提交＝创作层 1640b31/4867b74、智能层 d72eab8/d0f86f3，与本轮目标（devShim.ts / scripts / 档案）零交集；`docs/反馈清单.md` 无归属平台层「待落实/落实中」条目（F-20260912-04 已闭环）。环境核实：`pmset -g assertions` UserIsActive=0＝**仍锁屏**（候选 2 第 9 次核实受阻，本轮候选 3 无需解锁，照常推进）。
- **调研**（仓库内逐 API 对表，无需外网——本次是纯工程审计，主题=候选 3）：以 preload/index.ts 暴露的 60+ 个 API 名与 `Promise<>` 类型为契约面，逐一比对 devShim.ts（mock，1494 行）与主进程 handler 实际返回（ipc.ts + store/settings/slices/library/history/workspace/templates/annotations/agent·ipc）。**已核口径一致**（21/22 轮收口后无回归）：listDocs 剥 .md、listChapters「涉及人物」解析、listSlices updatedAt、getPaths 决策链、searchDocs 默认 50、createLibraryCategory sanitize、deleteDoc 防御、renameChapter/deleteChapter/editChapterSlice 引用面、writeDoc 版本化、listLibraryCategories 排除『采集池/点目录/zh 排序、runChapterUnlisted/Missing（ok/items/error 结构）、proposals（create 同章 pending 置 stale、meta/metas 逐条独立传、apply 后 devAnnoResolve 与 resolveAnnotationRows 同语义）、annotations（add row=追加后总行数 / list before 优先 loc 兜底 / remove 返回结构）、agentDirector/DirectorCheck/Acts/adoptActs/OutlineRebuild 返回形状、agentSync 骨架。**实锤差异 8 处**（均 devShim 侧，真机为权威）：① listHistory.size＝字符数 vs 真机 statSync UTF-8 字节（**HistoryDrawer 直接显示「字节」——dev 演示偏小约 3 倍，最实锤**）；② removeProject 无返回 vs `{ ok: true }`；③ openProject 返回项目对象 vs `true`（preload 类型 Promise<boolean>）；④ writeDoc 无返回 vs `true`；⑤ workspaceStatus.docs 未排序 vs listWorkspaceDocs 按 name zh 排；⑥ agentStatus 缺 message 字段（真机恒有）；⑦ agentListCapabilities 仅 7 项 vs 真机 listCapabilities 注册 9 项（缺 acts / annotation-sync——设置页「检查能力」dev 演示少 2 开关）；⑧ agentSend 无返回 vs `{ ok: true }`。剩余合理差异（登记观察，非 bug）：listTemplates dev 仅内建『示例』（真机扫描用户模板目录）、scanAnnotations dev 防重用「pending 存在即停」（01:30 轮已登记真机用 done 账本+mtime）、agent 演示分支数据形状（audit/acts/triage mock 输出）与真机 lastRaw/失败路径不同——mock 语义即演示，不算口径。
- **产品规划**：为什么现在做＝候选 3 正是 16:30 轮指定「下一轮主做」，且 ①⑤⑦ 是 UI 可见差异（历史大小显示、设置页开关数），其余为契约契约对齐（防未来调用方踩坑）；怎么做＝纯 devShim.ts 内 8 处一行级修复（每个改动都注释指向真机实现做权威依据）+ 新增一个**逐 API 断言冒烟** `scripts/devshim-consistency-smoke.mjs`（每修复点一条断言，防回归）；涉及：src/renderer/src/lib/devShim.ts、scripts/devshim-consistency-smoke.mjs、本档案；**不动**：主进程实现、preload、渲染层组件、其余 mock 语义（演示数据）、已有 60+ 冒烟；验收口径＝三道门＋新冒烟 9 项全 PASS＋**负向验证**（stash 掉修复 rebuild 重跑＝SMOKE FAIL 8，证明断言真实区分新旧）；无 UI 视觉改动（不触发截图契约）。
- **技术落地**：8 处 patch 全部落在 devShim.ts（size→new TextEncoder().encode(...).length；removeProject/openProject/writeDoc/agentSend 补真机返回；workspaceStatus docs 补 zh 排序；agentStatus 补 message:''；agentListCapabilities 补 acts/annotation-sync 两条（title/description 从真机 actDef/annotationDef 原样取）；新建冒烟脚本（沿用 home-stats 样板：/json/new PUT、evalUntil、ZJ_SMOKE_BASE 覆盖端口）。
- **测试验收**（真实跑通）：三道门 typecheck ✅ / build ✅（2995 模块）/ npm test **55 文件 569 例全绿**；`node scripts/devshim-consistency-smoke.mjs` **9/9 ALL PASS**（①②③④⑤⑥⑦⑨⑧；⑤ 实证 size=431 字节 ≥ 字符数 169；⑦ 实证 9 项清单与真机 id 全对齐；⑨ 设置页「检查能力」实渲染 9 项开关）；**负向验证**：`git stash push -- src/renderer/src/lib/devShim.ts` + rebuild + 重跑＝**SMOKE FAIL 8**（①②③⑤⑥⑦⑨ 全暴露，其中⑤同时抓出 size=169 字符数残留）→ stash pop 恢复 → 终验 9/9。环境：/tmp/spa_server.py 8899 + CDP 9224 就绪。
- **落档**：候选 3 第一波完成（8 处修复 + 逐 API 回归冒烟 + 负向验证）；剩余 3 处合理差异登记为观察项并入候选 3 下一波；候选 1 第 9 次核实仍缺（待主会话）、候选 2 第 9 次仍锁屏；下一步计划重写。

### 2026-09-13 22:30–23:1x（本轮：候选 3「devShim 口径审计第二波」——① listTemplates 收口 + ②③ 评估关闭；功能提交 e14fd09）

- **开工第 0 步**：`git status` 工作树仅 `?? release/`（**平台层构建域新发现**：22:18 主会话「发布冲刺」提交 7bc6ed5 双端打包底座（electron-builder.yml `directories.output: release`）后，打包产物 `release/`（dmg/mac-arm64/builder-debug.yml）未入 .gitignore——后续 `git add -A` 会把大二进制产物入仓）；近 90 分钟提交＝7bc6ed5（主会话 22:18）+ 创作层 dd3da97/8abec02、智能层 c7d6565/269b3f9，与本轮目标文件（devShim.ts / scripts / .gitignore / docs）零交集；`docs/反馈清单.md` 无归属平台层「待落实/落实中」条目（F-20260912-04 已闭环）。环境核实：`pmset -g assertions` UserIsActive=0＝**仍锁屏**（候选 2 第 10 次核实受阻）；`hermes cron list` 第 10 次确认采集管道 76de7e949a3b 仍不在活动列表（候选 1 待主会话）。
- **调研**（第二波评估；主题=候选 3，纯仓库内工程审计，无外网项；阶段权威源 docs/发布冲刺-2026-09-15.md 已读——其中「系统菜单｜平台层 B｜待落地」为本轮发现的冲刺缺口，列为下一步候选 1）：按 19:30 轮登记的 3 处合理差异逐项核对——
  ① **listTemplates**：devShim 仅内建『示例』（单元素数组），真机＝扫描 `工作区/模板/项目模板/` 用户目录（templates.ts：ensureBuiltin + readdir 目录 + builtin 排前 + zh 排序）；Home.tsx 新建对话框「初始内容」SelectItem 对 builtin 显示 `示例（名）`、非 builtin 直接显示 `t.name`（398-402 行）——dev 只有 1 个模板时「非内建显示名」分支从未被驱动。**判定：应补**（低成本、直接服务「多模板选择」UI 冒烟覆盖面，正是 19:30 轮指定评估项）。
  ② **scanAnnotations dev 防重**（pending 存在即停）vs 真机 done.json+mtime 账本：逐行比对真机（findAnnotationTargets 跳过 doneRows → runSubtask → 只记实际命中行、未命中行不记账保留下轮重试，01:30 轮 9d468bd 定稿口径）与 devShim（MOCK_AFTER 命中才生成、未命中返回「引擎未产出可用改写（批注未动，可稍后重试）」且不记任何状态、pending 存在即停）。**判定：关闭（等价简化）**——dev 无 fs 无 done 账本，但「pending 存在即停」与真机「done 记账 + 接受/拒绝删 csv 行」在单轮扫描语义上等价（真机行删了自然不再扫；dev 无外部改 csv 能力，mtime 账本无对象）；「未命中不视为已处理」两测行为一致（均不记账）。
  ③ **agent 演示分支 lastRaw/failedRaw/emptyRaw**：真机 DirectorResult / ActsResult / OutlineRebuildResult 等在正常路径**不带**诊断字段（8b07021 设计：仅重试仍空/失败时附），dev mock 恒返回 ok/result 正常形状。**判定：关闭（合理）**——UI 消费字段（ok/result/written/sheet/error）19:30 轮已全对齐；lastRaw 仅出现在真机失败/空结果路径，dev mock 语义即演示，无 UI 消费者，补 mock 诊断字段=无效改动。
  另核对：createProject 的 `_template?` 参数 devShim 忽略（不模拟 applyTemplate 复制进新项目）——**登记为合理简化差异**（真机语义＝templates.ts 纯函数 + 既有单测覆盖；dev 无 fs，模拟复制自欺）。
- **产品规划**：为什么现在做＝候选 3 是 19:30 轮指定「下一轮主做」；① 是本批唯一有实际 UI 差异的项（新建对话框「多模板选择」分支不可驱动）；②③ 做评估关闭（结论入档，避免后续重复立项）；另 `.gitignore` 补 `release/`＝打包底座（7bc6ed5，主会话 22:18）的配套收尾（发布冲刺阶段仓库卫生）。怎么做＝① devShim.listTemplates 扩为 2 项（新增 `{ id:'悬疑短篇', name:'悬疑短篇', builtin:false }`，注释引用真机口径与用途）+ consistency 冒烟加 ⑩ 段（API 形状断言 + 打开新建对话框选非内建模板 → 创建 → 进入项目页全链路，约 55 行）；`.gitignore` 加 `release/`（注释指向 electron-builder.yml）；②③ 仅档案登记关闭。**不动**：主进程 templates.ts / applyTemplate（真机权威）、createProject 签名与语义、proposals/annotations 链路、Home.tsx（SelectItem 渲染已支持非内建，无需改）、其余 60+ 冒烟；**不引依赖**。验收口径＝三道门 + consistency 冒烟 ⑩ 实跑 + template-ui-smoke 回归（下拉仍含空白/示例）+ 负向验证（stash 掉 devShim 修复 rebuild 重跑＝⑩ 挂）。涉及文件：devShim.ts、.gitignore、devshim-consistency-smoke.mjs、本档案；ROADMAP 登记该冒烟为「devShim 口径审计三波共同回归资产」。
- **技术落地**：3 文件 5 处（devShim 模板数组 2 项 + 4 行注释；.gitignore 1 行；冒烟脚本头注 + ⑩ 段四断言：API 含非内建且 builtin 排前 / 下拉出现「悬疑短篇」/ 选中后触发器无「示例（」前缀 / 选非内建模板创建并进入项目页）。选中态与创建输入用原生 value setter + input 事件（React 受控口径），不依赖 IME。
- **测试验收**：三道门 typecheck ✅ / build ✅ / npm test **55 文件 569 例全绿**；`node scripts/devshim-consistency-smoke.mjs` **13/13 ALL PASS**（新增 ⑩ 四断言实跑：API 2 项 builtin 排前、下拉含「悬疑短篇」、触发器显示非内建名、创建进入 /project/；另顺带把 ⑧ 滤掉 Radix 浮层动画的 ResizeObserver loop 浏览器噪音并计数显示）；回归 `node scripts/template-ui-smoke.mjs` **10/10 OK**（下拉仍含「空白（仅目录骨架）」与「示例（示例）」+ 新增第三项不破坏原路径）；**负向验证**：`git stash push -- src/renderer/src/lib/devShim.ts` + rebuild + 重跑 consistency = **SMOKE FAIL 3**（⑩ 前三断言准确暴露「仅 1 项无 builtin:false」；第 4 条创建成功为空白模板路径仍走通，断言粒度合理不误报）→ stash pop 恢复终验 13/13。另：本轮发现 9224 无头 Chrome 自 9-4 起跑 9 天钙化（104 Helper、CDP 命令无响应）——已 kill 主进程 20994 并按原参数重启（Chrome 152.0.7977.84），CDP 恢复从而冒烟可跑；后续轮次若遇 evaluate 超时先查该进程龄期/Helper 数量。
- **落档**：候选 3 第二波收口（①落地 + ②③ 评估关闭 + createProject 模板应用简化差异登记）；候选 1/2 维持；下一步计划重写（系统菜单列为候选 1——发布冲刺缺口、口径已定、规模评估见下）。

### 2026-09-14 01:30–02:0x（本轮：候选 1——系统菜单第一刀（主进程侧）落地；提交见下）

- **开工第 0 步**：`git status` 工作树干净；近 90 分钟提交＝35089d0/31dc04a（创作层 00:45）、b2a0766/9a4e25c（智能层 00:00）、3a84d38/18e3070（体验层）、e14fd09（平台层 22:30 上轮），与本轮目标（src/main/menu.ts 新建、index.ts/preload/shared/types/electron-stub/scripts、系统菜单设计口径与发布冲刺文档）零交集；`docs/反馈清单.md` 无归属平台层「待落实/落实中」条目。环境核实：`pmset -g assertions` UserIsActive=0＝**仍锁屏**（候选 3 第 11 次核实受阻；本轮主进程侧无窗依赖，照常推进）；`hermes cron list` 第 11 次确认采集管道 76de7e949a3b 不在活动列表（候选 2 维持待主会话）。
- **调研**（主题=档案候选 1 系统菜单；外网 HIG/Electron 官方来源已在 docs/系统菜单-设计口径.md §依据记档，本轮不做重复抓取）：① 本地权威核验 `node_modules/electron/electron.d.ts`（}31.7.7 类型即实现契约）——MenuItemConstructorOptions.role 清单确认 19 个所需 role 全部存在（undo/redo/cut/copy/paste/selectAll/services/hide/hideOthers/unhide/quit/close/resetZoom/zoomIn/zoomOut/togglefullscreen/minimize/zoom/front）；**showAboutPanel 在 app 上而非 dialog**（electron.d.ts 1687 行 App 接口）——设计口径文档 §二表写成了 `dialog.showAboutPanel`，按「与本文件不一致处先改此处再落」已同步修正；② 现状核对：src/main 无任何 Menu 代码（主进程从未注册应用菜单＝当前 Electron 默认英文菜单，与设计口径 §一现状一致）；preload 事件桥有 onFsEvent/onAgentEvent 两个先例可仿；electron-stub 是 24 个冒烟共用桩（本轮扩展需向后兼容——只增字段不删）；③ 设计口径 §七「涉及文件」已列本轮文件集；分两刀口径在该文 §五/§六（(a)(e) 主进程侧、渲染层分发/禁用态/双触发＝下一刀）。
- **产品规划**：为什么现在做＝发布冲刺缺口（发布冲刺-2026-09-15.md 第二节「系统菜单｜待落地」）+ 档案候选 1 指定「建议分两刀、本轮=主进程侧」；mac 菜单栏是 B 外壳的一等公民（custom titleBar 下当前无菜单=快捷键可发现性缺失），且主进程模板断言**锁屏可验**（设计口径 §五-4 明确）。怎么做＝新建 `src/main/menu.ts`（`buildMenuTemplate(handlers)` 纯函数可冒烟 + `registerMenuActions()` 注册）+ `index.ts` ready 后注册 + `preload onMenuAction`（onFsEvent 同型）+ `shared/types.ts` 加 `MenuActionId`/`MenuActionEvent`（渲染层下一刀共用，工程红线：shared 纯约定）+ electron-stub 扩展 + `scripts/menu-template-smoke.mjs`；**动作分发取舍（拍板）**：10 个自定义通道中 `关于织卷…`（app.setAboutPanelOptions+showAboutPanel）与 `打开说明文档目录`（shell.showItemInFolder(工作区/文档)）走**主进程侧动作**（前者无可渲染层消费、后者 §四动作地图明说「主进程直接」），其余 8 个 send `menu:action`——设计口径 §7 已补此例外注记；**不动**：渲染层任何文件（App.tsx 分发=下一刀）、快捷键文档/ShortcutHelp（无新增快捷键）、禁用态（属于下一刀 IPC 上报）、devShim（本轮渲染层无消费方）、采集管道、其余冒烟；无新用户层术语（菜单文案与口径表既有词汇一致：「新建项目/新建章节/保存/查找…」均已登记）。验收口径＝三大门 + 冒烟按 (a)(e)：模板结构逐项=设计表 + click 分发 id 集合 + registerMenuActions 全链路（setApplicationMenu/send 捕获/about/docs 主进程动作）。(b)(c)(d)(g) 下一刀/解锁。
- **技术落地**：`shared/types.ts` +MenuActionId/MenuActionEvent；`main/menu.ts`（~130 行：EXPECT 同源结构，role 项显式 label+accelerator——口径 §三-2「macOS 上 role 项仅 label/accelerator 生效」；「查找」子菜单 label 写「查找」（箭头由 macOS 系统渲染，口径 §六 (a) 已加注）；`index.ts` 2 行注册（ready 后、createWindow 前）；`preload` onMenuAction 桥；electron-stub +Menu（setApplicationMenu/getApplicationMenu/buildFromTemplate）/app.showAboutPanel+setAboutPanelOptions 记录/BrowserWindow.getFocusedWindow/shell._shown 记录（只增不删，向后兼容）。
- **测试验收**（真实跑通）：**首跑踩两坑并修复**——① esbuild alias 会把 electron-stub 代码 inline 进 bundle，外部单独 import 的 stub 与 bundle 内是同名**不同实例**（registerMenuActions 写的 _appMenu 在外面读不到→setApplicationMenu 断言假 FAIL）：修正＝冒烟 stdin 额外 `export * from 'electron'`，让 bundle 同时导出内联 stub，观察/改写 BrowserWindow 从 bundle 对象上做（坑注已写进脚本头）；② `dialog.showAboutPanel` TS2339 类型不存在——实现改 `app.showAboutPanel()`（顺带把设计口径文档同步修正）。终验：三道门 typecheck ✅ / build ✅（2995 模块）/ npm test **55 文件 569 例全绿**；`node scripts/menu-template-smoke.mjs` **14/14 ALL PASS**（六菜单逐项结构与设计表一致、10 通道+2 主进程动作 id 集合、setApplicationMenu 被调、保存/设置/速查 click→menu:action{id} mock 捕获、关于→showAboutPanel+applicationName=织卷、打开说明文档→shell.showItemInFolder(工作区/文档) 真实路径）；断言有效性已由首跑 6 FAIL 实锤（改对比较对象后全绿）。限：锁屏无法真机展开菜单 (g)，按设计口径留解锁主人过目；(b)(c)(d) 双触发/灰显=下一刀。
- **落档**：候选 1 第一刀完成 ✅（发布冲刺-2026-09-15.md 该行标「第一刀✅/渲染层分发=下一刀」；设计口径 §7/§六/表单行同步）；候选 2 第 11 次核实仍缺（待主会话）、候选 3 第 11 次仍锁屏；下一步计划重写（候选 1 第二刀上位）。

### 2026-09-14 04:30–05:0x（本轮：候选 1——系统菜单第二刀（渲染层单点分发 + 禁用态上报）落地；提交 f08c96b）

- **开工第 0 步**：`git status` 工作树干净；近 90 分钟提交＝903e427/e3ec456（创作层 03:45）、757bfdb/d34a647（智能层 03:00）、47a6b60/7ba8efa（体验层 02:15），与本轮目标（main/menu.ts、preload、App.tsx、DocEditor/Prose/Home/Novel、devShim、scripts）零交集——注意 02:15 体验层动过 EditorToolbar（非本轮文件）；`docs/反馈清单.md` 无归属平台层「待落实/落实中」条目。
- **调研**（主题=候选 1 第二刀；真实来源）：① Electron 官方文档 keyboard-shortcuts 教程（https://www.electronjs.org/docs/latest/tutorial/keyboard-shortcuts，curl 直抓）——「Local keyboard shortcuts … map to specific menu items within the app's main application menu … the click event associated to that menu item will trigger upon using that accelerator」＋「before-input-event … used to catch and handle custom shortcuts that are **not visible in the menu**」——官方语义＝菜单 accelerator 由菜单体系处理，renderer keydown 是另一通路；双触发未达彻底明文 → 实现层用**时间窗去重兜底**（同 id <750ms 跳过 keydown，菜单原生拦截时零副作用）；② MenuItem 官方文档（https://www.electronjs.org/docs/latest/api/menu-item，curl 直抓）确认 `menuItem.enabled` 为实例属性、getMenuItemById（Electron 长存 API）定点更新可行；③ 仓库权威源：设计口径 §7 动作分发/禁用态方案，第一刀（0fdad90）menu.ts 已预留 App.tsx 单点分发=下一刀。
- **产品规划**：为什么现在做＝发布冲刺缺口第二刀（候选 1）；渲染层分发后菜单动作才真正闭环（现在点了没反应）。怎么做＝新建 `features/menu/menuBus.tsx`（MenuBridge：onMenuAction→一处 switch；页面类动作经 CustomEvent『zj:menu-newProject/newChapter/save/find』派发——页面各自监听，禁用态保证只会点到对应页；门户类 settings/shortcutHelp 本层直接处理；`isMenuJustHandled` 去重窗口）＋ `features/menu/menuState.ts`（routeKindOfHash + DocEditor 挂载计数 + reportMenuState）；`store/ui.ts`（shortcutHelpOpen）；**发现并补缺**：App 路由**没有顶层设置页**（Settings 只在 /project/:id/settings）——口径明文「全局设置页 #/settings」，且 Settings 组件零项目依赖，故补 `<Route path="/settings">`（项目内 SectionNav 入口不变）；主进程 menu.ts 加 MENU_ITEM_ID（10 自定义项 id）+ `menuEnabledFor`（纯函数：newProject 仅 home、newChapter 仅 project、save/find* 仅 editor、其余常可用）+ `applyMenuState`（getMenuItemById 灰显不隐藏）+ ipcMain 'menu:state'；preload +reportMenuState；devShim +onMenuAction/`__ZJ_MENU_EMIT`/`__ZJ_MENU_STATE`（reportMenuState 记录）；**不动**：ShortcutHelp/CommandPalette/Settings 页内既有实例（保留本地态；菜单速查用 App 级实例）、快捷键文档（无新增快捷键）、采集管道、角色项（undo/copy 等）。验收口径＝三道门 + 主进程冒烟扩 20/20 + 无头 UI 冒烟新脚本 9 项 + 截图。
- **技术落地**：15 文件（详见提交）。接线点：Home(监听 newProject→setCreating)、Novel(监听 newChapter→openCreate——openCreate 依赖 prevChapter 用 ref 取最新闭包)、DocEditor(mount/unmount 上报 editor、监听 save→doSaveRef、keydown ⌘S 加 isMenuJustHandled)、Prose(监听 find 组→findActionsRef、keydown ⌘F/⌘E/⌘G 加去重)；MenuBridge 挂 Router 外（hash 跳转/监听，不踩 EngineBadge 路由外 hook 坑），且 App 挂载即上报初始态、hashchange 随动；electron-stub 向后兼容扩展（Menu.getMenuItemById 递归建 id 表、ipcMain.on/emit——只增符号）。
- **测试验收**（真实跑通）：三道门 typecheck ✅ / build ✅（3000 模块）/ npm test **57 文件 597 例全绿**；`node scripts/menu-template-smoke.mjs` **20/20 PASS**（原 14 项无回归 + id 全量唯一 + menuEnabledFor 4 场景矩阵 + applyMenuState home/project 两态 + ipcMain emit 实链路——首跑 5 FAIL 暴露「打开说明文档目录未设 id」「enabled 守卫过严」「重复 register 致监听×2」三缺口，修复后全绿，断言有效性实锤）；新 `node scripts/menu-actions-ui-smoke.mjs` **9/9 PASS**（初始上报 home/no-editor、settings 跳转（连带触发补全局 /settings 路由）、newProject 建项对话框、选章后 editor=true 上报、newChapter 建章对话框、save 真实写盘 readDoc 实锤、findOpen 查找条、shortcutHelp 速查、零 JS 异常）；截图 `~/Pictures/zhijuan/menu-settings-0459.png` / `menu-shortcut-0459.png`（无头渲染，菜单本体属系统 chrome 需真机过目=候选 3 ③）。
- **落档**：候选 1 第二刀完成 ✅（系统菜单全链路闭环；发布冲刺-2026-09-15.md 该行标✅）；当前状态补行；候选 2 第 12 次核实仍缺（待主会话）、候选 3 第 12 次仍锁屏；下一步计划重写。

### 2026-09-14 07:30–08:0x（本轮：候选 1「菜单双触发去重抽纯函数 + 双保存负向断言」→ 落地；提交 ebe93ac）

- **开工第 0 步**：`git status` 工作树干净；近 90 分钟提交＝e88adae/004ba8a（创作层 06:45 docs+feat 切片同步失败可重试闭环，触 Novel/Outline/toast）、969dadb/7a55947（智能层 03:00 档案切片核查）、3d93dd6（体验层 05:15 docs），与本轮目标（shared/menuDedup.ts 新、menuBus.tsx、tests/unit/menuDedup.test.ts 新、menu-actions-ui-smoke.mjs）零交集；`docs/反馈清单.md` 无归属平台层「待落实/落实中」条目（最新条目均已解决）。环境核实：`pmset -g assertions` UserIsActive=0＝**仍锁屏**（候选 3 第 13 次核实受阻）；`hermes cron list` **第 13 次确认**采集管道 76de7e949a3b 仍不在活动列表（候选 2 维持待主会话）。
- **调研**（真实来源）：① 候选 1 为 04:30 轮自立的「下一轮主做」（档案五-1）；② 设计口径 `docs/系统菜单-设计口径.md` §五-1 原文核对——「菜单 accelerator 注册后按键由主进程菜单体系处理；**是否仍向 renderer 派发 keydown 未达官方明文**；验收 (b) 各按一次只发生一次」；③ 外网佐证：Electron 官方 keyboard-shortcuts 教程（https://www.electronjs.org/docs/tutorial/keyboard-shortcuts，本轮 web_search 命中）重申「renderer 内可用 addEventListener 处理 keydown」，StackOverflow 68166286（https://stackoverflow.com/questions/68166286/how-do-i-handle-a-global-shortcut-and-filemenu-item-click-event-in-electronjs-fr）社区实践确认菜单 accelerator 与 DOM keydown 存在同达场景、需应用层去重——时间窗兜底方向正确；④ 代码事实：menuBus.tsx L22-25 去重断判＝模块级变量 + 内联表达式，调用面 4 处（DocEditor L119、Prose L680/684/688）全部走 isMenuJustHandled，但**判断规则无单测、冒烟无「双保存」负向断言**——正是候选 1 明示欠账。
- **产品规划**：为什么现在做＝候选 1 已立且锁屏可做（去重是第二刀后唯一残留工程点：规则内联 .tsx、无单测、无负向断言）；怎么做＝抽 `shared/menuDedup.ts`（`MENU_DEDUP_WINDOW_MS=750` + `MenuActionStamp` + `shouldSkipKeydown(last,id,now)` 纯函数——无状态无 fs，符合工程红线「真机/devShim 共用、shared 纯约定」）→ menuBus.isMenuJustHandled 改薄包装（行为零变化）→ 新单测 6 例（窗口内同 id 跳过/异 id 不跳/超窗不跳/窗口边界严格小于不跳/无记录不跳/窗口外同 id 不跳）→ menu-actions-ui-smoke 补 **P6.5**（菜单 save 后窗口内真实 dispatch keydown ⌘S → writeDoc 计数恰 1 + 正文实写盘）与 **P6.5a**（正向对照：无菜单前置时 keydown ⌘S 仍保存恰 1 次，证去重不吞正常按键）；**不动**：主进程 menu.ts/菜单禁用态/菜单模板/其他页面/其余冒烟/devShim（断言计数靠冒烟脚本内 monkey-patch writeDoc——devShim 对象方法可覆盖，无需改 mock）；验收口径＝三道门 + 单测 6 例 + menu-actions-ui-smoke 全过 + **负向验证**（窗口 750→0 重跑必抓双保存）+ 回归 menu-template-smoke。
- **技术落地**：4 文件约 70 行——`shared/menuDedup.ts`（新 26 行带口径注释）；`tests/unit/menuDedup.test.ts`（新 6 例）；`menuBus.tsx` 2 处（import + isMenuJustHandled 薄包装，注释指向 shared 与单测）；`menu-actions-ui-smoke.mjs` 补 P6.5/P6.5a 段约 30 行（writeDoc 计数包装经 page.eval 注入，注释注明真机部分归候选 3 ⑤）。
- **测试验收**（真实跑通）：三道门 typecheck ✅ / build ✅（3002 模块）/ npm test **59 文件 612 例全绿**（含新增 menuDedup 6 例）；`node scripts/menu-actions-ui-smoke.mjs` **12 项 ALL PASS**（P1-P9 无回归 + P6.5 双断言 + P6.5a）；**负向验证**：`MENU_DEDUP_WINDOW_MS` 750→0 + rebuild + 重跑＝**P6.5 NG（writeDoc=2，双保存实锤）RESULT 1 FAILED** → 恢复 750 + rebuild + 重跑＝ALL PASS（断言真实区分去重有无）；回归 `node scripts/menu-template-smoke.mjs` **20/20 PASS**（主进程菜单链路未受影响）。
- **落档**：候选 1 完成 ✅（设计口径 §五-1 双触发兜底逻辑：纯函数 + 单测 + 双保存负向断言收口；真机 ⌘S 键盘实测维持候选 3 ⑤）；候选 2 第 13 次核实仍缺（待主会话）、候选 3 第 13 次仍锁屏；下一步计划重写（候选 1＝find 组双触发断言，FindBar 已暴露 `zj-find-count` 可作硬断言）。

### 2026-09-14 10:30–11:0x（本轮：候选 1——菜单 find 组双触发冒烟断言（P7.5 同型 P6.5）→ 落地；提交见下）

- **开工第 0 步**：`git status` 工作树干净；近 90 分钟提交＝41dcd62/e606599（创作层 09:45 feat+docs 切片同步三入口失败可感知，触 Novel/Outline/toast）、f30ec43/6885586（智能层 09:00）、d782c2e（docs 截图契约升级），与本轮目标（scripts/menu-actions-ui-smoke.mjs；src/shared/menuDedup.ts 仅负向临时改）零交集；`docs/反馈清单.md` 无归属平台层「待落实/落实中」条目（F-20260912-04 已闭环）。环境核实：`pmset -g assertions` UserIsActive=0＝**仍锁屏**（候选 3 第 14 次核实受阻）；`hermes cron list` **第 14 次确认**采集管道 76de7e949a3b 仍不在活动列表（候选 2 维持待主会话）；`/tmp/spa_server.py`（8899）与 CDP 9224（Chrome 152.0.7977.84）就绪。
- **调研**（仓库内事实，主题=档案候选 1）：① 候选 1 为 07:30 轮自立的「下一轮主做」（档案五-1）；② Prose.tsx `stepFind`（595-607 行）核实**会回绕**（cur>=n→0 / cur<0→n-1）——断言方向据此定：双触发步进使计数 2/2→1/2 即失败信号；③ FindBar `zj-find-count`（57 行）文本 `${current+1}/${total}`（无匹配显示「无匹配」）＝可读断言面；④ 输入链路 `onQueryChange→recalcFind(q,true)`（880-883 行）同步无防抖，native setter+input 事件可驱动（模板冒烟同款）；⑤ 菜单动作分发 `handleMenuAction` findOpen/findNext/findPrev→MENU_EV_FIND（menuBus 52-57）、Prose 监听（704-707）；⑥ P6.5 样板（menu-actions-ui-smoke 133-157）与 writeDoc 计数包装先例沿用。
- **产品规划**：为什么现在做＝候选 1 已立且锁屏可做（save 组已有「菜单后 ⌘S 不双保存」负向断言，find 组 Prose L680/684/688 同走 shouldSkipKeydown 却无「只动作一次」实测——同型欠账）；怎么做＝menu-actions-ui-smoke.mjs 加 **P7.5**（正文注入两个「菜单find探针词」→ 查找条输入词 1/2 → 菜单 findNext 到 2/2 → 窗口内真实 dispatch ⌘G → 计数仍 2/2）、**P7.5a**（窗口过期后 ⌘G 正常步进 2/2→1/2 回绕，证去重不吞正常按键）、**P7.5b**（菜单 findPrev 后窗口内 ⇧⌘G 停 2/2）；顺带按 13:30 轮规格补 BASE 支持 `ZJ_SMOKE_BASE` 覆盖（本机 8899 常驻）与 `ZJ_SHOT` 截图规格（home-stats 同款）；**不动**：src 产品行为（零改动，负向验证后原样还原）、主进程菜单、其余冒烟、devShim；验收口径＝三道门 + 冒烟全过 + **负向验证**（窗口 750→0 重跑必挂）+ 回归 menu-template-smoke。
- **技术落地**：`menu-actions-ui-smoke.mjs` 三处（头注释验收点 + BASE/ZJ_SHOT 支持 + P7.5/P7.5a/P7.5b 三段约 50 行）；`src/shared/menuDedup.ts` 仅负向验证临时 750→0（还原，git diff 归零）。
- **测试验收**（真实跑通）：首跑 **15 项 ALL PASS**（原 12 项无回归 + P7.5/P7.5a/P7.5b）；**负向验证**：`MENU_DEDUP_WINDOW_MS` 750→0 + rebuild + 重跑＝**P6.5 NG（writeDoc=2 双保存）+ P7.5 NG（count=1/2，双步进回绕实锤）+ P7.5a TIMEOUT（计数错位）**——断言真实区分去重有无；恢复 750 + rebuild（bundle hash 变化证实重建）＝终验 ALL PASS；三道门 typecheck ✅ / build ✅ / npm test **59 文件 613 例全绿**；回归 `node scripts/menu-template-smoke.mjs` **20/20 PASS**（主进程菜单链路未受影响）；截图 `~/Pictures/zhijuan/find-menu-dedup-1043.png`（查找条 2/2 双触发场景态）。
- **落档**：候选 1 **完成 ✅**——设计口径 §五-1 双触发兜底 (b) 的无头面全覆盖（save+find 两组、正反向断言）；真机键盘实测仍归候选 3 ⑤；候选 4⑨「zj-find-count 为断言面」观察项随本轮关闭；候选 2 第 14 次核实仍缺（待主会话）、候选 3 第 14 次仍锁屏；下一步计划重写。

### 2026-09-14 13:30–14:0x（本轮：候选 1——Windows 兼容清单走查 → 产出走查清单 + 首修「相对路径跨平台一致」；提交 ee6266d）

- **开工第 0 步**：`git status` 工作树干净（仅本轮 6 文件）；近 90 分钟提交＝0dd242d/97b1ca9（创作层 12:45）、a0e6e9d/c6cd15f（智能层 12:00）、a499ca0/3424210（体验层 11:15），与本轮目标（store/library/relpath/docs）零交集；`docs/反馈清单.md` 无归属平台层「待落实/落实中」条目（F-20260912-04 已闭环）。环境核实：`pmset -g assertions` UserIsActive=0＝**仍锁屏**（候选 3 第 15 次核实受阻；本轮纯代码级走查无需 GUI）；`hermes cron list` 第 15 次确认采集管道 76de7e949a3b 仍不在活动列表（候选 2 维持待主会话）。
- **调研**（主题=档案候选 1「Windows 兼容清单走查」，发布冲刺-2026-09-15.md 该行「win 延后、清单照常走查（窗口/快捷键/路径已核；spawn 宿主已修）」——本轮以**官方文档逐项重核 + 代码事实对表**收口；Electron 官方文档按 v31.7.7（仓库 `node_modules/electron` 同版本）经 api.github.com contents API 直抓 markdown 源，URL＝`https://api.github.com/repos/electron/electron/contents/docs/api/<x>.md?ref=v31.7.7`（本机 raw 走代理慢，contents API 直连快且是文档原档）：① **base-window-options.md**：`titleBarStyle` 取值 `default/hidden/hiddenInset`，`hiddenInset` 标注 **仅 _macOS_**（hidden 在 win/linux 须配 titleBarOverlay 才有窗口控件）→ 织卷现状 `...(darwin ? hiddenInset : {})` + WindowChrome 按 `platform!=='darwin'` 隐藏（preload 暴露 platform、devShim 硬编码 darwin=演示口径）＝**win 走 default 原生标题栏、无双标题栏，已兼容**；② **accelerator.md**：「On Linux and Windows, the `Command` key does not have any effect so use `CommandOrControl`」→ menu.ts 硬编码 `Cmd+Q/H/W/M`/`Shift+Cmd+Z`/`Shift+Cmd+G`/`Ctrl+Cmd+F` 在 win 上**不注册**；③ **menu-item.md**：「additional roles available on _macOS_」＝`hide/hideOthers/unhide/services/front/zoom` 等仅 mac，基础清单含 `quit/close/minimize/undo/redo/cut/copy/paste/selectAll/about`（跨平台）→ menu.ts 的 `role: services/hide/hideOthers/unhide/front` 在 win 无效；④ **browser_win.cc**（shell/browser 源码直抓）：`Browser::ShowAboutPanel` **win 有实现**（从 exe FileVersionInfo 构建关于框；app.cc 中 setAboutPanelOptions/showAboutPanel 绑定无平台宏）→ 「关于织卷…」跨平台可用；⑤ **shell.md**：`trashItem/showItemInFolder/openExternal` 无平台标注＝跨平台（回收站/资源管理器语义）✅；⑥ 仓库代码事实对表（win 主要破坏点自己找）：`store.ts listDocs`（walk 内 `join(prefix,e.name)` 产相对 relDir 路径）、`store.ts watchProject`（`path.relative` 产 FsEvent.path）、`library.ts searchDocs/recentLibraryDocs`（walk 同型）——**渲染层与 devShim 的 IPC 契约恒为正斜杠**（`'目录/'+file`、`useFsChanged` 前缀匹配 `素材库/`、shared 各解析器 `split('/')`），Windows 上原生 join/relative 产出反斜杠 → **win 上文档列表读写/搜索/自动刷新全链路路径错乱**，且违反「真机+devShim 双实现同口径」红线（devShim 全 '/'）。obsidian 等外部参考本轮不需要（纯工程一致性），未抓外网。
- **产品规划**：为什么现在做＝候选 1 已立为上一轮「下一轮主做」+ ⑥是真实 win 兼容破坏（不是打磨）；怎么做＝产出走查清单 `docs/平台层-走向win-走查.md`（六面：窗口/菜单/路径/引擎宿主/shell/构建，每项「现状-官方依据-处置」），本轮**只修 A（相对路径跨平台一致）**——它是唯一「一行级可修 + 锁屏可验 + 零 mac 行为变化」项；B（accelerator Cmd 硬编码）与 C（mac-only role）登记不修（win 菜单布局将来随 win 包立项重设计，mac 优先）；D 已兼容确认；E（runtime.ts `nodeBin()` 仅 unix 探测 which//usr/local/bin/nvm→win 恒走 ELECTRON_RUN_AS_NODE 兜底，功能可用）登记观察；F（`--font-sans` 无 Microsoft YaHei，win 回退系统默认=雅黑，属体验层设计系统）登记观察；G（`.zhijuan` dot 目录 win 无隐藏属性）登记观察。涉及：新建 `src/shared/relpath.ts`（posixRel/toPosix 纯函数，遵守 shared 纯约定无 fs）、`store.ts`（listDocs walk 2 处 + watchProject 1 处）、`library.ts`（searchDocs/recentLibraryDocs walk 4 处）、`tests/unit/relpath.test.ts`（7 例）、`scripts/relpath-posix-smoke.mjs`（12 断言）、走查文档；**不动**：menu.ts/渲染层/devShim（口径不动）/electron-builder.yml（win 配置已备、打包延后）/真机模型链路。验收口径＝三道门 + 单测 7 例 + 数据层冒烟 12 断言（含「渲染层拼 `正文/`+file 回环可读」「嵌套子目录恒正斜杠」「searchDocs/recentLibraryDocs 相对项目根恒 '/'」）+ export-project-smoke 回归 + 负向论证（mac 上 join 与 posixRel 行为相同无法复现反斜杠——win 正确性由 Node path 语义 + `path.win32.join` 对照单测 + posixRel 恒 '/' 锁定，如实登记）。
- **技术落地**：`shared/relpath.ts`（~25 行：posixRel=prefix?prefix+'/'+name:name（无 fs 无 path 依赖，可被 renderer 引用）；toPosix=反斜杠归一）；store.ts 4 行（listDocs 递归+rel 两处、watchProject `toPosix(relative(...))`）；library.ts 4 行（两 walk 的递归+rel）；单测 7 例（含 win32.join 对照：`win32.join('素材库','环境','灯塔.md')==='素材库\\环境\\灯塔.md'` vs `posixRel==='素材库/环境/灯塔.md'`，论证「为什么必须 posixRel」）；冒烟按 export-project-smoke 样板（electron-stub、ZJ_USERDATA 干净目录、真盘建项目+嵌套素材+采集池任务卡，12 断言）。
- **测试验收**（真实跑通）：三道门 typecheck ✅ / build ✅（3003 模块）/ npm test **60 文件 624 例全绿**（含新增 relpath 7 例）；`node scripts/relpath-posix-smoke.mjs` **12/12 PASS**（首跑 5 FAIL 系脚本断言语义写错——listDocs 返回相对 relDir 路径、通用枚举含采集池，核 Novel.tsx 消费口径后修正断言＝`'正文/'+file` 回环、嵌套恒正斜杠、search/recent 相对项目根）；回归 `node scripts/export-project-smoke.mjs` **15/15 PASS**（store 链路无回归）；**negative 论证**：mac 上 `join` 与 `posixRel` 产出相同（均 '/'），故无法在 mac 真机复现反斜杠失败——win 正确性依据=Node path.posix 官方语义恒 '/'（代码即证据）+ 单测 win32 对照 + 冒烟锁定「永不产 \」；**截图契约说明**：本轮是主进程纯路径函数修复，mac 上同输入同输出（UI 状态零变化），无头截图无区分度，未产截图（功能层无视觉/交互状态变化）。
- **落档**：发布冲刺-2026-09-15.md「Windows 兼容清单」行更新＝走查完成 ✅（清单 docs/平台层-走向win-走查.md）+ 首修 ee6266d；候选 1 主体完成（走查+首修，B/C/E/F/G 登记观察随 win 包立项）；候选 2 第 15 次核实仍缺（待主会话）、候选 3 第 15 次仍锁屏；下一步计划重写。

### 2026-09-14 16:30–17:0x（本轮：候选 1——Win 菜单跨平台处置收口：最小可用口径定稿 + winTemplate 平台分支 + nodeBin win 探测；提交见下）

- **开工第 0 步**：`git status` 工作树干净；近 90 分钟提交＝0178e57/65788ff（创作层 15:45）、c30d3e6/bc5a617（智能层 15:00），与本轮目标（menu.ts/runtime.ts/docs）零交集；`docs/反馈清单.md` 无归属平台层「待落实/落实中」条目。环境核实：`pmset -g assertions` UserIsActive=0＝**仍锁屏**（候选 3 第 16 次核实受阻；本轮纯代码级无需 GUI）；`hermes cron list` 第 16 次确认采集管道 76de7e949a3b 仍不在活动列表（候选 2 维持待主会话）。
- **调研**（主题=档案候选 1，真实来源：Electron v31.7.7 官方源码+文档经 api.github.com contents API 直抓——与本仓库 node_modules/electron 同版本；URL=`https://api.github.com/repos/electron/electron/contents/docs/api/{menu-item.md,accelerator.md}?ref=v31.7.7`、`.../lib/browser/api/{menu-item.ts,menu.ts,menu-item-roles.ts}?ref=v31.7.7`）：① **menu-item-roles.ts 是决定性依据**：role 默认 accelerator **按平台取值**——`close`=CommandOrControl+W、`redo` win32=Control+Y（mac=Shift+CmdOrCtrl+Z）、`togglefullscreen` win=F11、`resetZoom/zoomIn/zoomOut`=CmdOrCtrl 系、**`quit` 仅非 win 平台有 CmdOrCtrl+Q（win 上官方就是不绑快捷键）**；② menu-item.ts：MenuItem 构造保留显式 accelerator、native 仅在未显式时取 `getDefaultRoleAccelerator`——**role 项显式写=覆盖平台默认**（mac 的 `Cmd+Q/W/M`、`Shift+Cmd+Z/G`、`Ctrl+Cmd+F` 在 win 上会覆盖成「Command 键无效果」＝快捷键失效，与 13:30 走查 W-1 判断一致但根因更深）；③ accelerator.md：win/linux 上 Command 键无效果须 CmdOrCtrl；④ menu-item.md：mac-only role＝services/hide/hideOthers/unhide/front/zoom（win 无效）；⑤ **修正 13:30 走查 W-1 的「逐项 CmdOrCtrl 平替」建议**——平替只能逐项猜 win 惯例（redo 该 Ctrl+Y 还是 Ctrl+Shift+Z？），role 默认才是官方平台化答案 → win 分支 role 项一律不写 accelerator。**产品视角（F-20260912-07 口径，本轮开工后 README 才生效、落档时补做）**：中文创作软件跨平台是市场常态而非边缘需求——写作软件 Effie 支持 Windows/macOS/iOS/Android 四端多设备同步（官网 effie.co；CSDN 全攻略 https://blog.csdn.net/waicsdn_haha/article/details/145947785；驱动之家 Windows 版发布报道 https://news.mydrivers.com/1/780/780117.htm），其 Windows 版 2022 年才正式发布（官网口径「后续也会重新开发」）——产品含义：①创作工具的平台覆盖决定「作者工作流连续性」（换机/共享/团队评审不中断），织卷若长期 mac-only，win 作者（中文写作市场主流平台之一）无法承接；②win 版体验落后于 mac 是 Effie 用户明显痛点（xiaoyi.vc 实测文），佐证「win 包延后≠忽略 win 体验口径」——本轮把菜单/nodeBin 平台性先备好正是低成本的体验连续性投资；③织卷的定位是「人与 agent 协作」，agent 依赖 node 宿主，win 上用系统 node 而非 Electron 兜底（W-3）直接关系 win 作者启动引擎的稳定性/性能——这是与本轮 win 主题接得上的唯一创作/产品落点。
- **产品规划**：为什么现在做＝候选 1（13:30 轮立的「下一轮主做」）恰是收口轮：13:30 已定「win 延后、清单照常」，本轮把 B/C 从「登记观察」推进到「口径定稿+模板备好」——决策＝**独立平台分支模板**（不是「win 不启用/菜单维持」的二选一，也不是 mac 平替）；同时顺带 13:30 登记观察项 E（nodeBin win 探测）低成本落地。怎么做＝`buildMenuTemplate(h, platform=process.platform)` 分派（mac 分支逐字不动）；`winTemplate`＝文件/编辑/显示/窗口/帮助（**无「织卷」应用菜单**——win 无 app menu 概念；设置→文件、关于→帮助＝平台惯例；role 项零显式 accelerator；自定义项 CmdOrCtrl 化：save=CmdOrCtrl+S、settings=CmdOrCtrl+,、find 组 CmdOrCtrl+F/E/G、findPrev=Shift+CmdOrCtrl+G；**MENU_ITEM_ID / menuEnabledFor / applyMenuState 与 mac 完全共用零改动**）；`runtime.ts nodeBin()` win32 分支＝`where node` → nvm（%APPDATA%\nvm\versions\node\<v>\node.exe）→ Program Files 两位 → Electron 兜底。涉及：src/main/menu.ts、src/main/agent/runtime.ts、tests/unit/runtime-win.test.ts（新）、scripts/menu-template-smoke.mjs（+7 断言）、docs/系统菜单-设计口径.md（§win 节新增）、docs/平台层-走向win-走查.md（W-1/W-2/W-3 标已解决＋新增 W-7 真机验收观察）、docs/发布冲刺-2026-09-15.md（Windows 行更新）；**不动**：mac 分支行为、渲染层、devShim、electron-builder.yml（win 配置已备、打包仍延后）、真模型链路。验收口径＝三道门＋单测 4 例＋menu-template-smoke 27/27（mac 20 零回归＋win 7）＋负向验证（win role 项带显式 Cmd accelerator 必被抓出）。
- **技术落地**：menu.ts（platform 参数＋macTemplate 原样搬迁＋winTemplate）；runtime.ts（win32 分支＋导出纯函数 `winNodeBinCandidateList(env, home, versions)`——**恒用 win32.join**（与 relpath.ts 的 posixRel 同思路：平台语义纯函数跨平台可测），单测 4 例锁定 APPDATA 回退/优先级/Program Files 恒在尾；unix 探测零改动）；冒烟 7 断言（顶级结构/无 mac-only role/role 零显式 acc/id 全量/accelerator 表/设置·关于置位/click 分发）。
- **测试验收**（真实跑通）：三道门 typecheck ✅ / build ✅ / npm test **62 文件 640 例全绿**（+4）；`node scripts/menu-template-smoke.mjs` **27/27 PASS**（原 20 项 mac 断言零回归）；**负向验证**：临时给 win 分支 quit 加 `accelerator: 'Cmd+Q'` → 「win 模板 role 项零显式 accelerator」**FAIL（准确抓出所防错误）**→ 还原后终验 27/27；单测首跑曾 2 FAIL——发现 `winNodeBinCandidateList` 原用平台相关 join（mac 上产正斜杠、期望反斜杠）→ 根因式修法＝恒 `win32.join` 而非改期望（平台语义纯函数先例 relpath.ts）；**截图契约说明**：mac 菜单渲染面零变化（mac 分支逐字未动）＋win 菜单是主进程原生组件（无头只覆盖 renderer、且本机 mac 无法呈现 win 菜单），无头截图无区分度——win 模板正确性由源码断言＋负向验证锁定（与 13:30 轮同类说明，契约精神=功能面必须有可验确认而非仅肉眼）。
- **落档**：候选 1 **完成 ✅**（win 菜单最小可用口径定稿+模板落地；走查 W-1/W-2/W-3 已解决、新增 W-7 随 win 立项验收）；候选 2 第 16 次核实仍缺（待主会话）；候选 3 第 16 次仍锁屏；下一步计划重写。

**提交**：1d2e0ad — feat(平台层): win 菜单最小可用口径+平台分支模板（role 项零显式 accelerator 交 Electron 平台默认；菜单/nodeBin 平台性先备好）+ nodeBin win 探测；口径 docs/系统菜单-设计口径.md §win，menu-template 27/27+单测 4 例

### 2026-09-15 04:30–05:0x（本轮：候选 1——素材采集管道 e2e 履约性走查 → 履约健康 + 修 1 处文档漂移；提交 629e056）

- **开工第 0 步**：`git status` 工作树干净（仅本轮 1 文件）；近 90 分钟提交＝70c6397/a484204（创作层 03:45 docs+feat）、441b53d/f23ff83（智能层 03:00 docs+feat）、95d2af5（体验层 02:15 docs），与本轮目标（scripts/collection-e2e-*、tasks 采集实现、模块设计 §11）零交集；`docs/反馈清单.md` 无归属平台层「待落实/落实中」条目（仅状态流表头）。环境核实：`pmset -g assertions` UserIsActive=0＝**仍锁屏**（候选 3 第 20 次核实受阻）；`hermes cron list` **第 20 次确认**采集管道 76de7e949a3b 仍不在活动列表（6 个 active 无「素材库采集」，候选 2 维持待主会话）。
- **调研**（主题=候选 1，仓库内履约走查，无外网项——候选定义即资产核验）：① 数据层 e2e `scripts/collection-e2e-smoke.mjs` 现行重跑＝**16/16 SMOKE OK**（真 fs + 真 store/library/taskCard bundle：建项目→pending 任务卡→模拟管道回填 done+素材→类别树/全文搜索/最近素材/失败卡/路径防线全过）；② UI 层 e2e `scripts/collection-e2e-ui-smoke.mjs` 现行重跑＝**11/11 ALL PASS**（无头 Chrome 9224 + out/renderer + devShim；页面发起采集→注入模拟管道回填→fs 事件自动刷新流转 done→详情结果预览闭环）——**App 侧改动（useFsChanged 19b01d4 等）后两脚本仍履约，无漂移**；③ 任务卡约定四方核对＝CollectionBar.submit 六键（status/类别/关键词/需求/来源/创建，L131-147）＝模块设计 §11 落盘样例（299 行）＝parseTaskCard 解析键（shared/taskCard.ts L58-72）＝rebuildTaskCardForRetry 重建键（L45-55）＝devShim 演示卡（L226-266 同构）；`status: pending` 唯一处理判据、isLibraryResultPath 结果路径防线（L26-28）完好；④ 命名约束核对＝采集池「任务_<14位ISO ts>.md」（CollectionBar L129）＋管道回填「素材库/<类别>/采集_<任务名>.md」（e2e 与 devShim 演示同构）；「roam」全仓检索仅 Windows APPDATA\Roaming 无关命中＝档案措辞笔误，实际指命名约束（④）。
- **产品规划**：走查结论＝e2e 履约健康、约定四方一致，无需代码级修；唯一发现＝**模块设计 §11 303 行「期望来源/归属类别」措辞与 299 行权威落盘样例（来源/类别）及实现不一致**（同段内部漂移，易误导后续实现/管道脚本）；为什么现在修＝候选 1 明文「发现问题直接修」＋权威源一致性红线；怎么做＝303 行措辞对齐（一行级）；**不动**：299 行样例（权威）、CollectionBar/parseTaskCard/taskCard.ts/管道约定/两个 e2e 脚本（均健康）；验收口径＝三道门。
- **技术落地**：`docs/模块设计-V2.md` §11 通道 App 侧行 front matter 改「status: pending / 类别 / 关键词 / 需求 / 来源 / 创建，与上方落盘样例一致」。
- **测试验收**：三道门 typecheck ✅ / build ✅ / npm test **70 文件 703 例全绿** ✅（docs 改动不涉编译，全量跑确认零回归）。
- **落档**：候选 1 **完成 ✅**（e2e 履约健康定论 + 文档漂移修复——为 cron 恢复后的「真端到端复验」排期扫清资产，恢复即可直接复验）；另盘点登记：**13 个冒烟脚本 BASE 硬编码 8123/8899（无 ZJ_SMOKE_BASE 覆盖）+ 50 个脚本头注释「python3 -m http.server 8123」过时**（实际＝`node scripts/serve-renderer.mjs 8123`，仓库已备该脚本）——登记为下一候选；候选 2 第 20 次核实仍缺（待主会话）、候选 3 第 20 次仍锁屏；下一步计划重写。

**提交**：629e056 — docs(平台层): 模块设计§11 通道措辞对齐落盘样例——App 侧任务卡 front matter 改「类别/来源/创建」与实现一致（采集 e2e 履约走查发现文档漂移）

### 2026-09-15 13:30–14:5x（本轮：候选 1——冒烟回归全量首跑 + 基线落档；顺带修入口自包含与 subtasks 模板语法；提交见下）

- **开工第 0 步**：`git status` 工作树干净；近 90 分钟提交＝创作层 5beb22e/74406fd、智能层 8a01d8d/43dee62、体验层 90b4983，与本轮目标（scripts/smoke-ui.mjs、subtasks-smoke.mjs、docs 新文档）零交集；`docs/反馈清单.md` 无归属平台层「待落实/落实中」条目。环境核实：CDP 9224（Chrome 152.0.7977.84）✅、8123（python http.server）/8899（serve-renderer）✅、8810 未起（agent-cancel-live-* 两脚本自动 SKIP）；`pmset -g assertions` UserIsActive=0＝**仍锁屏**（候选 4 真壳验收持续受阻，本轮第 23 次）。
- **调研**（主题=档案候选 1；仓库内入口语义核读＋环境核实）：① smoke-ui.mjs 语义核读——`--all`＝文件名含 smoke（实测 **148 个**，较 10:30 轮档案 145 多 3 个新脚本 edit-error-retry/quick-char 等）；预检逐脚本端口探活（8123/8899/CDP 必选，8810 可选项 SKIP）；② **发现入口自包含**：isSmoke=`/smoke/i.test(f)` 匹配 'smoke-ui.mjs' 自身 → `--all` 把入口自己排进去，无参运行打印用法 exit 2＝恒 FAIL 项（基线 FAIL 清单实锤）；③ vLLM 在线但偏慢；④ 真模型驱动脚本（acts/engine-sync/subtasks/director-*/zj-edit/context-realmodel）被 `--all` 含入，`--timeout 300` 偏紧——acts-smoke 单独跑 480.7s 引擎报「写作引擎驱动超时（已中止引擎本轮）」（模型侧慢，非产品 bug）。
- **产品规划**：为什么现在做＝候选 1 即 10:30 轮指定「下一轮主做」；入口（115b9cd）已建，首跑基线是入口真正接地的第一份产出＋发布门禁。怎么做＝`node scripts/smoke-ui.mjs --all --timeout 300` 全量实跑（后台，日志 /tmp/smoke-regression-20260915.log）→ 结果落档 `docs/冒烟回归基线-2026-09-15.md`：标记长尾/脆弱/过时，**首次失败即归因**（12 个失败已逐个取证归因，不留裸失败）。**顺手修两个确定 bug**（纯脚本层，零产品逻辑改动）：smoke-ui 自包含、subtasks entry 模板语法。**不动**：src 产品代码、devShim、其余 PASS 脚本、采集管道；模型类 `--all` 口径调整登记下一个候选（本轮只定性）。
- **技术落地**：① `scripts/smoke-ui.mjs` L60 isSmoke 加 `&& f !== 'smoke-ui.mjs'`（注释）；② `scripts/subtasks-smoke.mjs` L33 entry 模板 `director: … },` 去尾随逗号——原生成 `const EMPTY = {…},` 后直接 `async function`＝ESM 语法错误（8b07021 引入 EMPTY 判空表时逗号错行，该轮只实跑 director-check、subtasks 从未完整跑过＝漏网）。
- **测试验收**（真实跑通）：全量首跑 **PASS 134 · SKIP 2 · FAIL 12 · 148（1940.5s）**；失败逐个归因：**2 模型类**（acts-smoke 引擎驱动超时 480s、engine-sync-smoke >300s——vLLM 慢）、**3 脚本断言过时**（ref-budget-ui 「上下文」旧文案被 7805329 用量行改版取代；sidebar-v01 「main 留白 12px」被体验层 3424210 零 padding 取代；toolbar-ui 980「部分保留+More」被 03deec5 窄窗折叠与最新断点取代）、**1 断言路径过时**（toolcard-fail-ui ④ `span.closest('div')` 被智能层 e9ccfad ToolChain 容器包装破坏）、**3 功能面真实疑点**（chapter-prefill P11 编辑器挂载 MilkdownError headingAttr（@milkdown/preset-commonmark 内部，src 零引用）；presence-ref 「沈爷」条目无「关联档案」refFile 显示；fixed-command ⑦ /巡查 未知参数枚举提示 devShim 下未触发——登记对应模块候选）、**1 偶发**（acts-adopt-ui 全量失败、单独重跑 1.9s PASS——登记脆弱观察）、**2 本轮已修**。修复验证：`node scripts/smoke-ui.mjs --list --all` 修复后全绿（147 项含 2 SKIP，不再含自身）；subtasks entry 语法修复由 esbuild 编译通过锁定（完整真模型 5 步因 vLLM 慢拖时长，后台续跑，不影响结论——原错误即 esbuild 编译错）；三道门 typecheck ✅ / build ✅ / npm test **73 文件 731 例全绿** ✅。
- **落档**：`docs/冒烟回归基线-2026-09-15.md`（权威基线文档：环境/总览/12 项归因表/结论与后续动作/复跑命令）；下一步计划重写。

**提交**：feat(平台层): 冒烟回归全量首跑基线落档+入口自包含与 subtasks 模板语法修复（详入文）

### 2026-09-15 16:30–17:1x（本轮：候选 1——模型类冒烟 `--all` 口径收口；提交 055d911）

- **开工第 0 步**：`git status` 工作树有**创作层 15:45 轮在途 WIP**（`tests/unit/proposals.test.ts` +42 行批注 replace-text 单测、`scripts/anno-drift-ui-smoke.mjs` 新脚本，均批注提案漂移主题）——与本轮目标（`scripts/smoke-ui.mjs` / docs）零文件交集，按并发契约**只 add 自己文件不整树提交**；近 90 分钟提交＝智能层 321fd06（15:00）、体验层 b633a81/1ea64c5（14:15），与本轮零交集；`docs/反馈清单.md` 无归属平台层「待落实/落实中」条目。
- **调研**（主题=候选 1，仓库盘点+外网；来源记档）：
  - 模型类判据选择：初用特征 grep（`LOCAL_LLM_KEY`/`127.0.0.1`/`vLLM`/`dsh-runtime`）命中 9 个，但 **engine-sync-smoke（基线 13:30 实锤真模型，runSync 出真实提案）不命中**——字面量特征不可靠；再核 bundle 特征 `main/agent/engine` 命中 7 个但 context-cast/context-char-tail 无 LLM_KEY 字面量（走默认 local 配置、注释明示「真模型 runChat 走查」），sync-produce-loop 注释自证「**无模型调用**」（吃 engine-sync 产物做闭环断言）应排除——**逐文件读注释调用面定名单才可靠**，最终 12 个（判定依据写入代码注释）。
  - 外网：Playwright 官方 **Annotations** 文档（https://playwright.dev/docs/test-annotations，经本机无头 Chrome CDP 9224 抓 body.innerText——web_extract 与 r.jina.ai 均被判私有网络拦截，套路同 13:30 轮 sidebar 调研）：内建 `test.slow()`（三倍超时+报告标记）、`test.fixme()`（官方文案「running the test is slow or crashes」→ 不运行）、条件 `test.skip`、`@fast/@slow` tag + `--grep` 过滤、test-projects 分项目跑不同子集——**官方把「慢/脆弱测试不进快速门禁」作为一等模式：测试自己声明身份（tag/annotation），运行器不靠路径猜测**。
  - **产品/创作视角**：发布门禁＝「改动后一切如常」的信任载体；主人长篇资产按月/年积累，门禁必须确定性可归因——真模型冒烟依赖算力池（127.0.0.1 vLLM）忙闲**非织卷代码红/绿判据**（基线实锤：acts 480s 引擎侧驱动超时、engine-sync >300s，均为模型侧慢），混进门禁＝噪声侵蚀「全绿」信任；Playwright tag/分层（门禁快档＋慢档单独 job）正是此问题标准解法，织卷对应 `--all`（快档门禁）与 `--live`（真模型慢档，建议 900s/不限）。
- **产品规划**：为什么现在做＝13:30 基线轮指定「下一轮主做」，且 `--all` 恒红 2 项模型类＝入口（115b9cd）作发布门禁的硬伤；怎么做＝`smoke-ui.mjs` 加 **MODEL_SCRIPTS 显式名单**（12 个，逐条核对调用面的判定依据写注释）＋ `--all`（无 `--live`）门禁跳过计 SKIP（有显示、不计失败）＋ `--live` 包含模型类（追加语义扩展）＋ 单独指名**不受过滤**＋ 名单启动自检（脚本删除/改名→exit 2 防腐化，杜绝「--live 少跑一个无人知」）＋ 汇总脚注（显示跳过数）；**不动**：脚本本体/预检/可选环境 SKIP 语义（8810）/`*-live.mjs` 集合语义/其余冒烟/src 产品代码；验收口径＝`--all --list` 无模型类且全绿、`--all --live --list` 含模型类、单独指名照常、负向验证两条（移除成员→回集合；名单腐化→exit 2）＋三道门＋真模型单独跑实证。
- **技术落地**：`scripts/smoke-ui.mjs` 头注释更新（模型类口径+参考来源）+ `MODEL_SCRIPTS` 常量（12 项+判据注释）+ `MODEL_SKIP_REASON` + `modelSkipped` 收集 + 过滤分支 + 汇总脚注 + 名单自检；`docs/冒烟回归基线-2026-09-15.md` 顶部加注记（#1/#2 归因已收口、其余稳定失败项仍按原表处置）。
- **测试验收**（真实跑通）：① `node scripts/smoke-ui.mjs --all --list`＝**139 个非模型全绿 + 12 模型类 SKIP + 汇总脚注，exit 0**；② `--all --live --list`＝**163 个**（139+12 模型类+12 `*-live`），无模型类 SKIP；③ 单独指名 `node scripts/smoke-ui.mjs zj-edit --list`＝PASS 1 不被过滤；④ **负向验证**：移除名单中 zj-edit → `--all --list` 它回集合 `[140/140] ✓`；名单改不存在名 → **exit 2**「模型类名单含不存在的脚本」——两条均实锤断言真实区分新旧；⑤ `node scripts/smoke-ui.mjs --all --list` 后 grep 复核还原（61 行 `'zj-edit-smoke.mjs',` 在）。三道门 typecheck ✅ / build ✅ / npm test **74 文件 741 例全绿** ✅；真模型冒烟 `node scripts/context-realmodel-smoke.mjs` **单独实跑 REALMODEL SMOKE OK（RM_EXIT=0，runChat 第2章回退链走查出「雾/栈桥/凌晨」命中）**——模型类脚本可单独/`--live` 跑不受影响，实证闭环。**截图契约说明**：本轮是运行器口径改动（门禁工具纯函数层），UI/交互状态零变化，无头截图无区分度（先例=13:30 轮路径函数修复同类说明）。
- **落档**：候选 1 **完成 ✅**；基线文档注记；下一步计划重写（候选 2 两条断言自修上位 P1——`--all` 全绿还差这两条+5 条其它模块登记项）；候选 4 第 24 次核实仍锁屏、候选 3 第 24 次确认 cron 仍缺；观察项新增「新增真模型驱动冒烟须同步 MODEL_SCRIPTS」。

### 2026-09-15 19:30–19:4x（本轮：候选 1——两条冒烟断言随产品口径自修 → 落地；提交 9e8df99）

- **开工第 0 步**：`git status` 工作树干净；近 90 分钟提交＝4b79b45/2119a5d（创作层 18:45）、f67b52b/f9acf4e（智能层 18:00），与 scripts/ 零交集；`docs/反馈清单.md` 无归属平台层「待落实/落实中」条目；环境核实：`pmset -g assertions` UserIsActive=0＝**仍锁屏**（候选 3 第 25 次核实受阻；本轮纯脚本层无需 GUI）；`hermes cron list` 第 25 次确认采集管道 76de7e949a3b 仍不在活动列表（候选 2 维持待主会话）；CDP 9224 + 8123/8899 就绪。
- **调研**（仓库内代码事实，主题=候选 1，即 17:1x 轮自立的「下一轮主做」）：① 基线归因 #7 核对——AgentPanel L977-986 用量行现状＝`对话 {fmtCtx} · 装配 ≤{fmtCtx(WCTX_MAX)}（chapterRel） · @注入 {n}条 ≤{fmtCtx(budget)}`，fmtCtx（L1241-1245）：≥1 万→万字、≥1 千→千字、否则「N 字」——**全站无「上下文」字样**，旧断言 L85 `includes('上下文')` 必 TIMEOUT；② 基线归因 #12 核对——summary span（L131-132「章节已读完」）最近 `closest('div')` 命中工具卡**内部 flex 行**（L105 `flex items-center gap-2 px-2.5 py-1.5`，无 border-hair），工具卡容器在 L98-104（done 分支 `border-hair bg-surface`），外层 ToolChain 容器（L180）也带 border-hair 但更远——closest 向上找 `div[class*="border-hair"]` 恒命中**内层工具卡**（两种包裹形态皆中）；③ 两条旧断言在基线 13:30 首跑均稳定失败（归因 #7/#12 实锤），本次是纯断言修正、产品行为零变化。
- **产品规划**：为什么现在做＝候选 1 到期（`--all` 门禁全绿的最后一项平台层自查；全绿后「改动后一切如常」有确定判据，主人长篇资产可安心随改随回归）；怎么做＝2 处断言级修改（ref-budget ① 改等「对话」+ 头注同行更新；toolcard-fail ④ `closest('div')`→`closest('div[class*="border-hair"]')`+ 4 行注释说明层级）；涉及：scripts/ref-budget-ui-smoke.mjs、scripts/toolcard-fail-ui-smoke.mjs；**不动**：src 产品代码（AgentPanel/devShim/contextCaps/atRefs 零改动，断言随实现口径走）、其余冒烟、基线文档（归因表保持原记录并注明已修——文档即历史）；无 UI 视觉/交互变化（脚本断言层，截图契约不触发——ref-budget 脚本内截图保留为既有资产）。验收口径＝两脚本实跑全过＋三道门＋旧断言失败性由基线首跑实锤（负向证据成立，不再 stash 重复）。
- **技术落地**：ref-budget-ui-smoke.mjs 2 处（头注 ① 行 + L85 evalUntil 改等「对话」，注释指向 AgentPanel L978）；toolcard-fail-ui-smoke.mjs 1 处（④ selector 改 `div[class*="border-hair"]`，注释说明 ToolChain 层级）。
- **测试验收**（真实跑通）：`node scripts/ref-budget-ui-smoke.mjs` **5/5 ALL OK**（① 初始有「对话」无 @注入、② 2 条→`@注入 2条 ≤8.0 千字`、③ 4 条→`≤1.2 万字` 总预算兜底、④ 清空消失、⑤ 截图 `/Users/USER/Pictures/zhijuan/ref-inject-budget-1936.png`）；`node scripts/toolcard-fail-ui-smoke.mjs` **SMOKE PASS**（④ 实证容器 cls＝`rounded-lg border text-[11px] border-hair bg-surface`——断言真实命中工具卡容器）；三道门 typecheck ✅ / build ✅（2995 模块）/ npm test **74 文件 741 例全绿** ✅。
- **落档**：候选 1 **完成 ✅**（`--all` 门禁平台层自查项清零，剩 5 条稳定失败均属体验层/智能层/创作层已登记）；候选 2 第 25 次核实仍缺（待主会话）；候选 3 第 25 次仍锁屏；下一步计划重写（候选 1＝629e056 轮盘点的「冒烟环境契约归一」尾巴——13 脚本 BASE 硬编码 + ~50 脚本头注释过时）。

### 2026-09-15 22:30–22:5x（本轮：候选 1「环境契约归一」实测核销（19:30 误立）→ 自发现「smoke-ui 探活升级内容级」→ 落地；提交 7676c47）

- **开工第 0 步**：`git status` 工作树干净（011e16c/d032740 创作层 21:45、ae5f27e/81bff6e 智能层 21:00、644b6a4/ba7f42a 体验层 20:15，均与 scripts/ 零交集）；`docs/反馈清单.md` 无归属平台层「待落实/落实中」条目；环境核实：`hermes cron list` **第 26 次确认**采集管道 76de7e949a3b 仍不在活动列表（候选 2 维持待主会话）、`pmset -g assertions` UserIsActive=0＝**仍锁屏**（候选 3 第 26 次核实受阻）；8123（python http.server --directory out/renderer）/8899（serve-renderer）/9224（CDP）就绪。
- **① 调研**：① 候选 1（19:4x 轮立的「冒烟环境契约归一——13 脚本 BASE 硬编码 + ~50 头注释过时」）**实测即核销**：`grep` 全量 115 个含 localhost:8123/8899 字面量的脚本**全部**有 ZJ_SMOKE_BASE 覆盖（0 遗漏），`python3 -m http.server 8123` 过时头注释 **0 处**、`spa_server` 旧注释 **0 处**——该工作 07:30 轮 c39a02d 已收口（107 脚本 + 51 处注释归一），19:4x 轮未核实即照抄 04:30 盘点重新立项，属**误立**；② 转 10:30 轮登记观察项「CK 探活为端口级（服务在≠内容对，bundle 新旧/页面内容靠各脚本自身断言）」——真实来源 Playwright Test webServer 文档（https://playwright.dev/docs/test-webserver，curl 直抓 69KB 正文）：`webServer.url` 健康检查=「URL 返回 2xx/3xx/400-401-402-403 状态码即服务就绪」——**行业（含 Playwright）健康检查均为状态码级，无内容级标准**；本机特化可行：`out/renderer` 是唯一构建事实源，远程服务必须与之同构建。**产品/创作视角（README 要求）**：门禁「全绿」=主人长篇创作资产「改动后一切如常」的唯一信任载体；若服务错起在旧构建/别的目录（本机实态：/tmp/spa_server.py 历史硬编码 ROOT、python http.server --directory 起错目录），端口探活通过则 145 个脚本**整轮跑在旧 bundle 上仍输出「全绿」＝假绿**，且技能实踩坑「改了源码没 rebuild、服务停在旧 bundle」正同源——判据补上后回归防线才完整。
- **② 产品规划**：为什么现在做＝观察项「探活为端口级」到期＋假绿风险真实且与技能实踩坑同源；怎么做＝smoke-ui 预检升级两级判据：**(a) 构建新鲜度（全局一次）**：`out/renderer/index.html` mtime 必须 ≥ src/ 全递归与 electron.vite.config.* 最晚文件（容差 1000ms），过期→环境级失败计入退出码（**门禁必须红**，否则还是假绿）；**(b) 服务内容一致性（逐服务缓存一次）**：对非 9224/8810 端口 fetch `/index.html`，提取构建引用面（script/link src）与本地 `out/renderer/index.html` 比对，不等→FAIL 给双方资产清单+启动提示；非 HTML 响应（模型 API 等）跳过、本地无构建参考时以全局检查为准（防重复报）；涉及：`scripts/smoke-ui.mjs`（+105/-2）；**不动**：端口探活语义、8810 可选 SKIP、CDP /json/version、MODEL_SCRIPTS/名单自检、各冒烟脚本（零/src 产品代码）、基线文档（本轮另注记）；无 UI 视觉/交互变化（门禁工具纯函数层，头截图契约不触发——先例 13:30/16:30 同类说明）。验收口径＝正向（真服务预检过）+ 负向双实证（假服务/过期构建必被拦）+ 三道门 + `--all --list` 全量回归。
- **③ 技术落地**：`smoke-ui.mjs` 新增 `checkBuildFreshness()`（src 递归 mtime + config 文件，容差 1000ms）、`localIndexRefs()`/`contentCheck()`（URL 级缓存 Map）；`precheck` 端口探活通过后对非 9224/8810 端口接内容校验；主流程开头执行 freshness、汇总前把失败以 `（构建检查）` 环境失败条目计入退出码；头注释同步（预检两段+参考来源）。零产品代码改动、零新依赖。
- **④ 测试验收**（真实跑通）：正向 `node scripts/smoke-ui.mjs states-smoke --list`/`home-stats --list`＝预检 PASS（8123/8899 内容一致）；**负向 B（内容不一致）**：临时脚本 `_probe-fake-smoke.mjs` 引用假服务 8135（/tmp/fake-renderer 伪造 index.html）→ 预检 **FAIL「内容校验：服务内容与本地构建不一致…远端 assets=[] vs 本地=[…index-Cqywy8F6.js, …index--Gd-660e.css]」** 计入失败、退出码 1（临时脚本与假服务已清理）；**负向 C（构建过期）**：`touch src/main/index.ts`（内容不变、git 工作树仍干净）→ 报「⚠️ 构建过期：src/main/index.ts（…22:43:52）晚于 out/renderer/index.html」且 `（构建检查）[环境]` 计入 FAIL（PASS 1·FAIL 1，门禁红）→ `touch out/renderer/index.html` 恢复 PASS——两条负向均实锤判据真实区分；三道门 typecheck ✅ / build ✅（3016 模块）/ npm test **74 文件 741 例全绿** ✅；`node scripts/smoke-ui.mjs --all --list`＝**PASS 141 · FAIL 0** 全量预检回归（模型类 12 正常 SKIP 脚注）。
- **落档**：观察项「探活为端口级」**关闭 ✅**（7676c47：内容级+新鲜度两级判据）；候选 1（环境契约归一）**核销**（19:30 误立，c39a02d 已收口——本轮实测证据入档，防再立）；候选 3 第 26 次仍锁屏、候选 2 第 26 次仍缺（待主会话）；下一步计划重写（候选 1＝门禁判据回归资产化：把手动负向实证固化为自检脚本）。

### 2026-09-16 01:30–01:5x（本轮：候选 1「smoke-ui 门禁判据回归资产化」→ 落地；提交见下）

- **开工第 0 步**：`git status` 工作树含**创作层 00:45 轮在途 WIP**（ProposalDrawer.tsx「全部接受」失败可见 + untracked `allapply-fail-ui-smoke.mjs`；01:41 由 51c511c 提交落档）——与本轮文件集（scripts/smoke-* + docs）零交集，按并发契约只 add 自己文件；`docs/反馈清单.md` 无平台层「待落实/落实中」；环境：锁屏（UserIsActive=0，纯脚本轮不受影响）、CDP 9224/8123/8899 就绪、node v24.19.0、构建产物新鲜；候选 2 **第 27 次** `hermes cron list` 仍无 76de7e949a3b（维持待主会话）。
- **① 调研**：仓库事实——smoke-ui 两级判据逐行核对（`checkBuildFreshness` L138-170：src 递归 + config、容差 1000ms、环境失败计入退出码；`contentCheck` L189-217：仅非 9224/8810 端口、refs 引用面比对、URL 级缓存；`isSmoke` L92 仅排自身）——7676c47 的负向实证当时仅手动做过（临时脚本/假服务 8135/touch src，临时物已清理＝验证失传风险）。外网（本机 CDP 9224 实抓；web_search 三次空、web_extract 未配后端故走抓取）：Stryker 官网首页「**Test your tests with mutation testing.**」（https://stryker-mutator.io/ 正文实抓）＋官方 FOSDEM 2024 talk「Who's testing the tests? Mutation testing with StrykerJS」——**变异测试＝对被测对象注入「变异体」（故意缺陷），断言测试真会红**；这正是门禁判据自检的同构思想（surviving mutant ≈ 判据被误放宽后 `--all` 静默回旧假绿）。**产品/创作视角**：门禁是主人「改动后一切如常」的唯一信任载体（长篇创作资产回归前提）；判据不固化成资产＝下次顺手改 smoke-ui（如体验层新增 UI 冒烟时调预检）手动验证即失传，假绿防线无预警失效。
- **② 产品规划**：为什么现在做＝候选 1 到期（7676c47 明示立项）；怎么做＝新增**自包含**自检脚本 `scripts/smoke-gate-check.mjs`：自起 bad/good 双假服务（`listen(0)` 随机端口，bad=伪造旧构建 refs、good=本地 `out/renderer/index.html` 原文）＋临时探针 `scripts/_gate-*.mjs`（collectPorts 兜底识别 localhost 字面量）→ 四次断言＝判据 2 负向必 FAIL／判据 2 正向必 PASS／判据 1 负向必 FAIL（`utimes` 把 electron.vite.config.ts 前移 5s，内容零改动）／判据 1 恢复必 PASS（产物 mtime 提到最新）→ `finally` 清理假服务/探针/还原 mtime；顺带 `smoke-ui.mjs` isSmoke 排除 `smoke-gate-check.mjs`（**元测试，跑在 --all 之前单独执行，不属页面冒烟集合**——否则 `--all` 会把它当页面冒烟嵌套拉起）；基线文档追加注记。**不动**：判据本体（仅名单排除 1 行）、全部冒烟脚本、src 产品代码、MODEL_SCRIPTS；零新依赖。无 UI 视觉/交互变化（CLI 工具层，截图契约不触发——先例 13:30/16:30/22:30 同类说明）。验收口径＝自检实跑 4/4＋三道门＋`--all --list` 全量预检回归（确认排除生效、收集不破）。
- **③ 技术落地**：新增 `scripts/smoke-gate-check.mjs`（~150 行：产物存在前置＋历史 `_gate-*` 残留清理＋双假服务＋跑 smoke-ui 断言＋finally 清理；头注变异测试思想/用法/副作用说明）；`smoke-ui.mjs` isSmoke 名单 +1 排除（含注释）；`docs/冒烟回归基线-2026-09-15.md` 头部追加注记（资产化+用法+9e8df99 收口说明）。
- **④ 测试验收**（真实跑通）：`node scripts/smoke-gate-check.mjs` **PASS 4 · FAIL 0** ×2（落地初跑一次、`npm run build` 后复跑一次——证明判据对新鲜产物自洽、无假阳性）；三道门 typecheck ✅ / build ✅（3018 模块）/ npm test **75 文件 748 例全绿** ✅；`node scripts/smoke-ui.mjs --all --list`＝**PASS 144 · FAIL 0 · 全绿 ✅（0.7s）**（144＝scripts/ 158 个含 smoke 文件 − smoke-ui 自身 − smoke-gate-check − 12 模型类；收集与预检零回归，新脚本未混入）；临时探针/假服务运行后自动清理，`git status` 复核仅余本轮文件。
- **落档**：候选 1 **完成 ✅**；候选 2 第 27 次核实仍缺、候选 3 第 27 次仍锁屏（待主会话/解锁）；下一步计划重写（候选 1＝MODEL_SCRIPTS 漏网守卫脚本化——观察项 ㉔ 机械化）。

## 五、下一步计划（2026-09-16 01:5x 更新）

1. **候选 1（P1，锁屏可做·下一轮主做）：MODEL_SCRIPTS 漏网守卫脚本化**——观察项 ㉔「新增真模型驱动冒烟必须手动登记 MODEL_SCRIPTS，未登记=漏网需人盯」机械化：新增 `scripts/smoke-model-audit.mjs`（静态启发：扫 scripts/ 含 `runChat/runSync/runSubtask/runDirector/runActs/zj-bridge/LLM_KEY/真模型` 等特征串的 smoke，与 MODEL_SCRIPTS 求差集→列出疑似漏网待人工复核并入；只提示不阻断——判定最终仍需人工）。**产品/创作视角**：模型类冒烟误入 `--all` 会让门禁恒红（基线已实锤 acts/engine-sync 全量 300s 被杀=误杀），漏网守卫保证名单长期不靠人盯。验收＝脚本实跑（含构造一个 `-live` 冒烟做正向与差集断言）＋三道门。（上一轮候选 1＝门禁判据回归资产化已完成 ✅，见迭代日志 01:5x。）

2. **候选 2（P2，跨模块·需主会话/主人执行）：恢复「织卷素材库采集」管道 cron**——76de7e949a3b 失联（末次 2026-09-03；**本轮第 27 次 `hermes cron list` 实查仍无匹配**）；重建处方见 9-14 第 17 轮迭代日志；推进器不改 .hermes，仅登记；恢复后 e2e 资产（数据层 16/16、UI 11/11）可直接复验。

3. **候选 3（P2，待解锁验证）：真壳验收批（一次会话做五件）**——①项目导出真 Electron 壳（系统模态 dialog）；②Agent 面板宽度真壳端到端（拖拽→zhijuan-settings.json 写 agentPanelWidth→重启恢复）；③首页「更改库根路径」真壳链路；④系统菜单真机展开过目；⑤系统菜单真机键盘实测 (b)（⌘S/⌘F/⌘G 各一次只发生一次）+ (c) role 与 ProseMirror 关系；**本轮第 27 次核实仍锁屏**（UserIsActive=0），解锁后按既有规划执行。

4. **候选 4（P3，观察项/评估关闭清单，维持）**：①-㉓ 维持上一轮清单（createProject 模板应用 devShim 不模拟——已登记合理简化；agent mock 诊断字段——已评估关闭；scanAnnotations dev 防重——已评估关闭；模板演示项「悬疑短篇」可复用 consistency ⑩ 段；Novel/DocSection 写盘刷新优化属创作层；electron-stub 符号只增不删；系统菜单全局速查与页内实例并存；P6.5 依赖 devShim 对象方法可覆盖；zj-find-count 已关闭；minWidth 已随体验层关闭；W-4..W-7 随 win 包立项；盘点教训=按模式集合 grep；冒烟脚本头注释归一规格——**已由 c39a02d 收口**；smoke-ui 探活为端口级——**已由本轮关闭（7676c47 升级内容级+新鲜度）**；带 live 的 smoke 混在 --all 拉长（已由 MODEL_SCRIPTS 收口）；agent-cancel-*-smoke 依赖 8810 桥；OPTIONAL_PORTS 硬编码 8810；chapter-prefill 编辑器挂载 MilkdownError headingAttr（登记创作层/体验层排查）；presence-ref「沈爷」条目 refFile 未显示（登记智能层+体验层）；fixed-command ⑦ /巡查 未知参数枚举提示 devShim 未触发（登记智能层）；sidebar-v01/toolbar-ui 断言过时（登记体验层按新口径更新）；acts-adopt-ui 偶发失败（登记脆弱观察）；vLLM 慢（已由 MODEL_SCRIPTS 口径收口））＋㉔ 新增真模型驱动冒烟脚本时必须同步登记 `smoke-ui.mjs` 的 MODEL_SCRIPTS（名单自检会捕获删除/改名，但新增未登记=漏网，需人工并入）＋㉕ **推进器轮次把旧盘点重立候选前先实测核实**（本轮实证：19:4x 轮把 07:30 轮已收口的「环境契约归一」重立为候选 1——grep 实测 0 残留后核销；教训=重立候选先跑判据，别照抄盘点措辞）＋㉖ **改 smoke-ui 门禁判据（checkBuildFreshness/contentCheck/isSmoke/预检）后的标准动作＝先跑 `scripts/smoke-gate-check.mjs` 再改**（本轮已固化：脚本头注＋基线注记写明；不跑=判定改动是否误放宽/收严无自证，回归信任链断）。

5. **长期留意（不变）**：`.zhijuan/index` 检索索引**暂缓**（全扫 ≈6ms/MB；触发=单项目素材 md>3000 或总字节>20MB，候选 minisearch/flexsearch，不要 SQLite）；`.zhijuan/tasks` 采集状态机（多任务管控/重试时再立项）；**打包实施＝主会话发布冲刺进行中**（9-15 目标 mac 可用+包）。**工程红线**：凡「真机 + devShim 双实现」的解析/决策逻辑，先抽 shared 纯函数再挂两侧；冒烟脚本环境契约按已收口规格写；新冒烟脚本头注写 `node scripts/serve-renderer.mjs <port>`；**真模型驱动冒烟＝门禁外身份，声明进 MODEL_SCRIPTS 而非改名凑 `*-live.mjs`**。

6. **模块设计平台层欠账现状 + 发布冲刺缺口（2026-09-16 01:5x 更新）**：§四 A/§五 B/§十二/§九 F/§十一 H 全 ✅；发布冲刺缺口「系统菜单」两刀+去重 ✅；快捷键一致性体检 ✅（94f0f41）+ 编辑器实测两波 ✅；Windows 兼容走查+首修/二修 ✅（W-1..W-3）；采集管道 cron 恢复=候选 2（第 27 次核实仍缺）；采集 e2e 履约走查 ✅（629e056）；冒烟环境契约收口 ✅（c39a02d，19:30 误立候选已核销）；冒烟一键回归入口 ✅（115b9cd）；冒烟回归全量首跑基线 ✅（文档 `docs/冒烟回归基线-2026-09-15.md`）；模型类 `--all` 口径收口 ✅（055d911：MODEL_SCRIPTS 显式名单 12 个）；两条过时断言自修 ✅（9e8df99）；smoke-ui 预检升级内容级 ✅（7676c47：构建新鲜度+服务内容一致性两级判据，负向双实证；观察项「探活为端口级」关闭）；**门禁判据回归资产化 ✅（本轮：scripts/smoke-gate-check.mjs，变异测试思想自检 4/4×2）**——平台层剩余实质缺口=MODEL_SCRIPTS 漏网守卫（候选 1）/ 候选 3 真壳验收（待解锁）/ 反馈清单新条目 / 自发现小修。

