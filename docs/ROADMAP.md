# 织卷 · 长期计划（ROADMAP）

> 本文档是织卷的规划权威源。每次迭代前先读它；每个里程碑完成后更新它。
> 定位由主人 kk 定稿（2026-08）：织卷不是通用写作工具箱，而是围绕两条**引擎**做深做透的 AI 辅助创作工作台。

## 一、定位（主人钦定的两个核心）

1. **要素引擎** —— 关键要素的设定与召回，解决长文章后期「啃书」问题（写 50 章以后不失忆、设定自洽）。
2. **导演引擎** —— 用情绪曲线 + 人物曲线作为每一章的导演指令综合控制生成，让 AI 生成的故事**不再平淡**。

其余功能都是这两条主线的配套，砍多余、不铺摊子。

## 一点五、V2 架构转向（主人 2026-09 定稿）

> **发布冲刺（2026-09-12 主人定，直到 9-15 验收）：** 织卷达到「可用」+ mac 打包（win 推迟）——目标/缺口/验收见 **docs/发布冲刺-2026-09-15.md**（本轮阶段权威源，优先于本 ROADMAP 其余条目；各模块档案件与缺口对应关系在该文第二节）。> 冲刺期已到 9-15：首版 mac dmg 已产出（168MB，dsh-runtime 入包验证通过），未收尾项（打包产物真机冒烟/图标）与剩余缺口全部经**周交付窗口**滚动（机制见 docs/周交付窗口-机制-2026-09-16.md），不再单列冲刺日期。

> **续拍板（2026-09-16，主人拍板，两条）：** ① **多时间线叙事纳入 V2 一期**——产品设计新增「时间线」维度（章头 `时间线` 字段，缺省=主线；人物/世界状态按线分叉；「到本章为止」按线内序），验收口径 D-V2-5 修订为「含至少两条时间线交错叙事的中篇自洽」；设计权威源=**docs/多时间线叙事-设计文档-2026-09-16.md**（对应反馈 F-20260916-02）。② **推进体系改周交付窗口**：每周五固定验收一个确定增量，交付点天然确定（对应反馈 F-20260916-01；机制=docs/周交付窗口-机制-2026-09-16.md，订阅请见 docs/模块推进/README.md）。

> 主人重新定稿了织卷的架构方向，完整设计见 **docs/架构设计-V2.md**（新架构权威源）。要点：
> 1. **泛化**：不参考任何进行中具体项目，通用创作工作台。
> 2. 核心：**素材库积累** + **面向时间切片**的人物/世界观/环境设定；切片承载每章当下的世界状态。
> 3. **第一期全部文档化**（markdown），不做结构化存储；唯一约定是章节头的切片 front matter。
> 4. 首页＝项目管理；项目内左侧边栏：正文创作 / 人物设定 / 世界观设定 / 素材库。
> 5. 正文创作＝markdown 编辑器 + 右侧 agent 区，边聊边生成；可选中段落引用；相关设定都在 agent 下；**保存正文 → 触发更新当前章节对应时间切片的其他设定**。
> 6. 素材库支持按需求联网采集（素材库页表单 → 采集池 → 本机采集管道回填）。
>
> **已拍板（2026-09，主人在本会话拍板）**：D-V2-1 V1 遗产整体搁置；D-V2-2 一章一个时间切片；D-V2-3 设定改动走提案制（正文走“应用到章节”显式动作）；D-V2-4 采集走后端管道；D-V2-5 验收改为“写完一个中篇且各切片设定与正文自洽”。**UI 选型**：Tailwind v4 + shadcn/ui + Milkdown，设计语言＝暖纸面（Notion 系）+ 精密中性铬（Linear 系）混合。
>
> 模块级设计见 **docs/模块设计-V2.md**（新模块权威源）；实现按 **V2-S1～S6** 里程碑推进（见该文 §15）。
>
> 下文「现状快照 / M1 / M2」记为 **V1 历史**；V1 资产（要素条目、导演板、分幕、行为轴、兑现检查）已按 D-V2-1 **整体搁置**，待 V2 闭环后再回看回接。

## 二、现状快照（v0.1，2026-08，V1 历史）

### 已有（保留）
- 项目库：`~/Documents/织卷项目库/<id>/project.json`，纯 JSON、本地方便入 git
- 数据模型：Worldview / Character（自由字段）/ Chapter（elements、premise、curves、beats、content）/ SeriesCurve / PlotBeat
- 曲线采样：把曲线 0~100 分 8 段算出趋势（骤升/缓升/平缓/猛跌）+ 情节点定位，结构化进 prompt
- 生成：拼整包 prompt → 本地 vLLM（127.0.0.1:8888，deepseek-v4-flash-vision-exp-uncensored，1M 上下文；2026-09 vLLM 重启换名，旧 id 0731 已 404/空返）；`content || reasoning` 双保险已在
- 章节导出 md

### 短板（对应两个痛点）
1. 上下文是**整包硬塞**：全部世界观 + 全部人物全量进 prompt；长文时设定膨胀 → 噪音盖过信号；正文本身从不进 prompt → 前情全忘。
2. 曲线只是**参考建议**：一次性生成整章，长度一上去曲线契约就在中间流失；人物曲线只是抽象状态，没映射到「该角色具体怎么动」；写完没有「曲线承诺是否兑现」的检查。

## 三、路线图（按里程碑推进）

### M1 要素引擎（先做 —— 导演引擎的输入也要靠它保证最新状态）

**M1.1 设定条目化（数据模型改造）**
- 把世界观规则、人物身世/档案等大文本升格为**条目卡片** `Element`：id、分类（世界观/人物/场景/道具/规则）、标签、正文、状态、版本、更新时间。
- 各章的 `elements`（本章要素）可引用条目 id 或标签组，单独标「本章强制」。
- 平滑迁移：旧 project 加载时把自由文本折成条目，已有项目无缝可用。

**M1.2 前情记忆（每章写完后）**
- 每章完成后自动产出一份「章节审计」：一段事件摘要 + 出场人物各自的**结束状态快照** + 已兑现的伏笔 + 新埋的伏笔，结构化落库。
- 伏笔台账：跨章节前后指针；生成时把「未兑现伏笔清单」放进 prompt，从根上杜绝烂尾。

**M1.3 按需召回（每次生成前）**
- **组配器**：由本章要素（谁 / 在哪 / 要什么）+ 曲线情节点，算出召回清单：
  - 出场人物的完整档案 + 最近几章状态轨迹；
  - 本章相关的设定条目（场景、规则、器物）；
  - 前 1~2 章摘要 + 更早的衰减摘要。
- 第一版用纯本地标签/关键词匹配（零新依赖）；后续数据量上来再加 embedding 增强（可选）。
- **预算硬控**：设定 ≤2000 字、前情 ≤1200 字，超出按优先级截断 —— 长文就在这里保持不糊。

**M1.4 设置页**
- LLM 端点 / 模型从写死处（ChaptersView.doGenerate）抽到设置，README「已知限制」就此了结。

### M2 导演引擎（让曲线有牙齿）

**M2.1 曲线硬约束（导演板） ✅ 完成（2026-08-13）**
- `shared/director.ts`：把曲线切成 8 段，每段给出**戏剧任务**（白热化 / 推进 / 拉锯 / 低谷），定位**本章波峰（climax）与波谷（nadir）**，做**落差检测**（相邻段骤变时强制「必须用具体事件撑起」），情节点按进度归入对应段。
- generator 的曲线段由「采样参考」升级为「导演板硬指令」直达 prompt；verify-director 6/6；全 verify 现为 47 断言。

**M2.2 分幕生成 ✅ 完成（2026-08-13，提交 045ecb3）**
- `shared/acts.ts`：把导演板 8 段按戏份权重均衡地切成 **3~6 幕**（波峰段只属于一幕、落差段所在幕强制事件承载），确定性的线性切分；每幕带自己的**主调（白热/推进/低谷/拉锯）与戏剧任务**，`renderActDirective` 只下发本幕的段作为硬指令。
- **逐幕生成**：generator 按幕单独请求，每幕 prompt 带「前一幕末状态」（尾段约 1000 字）承接续写，结尾定格留给下一幕，曲线契约整章贯彻；支持 **「回滚到某幕重生成」**（保留前 N 幕，从第 N+1 幕起重写）。
- 数据：`Chapter.acts?: string[]`（新增可选字段，旧章节 normalize 自动回填为整幕，不破坏兼容）。
- verify-acts 16 断言（含回滚重演通路的 e2e）；全 verify 现为 **63** 断言；typecheck / build 全绿。

**M2.3 人物曲线 → 行为轴 ✅ 完成（2026-08-13；按主人要求升级为可视化可拖拽曲线）**
- `shared/axes.ts`：`CurveAxis`（自由命名）+ 通用三档（被动·生涩 / 拉锯·试探 / 主导·放开）+ 可选的预设档位（主动权/压迫感/距离/情欲/羞耻防线 的库保留为可选）；`pivotAxes` 把旧单轴曲线合成为一根轴（向后兼容）；`bandAt` 按值取档给动作要求；`translateAxisCurve` 把一根轴曲线按段采样成段级**动作硬命令**，档位跨段跳变时强制「必须用具体事件接住这一跃」。
- **UI（本日主会话）**：行为轴变为**可拖拽的可视化曲线** —— 每条人物曲线下方可「＋新增行为轴」，每根轴由 `AxisCurve` 组件呈现（名字可自由改、可增可删、整条曲线可拖），即见即所得；原来「下拉选轴」已移除。
- 导演板每段的 `curves` 携带 `acts`（每根轴的档位 + 动作要求），整章导演板、分幕指令、兑现检查都跟随；fulfill 按每根轴生成检查项。
- verify-axes 现为 9 断言；全 verify **90** 断言全绿；typecheck / build 全绿。**注**：织卷与主人正在进行的保密小说项目完全隔离，不用其内容当测试或种子数据。

**M2.4 曲线兑现检查 ✅ 完成（2026-08-13，由主会话收尾提交）**
- `shared/fulfill.ts`：从导演板（段任务）/ 行为轴 / 情节点派生**兑现清单** → 正文按块切分 → 交本地模型核验 → 结构化报告；`main/fulfill.ts` 为 LLM 调用层；UI 在章节页展示报告并可回看。
- `verify-fulfill.mts`（18 断言）+ `smoke-fulfill.mts`（12 断言）；全 verify 现为 **90** 断言全绿（M2.3 的 axes 重构把 verify-axes 收窄到 9 条，故总数从 93 回落）。
- **M2 至此全线完成**：导演板 → 分幕生成 → 行为轴 → 兑现检查。

### M3 体验与稳定（攒着别急）
- **项目模板 ✅ v1 完成（2026-09-09，平台层）**：模板＝`<工作区>/模板/项目模板/<模板名>/` 纯文件目录；新建项目可选「初始内容」（空白/示例/用户模板），补充复制不覆盖骨架与 project.md；内建「示例」教 front matter·切片·目录约定。设计依据见 docs/模块推进/03-平台层.md 第 2 轮。**devShim 口径审计回归资产（2026-09-13 平台层）**：`scripts/devshim-consistency-smoke.mjs`（⑩ 段含「多模板选择」非内建模板演示断言，见平台层档案 22:30 轮）。
- 多章连写（**✅ 2026-09-11 创作层**：新建章节预填上一章切片名/涉及人物 = 237d309；章节列表右键菜单（重命名/**修改切片名**/导出单章 md/删除）＝eb0e4d3 + 4867b74（修改切片名＝约定头切片字段编辑入口+引用面收口），M3 多章连写闭环）；正文版本历史（**✅ 2026-09-10 创作层**：保存自动快照 `.zhijuan/history/` + 历史抽屉「列表/行级 diff/一键恢复」，提交 c696f5b + 92ca8fe）；**同步记录（✅ 2026-09-16 创作层**：每次切片同步旁路追加 `.zhijuan/sync-log.jsonl`（时间/章节/切片/人档/提案/守卫/结果，上限 500 条），时间线页「同步记录」抽屉可回溯，不动同步链路本体）；打包可分发的 .app（**可行性规划 ✅ 2026-09-10 平台层**，形态与风险见 docs/模块推进/03-平台层.md 第 3 轮；实施待主人立项）
- embedding 增强召回（可选）

### M4 模型适配接入层 ✅ 完成（2026-09，主人拍板）
- **模型适配接入层**（src/shared/providers.ts）：除本地 vLLM 外，可对接 DeepSeek / 智谱 GLM / OpenAI / Claude / Gemini 各家远程 API；设置页选服务商 + 填 key 即切，写作引擎按新厂商重启（沿用既有机制）。各家默认型号按官方文档核实（glm-5.3-flash、deepseek-v4 系、gpt-5.6、claude-4.5 系、gemini-2.5 系），全部可改。
- **harness 默认生效**：删除 agentEngine 开关与 legacy 直连路径（llm.ts / context.ts / sliceSync legacy 分支），dsh 写作引擎是唯一创作引擎。
- **创作上下文装配到主进程**（src/main/agent/context.ts）：runChat / runSync 自动注入 当前章 + 上一章尾部 + 涉及人物档案 + 当下切片世界观 + 本章章卡 + 素材库路标（素材文件为权威运行时生成，2026-09-18 起——静态 素材库/索引.md 无维护端已回归作者侧目录文档）；预算硬控沿 M1.3 精神；不再要求模型每轮自己现读（细节仍可 zj_* 读）。
- 详案见 docs/模型适配接入层与创作上下文-2026-09.md。

### M5 agent 区交互完善 ✅ 完成（2026-09）
- **编辑页瘦身**：删除正文页 top 的章节名 header 行（左栏已有选中态）；「本章小环」入口并入 Agent 面板头部；切片同步结果改浮动提示，不占版面。窗口 Chrome 保持 红绿灯后品牌名 + 最右侧引擎状态。
- **agent 区能力对齐模型侧**：新增 思考过程折叠块（reasoning 流）· 工具活动卡（含参数，如「读文档 正文/xx.md」）· 正文修改卡（IDE 式 行号前/>后对比，采纳才写盘，写入经 fs 事件让编辑器静默重载），移除「应用到正文」复制粘贴式按钮。
- **新工具 zj_edit_doc**（src/plugins/zj-core.ts）：只读校验 find 唯一性并算好 before/after 载荷，不写盘；主进程 translate 将载荷转 `edit` 事件，采纳走 doc:applyEdit（find 唯一替换）。prompt 引导模型用工具改正文而不是甩整篇。
- **输入框**：发送按钮悬浮右下角；左侧常驻上下文用量——**分解口径（2026-09-14 智能层 7805329）**：对话（历史 trim+输入+quote，与主进程同口径）/ 装配 ≤4.1 万字（打开章节时，shared/contextCaps 同源；2026-09-17 复核后 chapter 8000→12000）/ @注入 ≤1.2 万；**不显示「X/6 万」**（60000 是渲染层单条回复首屏截断、非上下文预算）。
- **输入框 @ 引用 + / 命令 ✅ 完成（2026-09-11 智能层）**：`@` 触发浮层（人物/章节/世界观/素材，空 query 素材不刷屏；↑↓/Enter/Tab/Esc；插入所见即所得引用文本 `〔类型·名称｜路径〕`，9fd1359）；`/` 触发内置命令浮层（/续写 /润色 /延伸，命令=命名 prompt 模板+参数占位，发送前展开为任务指令；正文改动仍走 zj_edit_doc 修改卡口径）。两套触发同规则互斥（取光标前更近者）。shared 纯逻辑（mention.ts / commands.ts）可单测；无头 UI 冒烟 scripts/skill-menu-smoke.mjs + 命令模板真模型冒烟 scripts/skill-command-live.mjs。**@ 引用注入 ✅（2026-09-11 智能层 a7ed2b5）**：发送时主进程解析 `〔类型·名称｜路径〕` → 读取文档内容注入本轮上下文（main/agent/refs.ts，预算单引用 4000/合计 12000，章节剥约定头、人物/世界观/素材保留；对照 Cursor @ mention 口径）；真模型严格用例（引用默认上下文不含的总纲，模型复述「出海点灯」原文）实证注入生效；冒烟 scripts/at-refs-smoke.mjs + scripts/at-refs-live.mjs。**对话流收尾 ✅（2026-09-12 智能层 48bc9ca）**：/巡查 参数枚举校验（未知参数就地提示）+ /导演 参数=作者要求接进导演板 prompt + /导演 运行中可点「停止」（取消=不落资产）。**工具链轨迹 ✅（2026-09-15 智能层 e9ccfad）**：相邻连续 meta 卡聚合为 ToolChain 容器（左缘轨迹竖线+「工具链 · N 步」+步序号 i/N），链内 zj_read_doc 重复读同一文档标「续读」徽标（offset 续读链可一眼追溯）；toolArgs 抽 shared/summarizeToolArgs（file 带 offset 时 args 展示 `(offset=N)`）；单测 18 例 + tool-chain-ui-smoke 9/9 + stream-interleave 回归 5/5 + 真模型 zj-read-offset-live OK。
- 验证：typecheck / build 全绿；无头 UI 全流程实测（思考→读档→修改卡→采纳写盘回读一致）；真模型 zj_edit_doc 冒烟通过（scripts/zj-edit-smoke.mjs，模型自纠后给出完整前后对照载荷）。

### M6 导演引擎回接（V1 能力回接，按 D-V2-1，2026-09-07 起）
- **章节导演助手 ✅ 完成（2026-09-07）**：动笔前用写作引擎给一章导出「导演板」（情绪弧分段 + 每段戏剧任务：推进/白热化/拉锯/低谷 + 波峰定位 + 涉事人物行为轴：被压/试探/放开 + 写作红线 + 要还的钩子），随章卡直写 `大纲/<章>_导演.md`。走子任务骨架 + 能力注册表；素材复用 buildWritingContext（与 runChat / runSync 同一创作半径）。详见 `docs/章节导演-进度-2026-09-07.md`。
- **导演板随行注入 runChat 上下文 ✅ 完成（2026-09-08）**：正文创作时若本章已有导演板，buildWritingContext 自动注入「本章导演板」块（剥 front matter、预算 ≤2500 字、带硬指令：沿情绪弧分段与行为轴写、红线不许破、钩子到结尾要还）——导演板从「编辑器外可查看」变成「有牙齿地约束成文」。单测 +1（共 74 例）；数据层真项目冒烟（织卷smoke 第02章导演板 985 字进上下文）实锤。
- **导演兑现检查 ✅ 完成（2026-09-08）**：写完一章后对照本章导演板逐项核对——情绪弧分段 done/partial/missed、行为轴 aligned/drifted/absent、红线 kept/broken、钩子 paid/open/new，输出 summary+四类条目。走子任务骨架 + 能力注册表（设置页有开关）；材料只给 导演板 + 本章正文（请求小而准），axes 人物按本章涉及人物白名单防编造；**只读不改稿、不落盘**（导演板才是落盘副产物），结果经 IPC 回 大纲区「兑现检查」抽屉。至此「先设定（导演板）→ 约束成文（随行注入）→ 兑现核对」三步成环。单测 +9（共 83 例）；真模型冒烟 scripts/director-check-smoke.mjs。
- **导演板偏旧提示 ✅ 完成（2026-09-08）**：大纲区若某章导演板比正文更旧（正文在导板后又改过），侧栏「导演板」子项给「偏旧」角标、选中该章时顶栏下方一行轻提示建议重导——护栏住「兑现检查对照旧承诺」的坑。纯渲染层只用 mtime 对比（shared/boardAge.ts，可单测），不动主进程。单测 +4（共 87 例）；无头真机验证角标与横幅都出来；顺带修了 8a11cc6 带入的渲染崩溃（兑现检查按钮 title 的 hasBoard(selChapter!) 在未选中时把空值捅进 boardRel → 大纲区整页白屏）。
- **分幕生成 ✅ 完成（2026-09-08）**：有了导演板的章，把全章按导演板情绪弧分段**逐段**交给写作引擎成文（一段一个独立请求、前段末文带进下一段作为真正的续写上下文、本段戏剧任务为硬指令），拼成本章**定稿草稿**落 `大纲/<章>_分幕.md`（与章卡/导演板同约定：写作副产物·直写，不直接动正文，正文仍走 EditCard 采纳口径）。配套新增 shared/boardParse.ts（导演板 md → 结构化，V1 分幕能在 V2 直接拿分段做事，不必再把原文塞给模型自己解析）。单测 +12（共 99 例）；真模型冒烟 scripts/acts-smoke.mjs。
- **分幕草稿「采纳为正文」✅ 完成（2026-09-08）**：大纲区有分幕草稿的章，可一键把本章正文**整稿替换**为草稿（保留本章原约定头与题名、正文主体换草稿段，草稿文件保留、可再改再采纳）——「设定→分幕→定稿」从大纲区一步到正文。两击确认防误触（改稿类显式动作）。走**主进程 doc:adoptActs**：读章与草稿 → 过 adopt 纯逻辑（shared/actsAdopt.ts，可单测）→ writeDoc 落地，经 fs 事件让编辑器静默重载。单测 +6（共 105 例）；真数据冒烟：织卷smoke 第02章旧正文 35 字 → 分幕正文 1005 字、约定头原样保留；无头 UI 全流程（分幕→两击采纳→正文已换）通过。M6 至此把「导演板 → 分幕 → 采纳」走成一条**先设定后成文的完整链**；下一步可回接 V1 分幕的其余能力或继续打磨兑现检查下游。
- **路径前缀约定对齐 ✅ 完成（2026-09-08）**：真机 Novel 页一处长期隐藏 bug——`listChapters` 返回的 `file` 是相对 `正文/` 的裸名（组织约定），但 renderer 把 `c.file` 当**项目相对路径**用（DocEditor / AgentPanel / 本章小环 / 切片同步），真要点已有章节要么读到项目根游离文件要么空白。统一在边界拼 `正文/` 前缀再传主进程；devShim 的 listChapters 之前多拼了 `正文/` 前缀（dev 下全线叠成 `正文/正文` 全挂、真机被掩盖），改回与真 store 一致。无头 UI 点章后编辑器读到真实正文、outline 分幕→采纳全流程通过。
- **分幕首段带上一章结尾 ✅ 完成（2026-09-08，提交 950591f）**：分段链里首段是唯一没有自带承接的位置（其余各段都有前段末文）；此前非首章首段只给一句话「章节前情」，冷启动接不上气。现在 `runActs` 按章号找上一章，剥约定头取末 900 字作为首段「上一章结尾」硬承接（真正从上一章末尾续写，不让上一章的事当背景重述）；首章或无前章时不挂。写段循环里每段写完把 prevTail 换成本段末文，接续自然移交。单测 +2（共 107 例），三道门绿。
- **非首章分幕真模型冒烟 ✅ 完成（2026-09-08 晚）**：acts-smoke 在修复后的干净环境重跑——非首章（第02章）按导演板前 2 段逐段起草，**630s 出 1899 字草稿**（acts=2，分幕约定头/草稿落盘正常），首段提示词实测含「【上一章结尾】」+ 上一章末 900 字（`谁也没再说话` 承接实锤），草稿从上一章结尾续写、氛围连贯。此轮同时**发现并修复冒烟脚本 dsh 孤儿残留**（随本轮 fix(smoke) 提交）：acts-smoke / director-smoke 的 entry 在 try 块内直接 `process.exit()`，会让 finally 里的 `await closeHarness()` 只开始不完成（Node 实测 process.exit 不等 async finally）→ 每次跑完边车 dsh 进程残留为孤儿（本次跑完就留了一个，630s 的进程还在占内存）。修复：统一 `exitCode` 变量 + finally 内 `await closeHarness()` 之后才 `process.exit`；director-smoke 真机重跑验证 `dsh-after=0`。单测仍 112 例，三道门绿。详细见 docs/推进器日志.md。
- **分幕缺段防护 ✅ 完成（2026-09-09，提交 a990682）**：分幕某段重试后仍太短（<120 字）时，原逻辑静默跳过该段并把「有洞」草稿照常写盘、界面还报成功——被采纳即残缺正文。现该段记入结果 `failed[]`：全部失败则整单失败并报段号；部分失败则草稿头部注入 ⚠️ 缺段警示（段号+段数+请勿直接采纳），界面明示「缺段处会断戏」并提示重跑或手补；承接链语义不变（后续段仍从上一成功段末文续写）。单测 +4（共 116 例），三道门绿。
- **分幕「补写缺段」✅ 完成（2026-09-09，提交 6bf626a）**：缺段防护的下一步——每段约一轮完整请求，补一段比重跑全章省时。草稿升级为带「## 第 N 段」段标记（新 shared/actsSeg.ts：标记渲染/反解析/缺段警示抽取，纯逻辑可单测；采纳为正文时剥标记与警示行）；runActs 增 `opts.only/onlyFailed`（只重写指定/缺失段，成功段原样保留、承接取最近已存在段末文、补写首段用上一章结尾）；大纲区选中章草稿含缺段警示时显示「补写缺段」按钮（只补失败段，不重跑全章）；字数口径只计段文本（不计标记）。单测 +11（共 127 例），三道门绿；真模型冒烟：对真实全量跑产出的缺段草稿（第 1 段成文+第 2 段失败）跑 onlyFailed → 正确识别缺段、只补第 2 段、第 1 段原样保留、模型未写够长时警示不消失（机械链路端到端实锤；当日该模型第 2 段连续失败为环境观察，非代码引入）。
- **采纳分幕后自动触发切片同步 ✅ 完成（2026-09-09，提交 56768f7）**：补闭环缺口——`doc:adoptActs` 采纳分幕草稿为正文时直接 writeDoc 整稿替换（正文被换掉但设定没有跟进），而定案「保存正文 → 触发切片同步（正文为源、设定为流）」只挂在编辑器保存路径上，采纳这条「先设定后成文」的落地点反而断了。现在 Outline 页采纳成功后自动调 runSliceSync（与保存正文同口径），提示「切片同步中…」→ 生成 N 条提案 / 无设定变化；同步失败不影响采纳结果（正文已替换，提示稍后保存再触发）。验证：三道门全绿（127 例），无头 UI 冒烟 scripts/acts-adopt-ui-smoke.mjs（devShim 演示项目：分幕生成 → 两击采纳 → msg 出现切片同步结果 + 正文读回已替换）通过。
- **兑现检查下游：「没兑现」→ 重写对应分幕段 ✅ 完成（2026-09-09）**：兑现检查此前只读展示（对照导演板核出 missed/partial/drifted/broken 就完事），发现问题后没有处置入口。现在检查抽屉的情绪弧条目若为「没兑现/部分兑现」，给出「重写第 N 段」按钮：N=该弧在导演板中的顺序（检查报告 arcs 按板段落逐段给出、runActs 段号同序，映射按顺序、不靠模型字段）；点击走 `agentActs({ only: [N] })` 只重写该分幕段、其余段原样保留（与补写缺段同一条 only 通道），无分幕草稿时按钮禁用并提示先「分幕生成」。同时修掉 runActs `only` 路径一个静默覆盖风险：此前指定段号时若草稿不存在/是旧格式（无段标记），会用「只含目标段的空草稿」把原稿盖掉——现在无草稿/旧格式直接报错。单测 +3（共 130 例）；无头 UI 冒烟 scripts/acts-rewrite-ui-smoke.mjs（兑现检查 → 点「重写第 2 段」→ 草稿第 2 段被重写、第 1 段原文保留）通过。

## 四、迭代原则

- 每到一个里程碑就以「**用织卷真的写完 5 万字且前后自洽**」为验收，功能各要素齐全但走不通写作流程，不算完成。
- 关键决策由主人拍板，敢于直接问，不装作全懂。
- 数据纯本地、明文可入 git，作品永不锁死在工具里。

## 五、待拍板决策（随着迭代滚动更新）

| 编号 | 决策 | 状态 |
|---|---|---|
| D1 | 第一刀先下哪个引擎：要素引擎（推荐，先解决失忆）还是导演引擎 | 待主人 **此刻拍板** |
| D2 | 长文记忆档位：结构化前情召回（推荐，token 可控）vs 整书大上下文硬塞 | 待拍板 |
| D3 | 是否内置「生成前先出要素+曲线方案 → 主人确认 → 再写正文」的流程 | 待拍板 |
