# 项目引导 · 章节要素 · 工作区（2026-09-04）

> 主人拍板的方向：新项目要走流程导引；章节要有故事要素；设置里有工作区，相关文档在工作区内落档。本文是整个改动的一个快照。

## 已完成（全部 typecheck + build，落库提交见 git）

### 项目创建引导（交付前有）
- **入口**：Home 新建项目后 `navigate(/project/<id>?guide=1)`，Workspace 看到就弹 `features/guide/ProjectGuide.tsx`（用完即清参）
- **流程**：世界观（时代背景/世界基调/核心设定要点）→ 主要角色（可增删，姓名/身份定位/关键特征）→ 完成页；可随时「以后补充」跳过
- **落盘**：世界观写 `世界观/总纲.md`（替换模板，保留目录约定），每角色**一个** `人物/<名>.md`（基础档案+成长轨迹按切片模板）
- 注意：引导只在新创建进项目那一刻弹一次

### 新建章节 = 故事要素流程（Novel.tsx）
- 表单：题名* / 时间切片 / 涉及人物 + **本章目标、核心冲突、关键事件、前情呼应**
- 创建时按要素初始化正文：**front matter + “本章故事要素”引用块 + 标题**；该块随正文进入 **agent 上下文装配与切片同步**

### 设置 → 工作区（workspace）
- `AppSettings.workspace`（空 → 默认 `~/Documents/织卷工作区`）；设置页最上「工作区」卡：路径 + 落档按钮 + 文档列表可点开读
- **落档**：`<工作区>/文档/使用说明.md` 与 `约定与结构.md`（在 `src/main/workspace-docs.ts`）；主进程 `registerIpc` 首次启动自动幂等落档，设置按钮手动补
- **项目库默认位迁移**（store.libraryRoot）：设置里 libraryRoot 为空时——**老默认 `~/Documents/织卷项目库` 若存在且非空则原地保持（已有项目不消失）**；否则新默认 `工作区/project`。**写代码时务必保留这个回退判断**
- IPC 新增：`workspace:status` / `workspace:init` / `workspace:read`（主进程落盘）；devShim 用内存文档演示

## 验证（都真跑了）
- 无头页面完整驱动：建项目 → 引导全流程 → 总纲/角色读回 WORLD_OK/CHAR_OK → 同 tab 建章节（四要素）→ 正文含要素 ELEM_OK（devShim 内存）；工作区设置页：初始→点落档→已落档 2 篇→展开读文（devShim）
- 主进程真落盘冒烟（**scripts 在仓库外**）：`ZJ_REPO=$PWD node /tmp/zj-cdp/ws-smoke.mjs` → 使用说明/约定落地、幂等、**libraryRoot 回退到旧库** 均验证

## 待办
- 真 Electron 全栈一件验收（解锁后）：含引导落盘、工作区落档的真机路…
- 引导“以后补充”**没有二次提醒**：正文页可根据总纲还是模板（或本文件判定）给横幅入口，主人未要就有

## 设置页二级分类（2026-09-04 完成）
- 重构成左侧一级导航 + 右侧分区内容（**SECTIONS 常量在 Settings.tsx**，将来加配置先挂这里）：工作区与项目 / 写作引擎（模型连接、常用工具、运行）/ 外观与数据 / 关于；保存按钮移到每个分区右上角固定
- 无头验证：四个分区切换正确、内容按区展示
