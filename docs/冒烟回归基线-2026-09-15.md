# 织卷 · 冒烟回归基线（2026-09-15 全量首跑）

> 由 `docs/模块推进/03-平台层.md` 候选 1（2026-09-15 13:30 轮）执行：`node scripts/smoke-ui.mjs --all --timeout 300`（148 个脚本，全量首跑，32.3min）。
> 此后发布冲刺/大改前重跑并对比本基线。**基线=入口（115b9cd）接地的第一份产出**，也是「改动后一切如常」的参照系。
>
> **注（2026-09-16 01:5x 平台层轮，候选 1 资产化）**：smoke-ui 两级门禁判据（构建新鲜度/服务内容一致性，7676c47）的负向实证已固化为仓库内自检脚本 **`scripts/smoke-gate-check.mjs`**（变异测试思想：自起「旧构建/过期构建」两个变异体，断言两判据必 FAIL、恢复后必 PASS；实跑 4/4）——以后改 smoke-ui 判据前后各跑一次即可自证，不必再手动搭假服务/touch 文件；该脚本为元测试，已从 `smoke-ui --all` 收集排除（isSmoke 名单）。基线 #7（ref-budget 断言）/#12（toolcard 断言）已由 9e8df99 自修收口，不属当前失败项。
>
> **注（2026-09-16 04:30 平台层轮，候选 1 收口）**：`MODEL_SCRIPTS` 名单已抽为单源 `scripts/model-scripts.mjs`（smoke-ui.mjs 共用引用，判据/维护契约见该文件头注），并新增漏网守卫 `scripts/smoke-model-audit.mjs`——静态扫描 scripts/ 特征与名单求差集（只提示不阻断；`--strict` 可门禁化；`--selfcheck` 判据自检 5/5）。**新增真模型驱动冒烟后必须：登记 model-scripts.mjs → 跑 smoke-model-audit 确认健康**（观察项 ㉔ 机械化收口）。
>
> **注（2026-09-17 01:30 平台层轮，候选 2 核销＋守卫资产化）**：候选 2「数据层冒烟环境契约补全」经实测核销——引用 `electron-stub` 的 52 个脚本全部带独立 clean userData（51=固定独立目录＋启动 rmSync 清空（含 `const UD=process.env.ZJ_USERDATA` 别名形态 7 个）或 mkdtempSync 全新目录；1=zj-bridge.mjs 设计豁免（answerDir 与边车插件同目录、不读 settings）），**零缺口**；剩余风险（新增脚本忘设/忘清）已资产化为静态守卫 **`scripts/smoke-userdata-check.mjs`**（判据＝无 ZJ_USERDATA／固定目录无启动清空→硬缺口；mkdtemp→干净；其他→软提示；`--strict` 门禁化 exit 1；`--selfcheck` 5/5 变异测试思想；白名单与 SELF 排除见头注），并同 07:30 模型审计先例接入 `smoke-ui.mjs --all` 自动审计（只提示不阻断）。**新增数据层/真模型冒烟后必须：ZJ_USERDATA 指向独立目录并启动清空（或 mkdtemp）→ 若脚本头注含模型类特征词（zj-bridge/真模型 等）同时登记 model-scripts.mjs 的 REVIEWED_NON_MODEL**（交叉误报实踩：守卫头注含强特征 zj-bridge 被模型审计误判漏网，已人工核对登记）。
>
> **注（2026-09-16 07:30 平台层轮，候选 1 接入）**：漏网守卫已接入 `smoke-ui.mjs` 门禁主流程——`--all` 自动跑一次审计（非 strict，同进程复用 analyze/report；健康显示一行、疑似漏网列清单），**默认只提示不阻断**（continue-on-error 语义，官方原语 docs.github.com/en/actions/reference/workflows-and-actions/workflow-syntax），CI 需阻断用 `node scripts/smoke-model-audit.mjs --strict`；负向实证：临时强特征探针 → 汇总「模型类审计不健康」预警且 exit 0（探针自建自删）。审计脚本为元审计，已与 smoke-gate-check 同口径从 `--all` 收集排除（isSmoke 名单）。
>
> **注（2026-09-15 16:30 平台层轮，候选 1 收口）**：本表 #1（acts-smoke）/ #2（engine-sync）的「模型类超时」归因已在 `scripts/smoke-ui.mjs` 以 `MODEL_SCRIPTS` 显式名单收口——真模型驱动脚本（12 个）`--all` 默认门禁跳过计 SKIP、`--live` 或单独指名运行。新口径下 `--all` 不再出现模型类 FAIL，但**其余稳定失败项（#3/#5/#6/#7/#8/#11/#12）仍按原表处置**（各模块档案登记），重跑命令不变。

## 一、运行环境（2026-09-15 13:30 实测）

| 项 | 状态 |
|---|---|
| 机器/时间 | macOS 26.3.1，2026-09-15 13:30 CST；锁屏（UserIsActive=0，无头验收路径） |
| CDP 9224 | ✅ Chrome/152.0.7977.84（无头，本机专用） |
| 8123 | ✅ python http.server（serve out/renderer） |
| 8899 | ✅ `node scripts/serve-renderer.mjs 8899`（SPA fallback） |
| 8810（zj-bridge） | ❌ 未起 → agent-cancel-live-* 两个脚本自动 SKIP（可选环境，不计失败） |
| out/renderer | 当前已构建产物（含 09-15 上午各轮改动；基线环境即此） |
| vLLM 127.0.0.1:8888 | 在线但响应偏慢（见 §三 模型类归因） |

## 二、总览

```
PASS 134 · SKIP 2 · FAIL 12 · 共 148（1940.5s ≈ 32.3min）
```

- 脚本数 148 与 10:30 轮档案记的 145 不同：**近 3 轮新增 3 个 smoke**（edit-error-retry、quick-char-* 系等），基线以实测为准。
- 失败率 8.1%；其中 **2 个已本轮修复**（smoke-ui 自包含、subtasks entry 语法），**7 个稳定失败待对应模块更新**，**2 个模型类长跑超时**（参数/环境问题），**1 个偶发**（重跑通过）。

## 三、失败明细与归因（12 项）

| # | 脚本 | 结果 | 归因（实测证据） | 处置 |
|---|---|---|---|---|
| 1 | acts-adopt-ui-smoke | 重跑 **PASS** | **偶发**：全量时「点击: NOT_FOUND → TIMEOUT 正文编辑器就绪」；单独重跑 1.9s 全过 | 登记脆弱脚本（连跑三次仍偶发→改等待逻辑）；下一轮回归继续观察 |
| 2 | acts-smoke | 全量 300s 被杀；单独跑 **480.7s 引擎报「写作引擎驱动超时（已中止引擎本轮）」** | **真模型长跑 + vLLM 慢**：分幕生成 480s 未完成触发引擎侧驱动超时——非产品 bug，模型侧波动 | 模型类脚本不进 `--all` 300s 档；acts 归入 live 类或 --all 排除（候选） |
| 3 | chapter-prefill-ui-smoke | 稳定失败（仅 P11） | **真实前端 console 错误**：`MilkdownError: Context "headingAttr" not found`（@milkdown/preset-commonmark 内部；src/ 无 headingAttr 引用）——编辑器挂载时报错；P1-P10 功能全过 | 登记创作层/体验层：Milkdown 插件注入面排查；全量首跑唯一抓到 console 错误的脚本（其余开编辑器冒烟未查 console） |
| 4 | engine-sync-smoke | 全量 300s 被杀；**单独跑 PASS** | **runSync 走引擎真模型**（syncSystem 提示词→引擎生成本轮比对），vLLM 慢时 300s 不够；单跑 40min 内出 `ok:true` + 真实提案（人物切片修正「机场值夜 vs 雾港栈桥重逢」/世界观环境补充）+ evidence | 同 #2：模型类脚本超时档问题（候选） |
| 5 | fixed-command-ui-smoke | 稳定失败（⑦） | `/巡查 乱写` 后「无法识别…本章（短巡查）」枚举提示未出现；AgentPanel L798 提示逻辑存在、L123 断言文案与实现一致——devShim 下未触发（fxBusy/checkEngine/时序待核） | 登记智能层：固定命令未知参数枚举提示 devShim 链路 |
| 6 | presence-ref-smoke | 稳定失败 | 「沈爷」条目已渲染，但「关联档案：人物/沈藏.md」未显示；devShim L1148 注释声明 unlisted 命中带 refFile，AuditDrawer L198 渲染 `it.refFile`——数据/渲染字段未对上 | 登记智能层+体验层：在场抽屉 refFile 显示链路 |
| 7 | ref-budget-ui-smoke | 稳定失败 | 旧文案断言过时：L85 等 `innerText.includes('上下文')`，而 09-14 7805329 已把用量行改版为「对话 X · 装配 ≤3.7 万 · @注入 N 条」（AgentPanel L958-960，无「上下文」字样）；usage-line-ui-smoke（新版断言）PASS | 平台层：改脚本断言（等「对话」+ 核对 @注入 文案），一行级 |
| 8 | sidebar-v01-smoke | 稳定失败 6 项 | **断言过时**：断言「main 台面留白 12px/台缝 ~12px」，被 09-14 体验层 3424210「页面容器零 padding」设计变更明确取代（文本区留白只由 .ProseMirror padding 提供） | 登记体验层：V-01 留白断言按新口径更新/删除 |
| 9 | smoke-ui.mjs | exit 2 | **入口自包含 bug（本轮已修）**：`isSmoke=/smoke/i` 匹配自身 → `--all` 把 smoke-ui.mjs 排进去，无参运行恒打印用法 exit 2 | ✅ 修复：isSmoke 排除 `smoke-ui.mjs`（提交见档案） |
10 | subtasks-smoke.mjs | esbuild 编译错（全量）→ **修复后编译通过且已执行到模型步** | **entry 模板语法 bug（本轮已修）**：L33 `director: … },` 尾随逗号 → `const EMPTY = {...},` 后接 `async function` = ESM 语法错误；8b07021 加 EMPTY 判空表时引入，从未实跑验证（9-12 22:30 轮只跑了 director-check） | ✅ 修复：去尾随逗号；修复后 esbuild 编译通过+ bundle 已启动执行（完整 5 步模型结果挂后台/下轮） |
| 11 | toolbar-ui-smoke | 稳定失败 | **断言过时**：980 宽「部分保留 + More」超时；L107 注释自述「原 900 口径因 Agent 面板宽拖拽失效」→ 改 980 仍失效；03deec5 窄窗折叠 + 体验层最新断点（toolbar-disabled 700 全量口径）后「部分保留」档已不存在 | 登记体验层：按现断点更新中窄窗断言 |
| 12 | toolcard-fail-ui-smoke | 稳定失败（④） | **断言选择路径过时**：脚本 `closest('div')` 取到内容行（cls 无 border-hair）；AgentPanel L95 done 容器仍有 border-hair，但智能层 e9ccfad ToolChain 容器包装改变 DOM 后 `span→closest(div)` 不再命中容器 | 登记平台层/智能层：断言改向上找容器（closest('[class*=border-hair]') 或 data-testid） |

## 四、结论与后续动作

1. **全量入口自身修复**：smoke-ui.mjs 不再把自己排进 --all（已修，见提交）；修复后全量=147 个。
2. **subtasks-smoke 修复**：entry 模板语法错误已修（同提交）；完整真模型验证（vLLM 慢，可能 20min+）挂在下一轮或本会话末尾查看。
3. **模型类脚本（acts / engine-sync）超时定性**：不是代码问题——`--all --timeout 300` 档对模型驱动脚本不足，且 acts 单独跑触达引擎侧 480s 驱动超时（vLLM 慢）。**建议**：模型类 smoke 从 --all 排除（进入 --live 语义），或 --all 对模型类自动放宽超时——列为下一轮候选 1。
4. **稳定失败 6 项**（#3/#5/#6/#7/#8/#11/#12 中稳败的）：按表处置已在对应模块档案登记：
   - 平台层自修：#7（ref-budget 断言一行改）、#12（toolcard 断言改——若愿揽）→ 排下一轮候选。
   - 创作层/体验层：#3 headingAttr、#8 sidebar-v01、#11 toolbar-ui（断言/插件问题）。
   - 智能层：#5 fixed-command 提示、#6 presence-ref refFile。
5. **偶发 #1 acts-adopt**：登记脆弱观察，下次全量回归复看。
6. **重跑命令**（对比基线用）：
   ```bash
   node scripts/smoke-ui.mjs --all --timeout 300   # 全量（模型类超时预期仍 FAIL = 已知项）
   ```
   首次失败即登记修复候选，不留裸失败（本轮已全部归因）。

## 五、基线快照（PASS 134 个略，失败明细见 §三；完整日志 /tmp/smoke-regression-20260915.log）

- 更新时间：2026-09-15 14:3x CST（本次归档）。

## 六、第二次全量实跑（2026-09-16 10:30–10:5x 平台层轮，菜单化后首跑）

```
PASS 134 · FAIL 15 · SKIP 12（模型类门禁外）· 共 149（886.4s ≈ 14.8min）
（另：模型类名单审计 1 项弱特征疑似漏网 → 人工核对为 devShim 链路 → 已登记 REVIEWED_NON_MODEL，审计归健康）
```

- **与首跑（13:30）关系**：脚本集 148→149（+sync-log-ui-smoke）；**首跑 PASS 的 9 个脚本本轮变 FAIL**——根因＝首跑后 3e0ec55（09-15 18:13）「检查」菜单化（10 个检查 icon 收进折叠菜单）＋1ea64c5（09-15 15:17）Home 搜索框（import-ui 第一 input 被搜索框截胡），**均为「功能已变、断言未适配」而非产品回归**；首跑 12 项失败中 7 项仍 FAIL（chapter-prefill/fixed-command/presence-ref/sidebar-v01/toolbar-ui/tool-chain + 新增 tool-detail 同因），2 项已修（9e8df99），2 项模型类已转门禁外。
- **15 项全归因与处置（不留裸失败）**：
  - 8 项＝菜单化根因（actgaps/mixform/nameform/order/presence-ui/sliceord/unused-ui/presence-ref 入口段）→ **f87df1c 已适配**（openCheckMenu＋menuitem pointer 序列），7 项实测全绿、presence-ref 通到既有 refFile 登记断言；
  - 1 项＝搜索框根因（import-project-ui）→ **f87df1c 已修**（input 限定导入对话框），成功+失败路径全 PASS；
  - 4 项＝既有登记项（chapter-prefill P11 headingAttr→创作层/体验层、fixed-command /巡查 提示→智能层、sidebar-v01/toolbar-ui 断言过时→体验层）；
  - 2 项＝既有环境超时（tool-chain/tool-detail 续读链，发送链路当前宿主环境超时，04:30 轮已注明非本改动回归）。
- **重跑命令（同 §四.6）**；下一轮候选 1＝全量复跑核销 f87df1c（预期 FAIL 收敛至 7 个＝4 登记＋2 环境＋presence-ref 既有断言）。
- 完整日志 `/tmp/smoke-full-20260916-1030.log`；档案归属 `docs/模块推进/03-平台层.md`（10:5x 轮）。

## 七、维护契约速查（2026-09-16 10:5x 更新）

- 改 `scripts/smoke-ui.mjs` 门禁判据 → 先跑 `scripts/smoke-gate-check.mjs`（㉖）；改 `scripts/model-scripts.mjs` → 跑 `node scripts/smoke-model-audit.mjs`（㉗）；改 `scripts/smoke-model-audit.mjs` → `--selfcheck`＋正常审计（㉘）；**新增 `scripts/smoke-userdata-check.mjs` 相关（改自身加 SELF/白名单/判据）→ `--selfcheck`＋正常审计；新增数据层冒烟（引用 electron-stub）→ 守卫自动审计（有硬缺口即提示）**——四道契约均为「门禁自证」链条，缺一不可。
- **改演示种子/演示 UI 结构 → 必须先 `grep -rn "<断言特征>" scripts/` 交叉核对再动手**（2026-09-17 10:30 平台层轮实踩：F-20260916-05 迁走本地规则后，`scripts/` 9 个审计/菜单冒烟整体断链——数量断言（dropdown B2/B3「检查菜单 10 项」）、入口路径断言（8 个 `openCheckMenu`→`[role="menuitem"]` 点击）与 `devShim.ts` 硬编码投影（demo-aseya stats.chapters=3/lastChapter=第03章、demo-multiline/demo-order lastChapter=最大章）全部过期；本轮已统一适配为「规则体检状态栏」路径＋Tab 切换，并修正 devShim 投影与真机 `listProjects` 口径（chapters=文件数 5、lastChapter=listChapters[0]=章号升序第一）。**规则：任何轮次改 devShim 演示数据、Agent 面板入口/菜单数量、抽屉 Tab 列表后，必须同步 grep 硬编码期望并跑相关冒烟（至少 9 个审计/菜单族脚本单跑一遍），否则留断链给周五验收。**
- **功能点收敛类 UI 改动（按钮 icon-only/删除/菜单化）→ 先跑 `node scripts/textclick-audit.mjs` 再提交**（2026-09-17 16:30 平台层轮，观察项 ㉟ 落实）：该工具扫描 scripts/ 全部「按钮/菜单项按文本查找」调用点（98 处），与 ICON_ONLY_LEXICON（已收敛按钮文本，手维护在工具头）求交集——零命中 exit 0（当前状态：✅ 零断链，13:30 轮全量 166 绿实证），有命中 exit 1 并列出文件:行。**契约（三条）**：① 任何 UI 收敛提交必须**同步更新 ICON_ONLY_LEXICON**（新旧按钮文本）并跑本工具；② 命中=改脚本断言为 aria-label/title 查找（toast-ui 先例：f535050 后 13:30 轮改 aria-label 点击），不许删断言蒙混；③ 负向断言（「按钮已移除」类，icon-size「引用选中」先例）工具自动豁免，勿改。工具自身判据自检=临时探针正向命中/删除恢复（16:30 轮实证），控噪=只扫 button/menuitem 上下文，内容断言（toast/正文/章节项）不误报。

## 八、第三次全量实跑（2026-09-16 13:30–13:50 平台层轮，候选 1 核销）

```
PASS 146 · FAIL 6 · 共 152（实跑，790.9s ≈ 13.2min）
（另：模型类门禁跳过 12 个（--live/单独跑）；模型类名单审计健康）
```

- 与第二次（10:30）关系：脚本集 149→152（**+3**：multiline-audit-smoke（智能层 49558e4）、multiline-context-smoke（创作层 41c2105）、toolbar-press-ui-smoke（体验层 4392ae5）；日志 94/95 位＝多线两脚本实测 PASS；**multiline-slices-smoke（平台层本轮新建）在本次实跑结束后落盘，未在 152 内**——已 `smoke-ui.mjs multiline-slices` 指名 PASS（门禁预检两级判据拦截过期构建→build 后全绿，7676c47 生效实证），下轮全量自动入列（153）。
- **6 项失败全部与既有登记一一对应（零新增失败）**：4 登记＝chapter-prefill P11 headingAttr（→创作层/体验层）、fixed-command ⑦ /巡查 参数提示（→智能层）、presence-ref「关联档案：人物/沈藏.md」未显示（→智能层+体验层）、sidebar-v01 LIGHT/DARK 留白断言（→体验层）；2 既有环境超时＝tool-chain/tool-detail「续读链结束」（04:30 轮已注明本宿主环境发送链路超时，非回归）。
- **收敛**：较 10:30（FAIL 15）→ 6：10:30 轮修复 9 脚本复证全绿；toolbar-ui（体验层登记项）已由体验层 4392ae5（11:15 中窄窗断言改逐级试探）转 PASS——登记项清 1。
- 与 09-15 首跑对照：首跑 12 项中仍未解决＝本章 6 项（4 登记+2 环境）；其余已修或已转门禁外。
- 完整日志 `/tmp/smoke-regression-20260916-1330.log`；档案归属 `docs/模块推进/03-平台层.md`（13:5x 轮）。

## 九、第四次全量实跑（2026-09-16 16:31–16:57 平台层轮，候选 1 基线刷新）

```
PASS 139 · FAIL 16 · 共 155（1022.5s ≈ 17min）
（另：模型类门禁跳过 12 个——--live/单独跑；多线冒烟四件 [95-98/155] 全部 ✓）
```

- 脚本集 152→155（**+3**：multiline-slices-smoke（平台层 638fb04）、multiline-view-ui-smoke（体验层 3f2eaf8）、createchapter-line-ui-smoke（创作层 0181b8d））——第三次 §八 明示的「multiline-slices 下轮自动入列」已确认，**多线四冒烟（audit/context/slices/view）全 PASS**，周增量 #6 的冒烟资产全量内闭环。
- **FAIL 16 全部归因（不留裸失败）**：
  - **9 项＝CDP 9224 长跑退化**（约 [148/155] 后失联：toolbar-kbd 300s 超时被杀 1 ＋ 8 项 [环境] 服务未起）→ **CDP 重启（Chrome 153）后 9 项逐一单独复跑全 PASS**（toolbar-kbd 68.6s 通过，实证全量阶段超时=环境退化非产品回归）。教训：155 脚本共享一个 CDP 实例，长跑尾部资源退化——登记观察项（smoke-ui 考虑 tab 清理/分组长跑）。
  - **search-fields-ui-smoke＝种子漂移**：智能层 77be4e3（15:16）给 devShim 加 demo-multiline（双线书）与 demo-order（潮汐的岔路），项目数 3→5；脚本 ③④⑤ 硬编码 `cards===3` → 断言假失败（功能本身正常，② 过滤 1 卡/⑥ 素材库搜索均过）。**本轮修复**：断言数据驱动化（baseCards 取初始态，全仓复查无同类硬编码卡片数残留——仅查询特异性 `cards===1` 保留）。修复后单独复跑全 PASS。
  - **6 项＝既有登记一一对应（零新增产品回归）**：chapter-prefill P11 headingAttr（→创作层/体验层）、fixed-command ⑦ /巡查 参数提示（→智能层）、presence-ref「关联档案：人物/沈藏.md」（→智能层+体验层）、sidebar-v01 留白断言（→体验层）、tool-chain/tool-detail 续读链环境超时（04:30 轮已注明本宿主环境发送链路超时，非回归）。
- **收敛**：较第三次（FAIL 6 全登记）零新增产品失败；窗口内唯一新出现（search-fields）已当轮修复并复证。**基线状态＝155 脚本、149 有效 PASS（全量 139 ＋ CDP 退化 9 单独复跑证实 ＋ search-fields 修复后复证）、6 项全部有主**。
- 完整日志 `/tmp/smoke-all-20260916-1630.log`；档案归属 `docs/模块推进/03-平台层.md`（16:5x 轮）。

## 十、第五次全量实跑（2026-09-16 20:14–20:26 平台层轮，候选 1 收口：CDP tab 治理接入）

```
PASS 151 · SKIP 0 · FAIL 6 · 共 157（725.2s ≈ 12.1min）
CDP tab 治理：起点回收 0 · 跑后清理累计 141 · 观测行 87（尾态 31 个织卷页，阈值 40）
（另：模型类门禁跳过 12 个——--live/单独跑；模型类名单审计健康）
```

- **本轮改动**（commit 见档案）：① `scripts/smoke-ui.mjs` 接入 **CDP tab 治理**——长跑中每个使用 9224 的脚本跑后关闭其新增 page target（官方 `PUT /json/close/<id>` 端点）、织卷本地页超阈值起点清理、逐脚本观测行（前后 tab 数/清理数）；调研依据 codeables/16yun/chrome-devtools-mcp #1921（未关闭 tab 累积 renderer 进程 → CDP 退化）。② `scripts/acts-adopt-ui-smoke.mjs` 偶发根因修复（三连：路由过渡旧 DOM 判据污染 / find→click 非原子 / hydration 竞态「幽灵按钮」+ `.ProseMirror` 存在≠内容就绪）——15 连跑 PASS。
- **与第四次（16:30）关系**：脚本集 155→157（+tool-cancel-ui 等 2 个）；**9 项 CDP 退化全部转 PASS**（tab 治理生效，尾部 [148-157/157] 全程无失联：toolbar-kbd 等前轮失联项本轮 PASS）；chapter-prefill 转 PASS（创作层 238fde5 18:45 修复）；**acts-adopt 偶发根治转 PASS**（16:30 轮登记偶发 #1）。
- **FAIL 6 全部归因（零新增产品回归）**：4 既有登记＝fixed-command ⑦ /巡查 提示（→智能层）、presence-ref refFile（→智能层+体验层）、sidebar-v01 留白断言（→体验层）、**float-kbd 「Esc 关浮层」TIMEOUT＝CDP 协议面挂死波及**（Chrome 主进程活但 9224 不再响应；`agent_browser.sh stop/start` 后 3/3 PASS，非产品回归——见下观察项）；2 既有环境超时＝tool-chain/tool-detail 续读链（04:30 轮已注明，非回归）。
- **新观察项（下一轮候选）**：**CDP 协议面挂死**——tab 治理保持 tab 恒定（全程 157 无失联、汇总尾态查询成功），但全量结束约 2–3 分钟后 9224 协议面死（进程活、DevTools HTTP 不响应；16:30 轮「148/155 后失联」与 9-13 22:30「9 天钙化」同型可能同根因＝高频 target 创建/关闭后的内部泄漏，非单纯 tab 累积）。处置方向＝smoke-ui 长跑加 CDP 活性看护（每 N 脚本探测 /json/version，失效自动 agent_browser 重启续跑或分组长跑）。
- 完整日志 `/tmp/smoke-all-20260916-2014.log`；档案归属 `docs/模块推进/03-平台层.md`（19:3x–20:4x 轮）。

## 十一、第六次全量实跑（2026-09-16 22:45–22:57 平台层轮，候选 1 收口：CDP 协议面挂死看护接入）

```
PASS 152 · SKIP 0 · FAIL 7 · 共 159（770.2s ≈ 12.8min）
CDP tab 治理：起点回收 0 · 跑后清理累计 139 · 观测行 88（尾态 1 个织卷页，阈值 40）
（另：模型类门禁跳过 12 个——--live/单独跑；🩺 CDP 协议面看护统计未打印＝全程零触发＝CDP 全程健康）
```

- **本轮改动**（提交见档案）：① `scripts/smoke-ui.mjs` 接入 **CDP 协议面看护**（观察项 ㉜ 收口）——「活性探测＋自动重启＋有界重试」三件套：每 CDP_WATCH_EVERY=10 脚本主动 ping `/json/version`（挂了立即 agent_browser stop+start 恢复）；useCdp 脚本预检发现「CDP 未起」先自动重启一次再重预检（不再直接标环境 FAIL）；脚本失败且协议面已死→自动重启＋重跑 1 次（有界，Playwright test-retries 先例；重试通过计 PASS 并统计）；重启就绪轮询 ≤60s，重启失败才按环境 FAIL 计。② `icon-size/loading-indicator` 冒烟脚本「复用已开宿主页」模式全量偶发修复——list.find 找不到 8123 页即 NO PAGE → 加「无宿主页则 `/json/new` 自开」兜底（脚本自身会 navigate 目标路由）。
- **看护三路径注入实测**（22:5x）：场景 A 预检恢复（stop agent_browser 后跑 states-smoke → 看护自动重启 1.1s＋重预检 → PASS 6.7s，修前＝环境 FAIL）；场景 B 失败重试（哨兵脚本首轮 stop CDP＋确认协议死＋exit 1 → 看护重启＋重试 1 次 → PASS，汇总「自动重启 1 次 · 重试后通过 1 个」）；场景 C 主动探测（哨兵 v2 正常 exit 0 但停掉 CDP → 下一脚本前主动探测发现并重启 → 后续全 PASS；临时 CDP_WATCH_EVERY=1 探测，验后复原）。
- **与第五次（20:14）关系**：脚本集 157→159（+zz 哨兵验证期无、实际 +2）；**11 项前轮失败中 9 项转 PASS**（float-kbd 协议面挂死波及、chapter-prefill、acts-adopt、5 项 tab 治理受益项等）；**FAIL 7 全部归因（零新增未修产品回归）**：5 既有登记——fixed-command ⑦ /巡查 提示（→智能层）、presence-ref refFile（→智能层+体验层）、sidebar-v01 留白断言（→体验层）、tool-chain/tool-detail 续读链环境超时（**本轮协议面全程健康故不触发看护重试＝设计正确**，区别于第五轮挂死波及）；**2 项本轮新发现并已修**——icon-size/loading-indicator 宿主页缺失偶发（兜底后单独复跑 7/7·13/13 PASS）。
- **看护有效性对比**：第五轮全量后 2-3 分钟协议面挂死（float-kbd 波及 TIMEOUT）＝看护待命未开火；本轮全程零触发（看护统计行未打印）＝159 脚本全程健康，且若再现挂死会自动消化（场景 A/B/C 已注入实证）。
- 完整日志 `/tmp/smoke-all-20260916-2245.log`；档案归属 `docs/模块推进/03-平台层.md`（22:30 轮）。

## 十二、第七次全量实跑（2026-09-17 01:50–02:03 平台层轮，候选 2 核销+守卫接入）

```
PASS 155 · SKIP 0 · FAIL 5 · 共 160（785.4s ≈ 13.1min）
CDP tab 治理：跑后清理累计 141+ · 观测（尾态正常）
（另：模型类门禁跳过 12 个；模型类名单审计健康；数据层 userData 契约审计健康）
```

- **本轮改动**（提交见档案）：`scripts/smoke-userdata-check.mjs` 新增（数据层 userData 环境契约守卫：无 ZJ_USERDATA／固定目录无启动清空→硬缺口；mkdtemp→干净；`--strict` 门禁化；`--selfcheck` 5/5）+ 接入 `scripts/smoke-ui.mjs` `--all` 自动审计（只提示不阻断，同 07:30 模型审计先例）+ `scripts/model-scripts.mjs` REVIEWED_NON_MODEL 登记（交叉误报）。
- **与第六次（22:45）关系**：脚本集 159→160（守卫接入不改集合：守卫自身与 smoke-ui 均排除；+1 为收集面变化无关——实际本轮 `--all` 收集 160 个）；**FAIL 5 全部既有登记、零新增未修产品回归**——fixed-command ⑦ /巡查 提示（→智能层）、presence-ref refFile（→智能层+体验层）、sidebar-v01 留白断言（→体验层）、tool-chain/tool-detail 续读链环境超时（本宿主环境发送链路超时，非回归）；**两审计全程健康（模型类 + userData）**。
- 完整日志 `/tmp/smoke-all-20260917-0150.log`；档案归属 `docs/模块推进/03-平台层.md`（01:30 轮）。

## 十三、第八次全量实跑（2026-09-17 07:34–07:49 平台层轮，候选 4 基线刷新）

```
PASS 157 · SKIP 0 · FAIL 5 · 共 162（858.5s ≈ 14.3min）
CDP tab 治理：跑后清理累计 141+ · 观测（尾态正常）
（另：模型类门禁跳过 12 个；模型类名单审计健康；数据层 userData 契约审计健康）
```

- **与第七次（01:50）关系**：第七次 FAIL 5（fixed-command ⑦/presence-ref refFile/sidebar-v01 断言/tool-chain·tool-detail 续读链环境超时）**本轮全部 PASS**——04:30 轮 d567468 三修（waitAgentIdle+第05章别名种子+断言新口径）零全量回归实证；tool-chain/tool-detail 本轮环境超时未再现。脚本集 160→162（收集面变化，与本轮修复无关）。
- **FAIL 5 全部归因（零产品回归）**：
  1. `devshim-parity-ui-smoke` listSlices count 5→6：**断言过期**——demo-aseya 种子 4→5 章（第05章_灯下＝04:30 轮 presence-ref 别名种子，带切片「第五幕_灯下」）；真机/mock 口径均正确。**本轮修断言（6）＋注释，单跑 11/11 PASS**。
  2. `copy-voice-ui-smoke` ②③ 预填断言：**断言过期**——「预填上一章」从第04章变第05章（多章连写预填口径正确取章号最大章）；期望值过时。**本轮修断言（阿七／第五幕_灯下），单跑 ALL PASS**。
  3. `dropdown-menu-ui-smoke` C3：**体验层 dff13d7（09-17 02:15）More 菜单补键位提示**引入后菜单项 textContent 含「撤销⌘Z」，断言 `i.t==='撤销'` 精确匹配失配（filter 未排除含快捷键项，置灰项混入 others）；UI 功能正常。**本轮修断言（startsWith），单跑 ALL PASS**。跨模块登记：体验层 f74b927 同源改动只修了 press 项断言，未同步本脚本——后续体验层改菜单文案须扫此脚本。
  4. `annotations-ui-smoke`（TIMEOUT 正文载入 90.3s）/ 5. `chapter-stale-ui-smoke`（TIMEOUT 索引重建空态 118.4s）：**全量环境退化型**——单独复跑 10/10、ALL PASS，非回归，不修。
- **审计全程健康**（模型类名单 + userData 契约）。
- **修复后全量复跑（08:0x）：PASS 162 · SKIP 0 · FAIL 0 · 共 162（692.6s ≈ 11.5min）——历史首次全量零失败**；CDP tab 治理起点回收 49 · 跑后清理累计 143 · 尾态 1 织卷页（阈值 40）；两审计健康。**第八次基线终态＝162 全绿**（周五 20:00 周增量验收「冒烟齐」口径满足）。
- 完整日志 `/tmp/smoke-all-20260917-0734.log`（首跑）/`/tmp/smoke-all-20260917-recheck.log`（修复后）；档案归属 `docs/模块推进/03-平台层.md`（07:30 轮）。

## 十四、第九次全量实跑（2026-09-17 13:35–13:52 平台层轮，候选 4 门禁预检）

首跑：

```
PASS 164 · SKIP 0 · FAIL 2 · 共 166（1024.4s ≈ 17.1min）
CDP tab 治理：起点回收 41 · 跑后清理累计 145 · 观测行 94（尾态 1 个织卷页，阈值 40）
（另：模型类门禁跳过 12 个；模型类名单审计 ⚠️ 疑似漏网 1（已人工核对登记，见下③）；数据层 userData 契约审计健康 55）
```

- **与第八次（08:0x 终态 162 全绿）关系**：脚本集 162→166（+multiline-outline-director 等 4 个）；第八次全绿项全部保持，**FAIL 2 均为测试资产/守卫类问题（零产品回归）**，逐项归因并修复：
  1. `health-bar-ui-smoke`「NO NOVEL PAGE」＝**㉝ 宿主页复用坑余留**（22:30 a07596f 兜底只加给 icon-size/loading-indicator，health-bar 同模式漏网；全量 tab 治理后无 8123 novel 页即 NO PAGE）。**修**：同款「任意 8123 页或 /json/new 自开」兜底，并落实候选 4 评估项——**+4 断言**（抽屉 Tab 栏 缺段/切片/混用、让 agent 改 存在、缺段 Tab 可点、切后标题=正文缺段核查；F-20260916-05 迁移完整性自动化，㉞ 教训资产化）；单跑 **10/10 PASS**。
  2. `toast-ui-smoke`「导演成功 toast TIMEOUT」＝**f535050 断链**（体验层 11:59 大纲顶栏按钮规范 F-20260917-01：7 文字按钮→2 文字＋5 icon-only；脚本仍按 innerText 点击 导演本章/回建缺失→NOT_FOUND→静默→无 toast；f535050 只适配 4 个相关冒烟，toast-ui 漏网——全仓 grep 后无其它按文本点击大纲顶栏的脚本）。**修**：改 aria-label 点击（导演本章=精确、回建缺失=前缀）；单跑 **SMOKE PASS**。
  3. 模型类审计报 `multiline-outline-director-smoke`（runDirector L29 强特征）＝**守卫正常工作**：创作层 09:45（29e4355）新增数据层冒烟（bundle outline/director 真实现＋`scripts/drive-mock-outline.mjs` 驱动 mock，头注明示「不依赖模型」；runDirector 只是 esbuild stdin export 行）。人工核对调用面后登记 **REVIEWED_NON_MODEL（12/8）**，审计归健康。
- **复跑（14:0x，766.1s ≈ 12.8min）＝PASS 166 · SKIP 0 · FAIL 0 · 共 166——历史第二次零失败、166 规模首次全绿**；两审计全程健康；CDP tab 治理起点回收 0 · 清理累计 143 · 尾态 2。
- **观察增补 ㉟**：功能点收敛类 UI 改动（icon-only/菜单化）后必须 grep `scripts/` 按文本点击的断言并同步——toast-ui 断链即本期实证（经验层 f535050 动了 UI、冒烟脚本未同步）；全仓扫描落实立为平台层下一轮候选 4。
- 完整日志 `/tmp/smoke-all-20260917-1335.log`（首跑）/`/tmp/smoke-all-20260917-recheck.log`（复跑）；档案归属 `docs/模块推进/03-平台层.md`（13:30 轮）。
