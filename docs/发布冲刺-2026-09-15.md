# 织卷 · 发布冲刺（主人 2026-09-12 定：9-15 达到可用 + 双端打包）

> 目标：**2026-09-15 前**织卷达到「可用」程度，并产出 **macOS 安装包**（**Windows 包：主人 2026-09-12 拍板推迟到后续**，路径另议）。
> 里程碑口径：核心写作闭环（建项目→写正文→agent 协作→切片/提案→导出一章）无阻塞地走通，且首装可用（**首次启动模型/引擎引导页**——主人 9-2 拍板两日内补）。

## 一、mac 打包（主会话负责，进行中）

- ✅ electron-builder 24.13.3 已装；`electron-builder.yml` 已建（mac dmg；dsh-runtime 作 extraResources 拷出 asar）
- ✅ runtime.ts 打包路径适配（app.isPackaged → resourcesPath/dsh-runtime）
- ✅ **引擎宿主修复**：打包机无系统 node 时，兜底 Electron 自身当 node（ELECTRON_RUN_AS_NODE 注入，规避「Electron 当 node 吞输出致引擎握手超时」的旧坑，2026-09-12 冲刺项，真机实测待验收）
- ✅ **首版 mac arm64 dmg 成功**（168MB，dsh-runtime 入包验证通过）；第二轮（含引擎宿主修复）构建中
- 🔲 打包产物真机冒烟：release/.app 启动 → 引擎在线（resources/dsh-runtime 被拉起）→ demo 项目闭环
- 🔲 图标（build/icon.icns）：icon.html 草稿（青黛+宋体「织」），待渲染管线（列入体验层/主会话后续）
- 🔲 mac x64（Intel）包：本机 arm64 可交叉打（--x64），规划在冲刺后期出双 arch

## 二、可用性缺口（四模块推进器按轮次消化；9-12 起冲刺节奏）

| 项 | 归属 | 状态 |
|---|---|---|
| 采集管道 cron 恢复（76de7e949a3b 失联，恢复处方在 03-平台层.md 第17轮） | 平台层 | 待恢复 |
| **首启引擎引导页**（主人拍板两日内补：首次启动/引擎离线时的模型与引擎连接引导） | 智能层+体验层 | 待做（先出口径：何时触发/内容/跳过；复用 ProjectGuide 模式） |
| 系统菜单（口径 docs/系统菜单-设计口径.md 已出） | 平台层 B | 待落地 |
| 已知 flaky：chapter-stale-ui-smoke 1 项失败（时序类） | 体验层 | 待修（低优先） |
| Windows 兼容清单 | 平台层 | **win 延后**，清单照常走查（窗口/快捷键/路径已核；spawn 宿主已修） |

## 三、验收（9-15）

1. `release/zhijuan-*-mac-*.dmg` 存在且可运行（真机 .app + 引擎在线）
2. 打包版走通写作闭环：建项目 → 写一章 → agent 续写（引擎在线）→ 切片提案
3. 冒烟全集回归（无回归 > 2 处并已登记）

## 四、决策记录

- 打包不签名（identity:null）：自用/局域网分发允许 Gatekeeper 右键打开；对外分发再补签名+公证。
- **Windows 包：2026-09-12 主人拍板推迟**（后续路径：本机 wine 或 GitHub Actions，届时再定）。
- **首启引擎引导页：主人拍板两日内补**（2026-09-12）。
