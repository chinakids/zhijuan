# 2026-W38 周增量 · 多时间线叙事 · 模型层贯通（第一阶段）

> 机制：docs/周交付窗口-机制-2026-09-16.md；本周=2026-09-14~09-20（本计划 09-16 起生效，首期验收窗口=**2026-09-18（周五）20:00**，时间紧顺延 09-19 周六补验）。
> 设计权威源：docs/多时间线叙事-设计文档-2026-09-16.md（F-20260916-02）。

## 一、范围（做什么）

| # | 项 | 归属模块 |
|---|---|---|
| 1 | 章头 `时间线` 字段解析＋SliceEntry.line＋listSliceEntries 透传＋线枚举 `.zhijuan/lines.json`（可重建，与 slices.json 同构） | 平台层 |
| 2 | 上下文装配：**「上一章尾部」= 同线内前驱**（按章号）；装配块注明本章线/同线前章 | 创作层 |
| 3 | 建章向导/多章连写：新增「时间线」选择（预填上一章线，可新建） | 创作层 |
| 4 | 审计：切片顺序/同名检查改**线内**判定；新增「跨线切片名重复」提示 | 智能层 |
| 5 | 时间线页：按线分组＋筛选；章节列表章项线徽标（单线项目零打扰） | 体验层 |
| 6 | devShim 同口径加 line；新增多线冒烟（两线交错项目：线内序/状态分叉/视图分组）；单测；project.md 模板时间线总纲段改多线示例 | 各层 |

## 二、不做什么（本周不做）

- 一章多线、线合并/拆分/重命名工具、线元信息 UI 编辑、跨线状态迁移工具（设计文档 §4.6，均二期）。
- 真机走查若因锁屏不可验收：登记待办，滚动入下周窗口，**不**以静态验证冒充验收。

## 三、验收口径（全部通过=本周交付完成）

1. `npm run typecheck && npm run build && npm test` 三道门全绿。
2. 新增多线冒烟脚本全过（含：两线交错项目的线内序正确、人物状态分线不串、时间线页可按线分组查看、老项目零迁移仍单线正常）。
3. 无头截图两张（时间线页分组视图 + 章节列表线徽标），存 `~/Pictures/zhijuan/`。
4. 周交付汇报发织卷反馈群（含提交号/冒烟结果/截图/未完成项）。

## 四、进度记录

- 2026-09-16：设计文档落档（docs/多时间线叙事-设计文档-2026-09-16.md）；本计划建立。推进开始。
- 2026-09-16 12:30：**智能层任务 #4 完成**（提交 49558e4）——切片时序核查/档案切片核查改**线内**判定（多线交错不再误报）+ 新增「跨线切片名重复」提示 R7（low，列线名与章号）；时间线提取权威源 `shared/line.ts`（平台层透传时复用）；三道门 788 例 + 数据层冒烟 multiline-audit-smoke.mjs 12/12 + order-ui-smoke 回归全过。
- 2026-09-16 13:10：**创作层任务 #2 完成**（提交见 02-创作层.md 当天日志）——上下文装配「上一章尾部」改**同线前驱**（shared/line.linePredecessor 纯函数，单线老项目退化为全局按章号=零回归）；多线项目装配块注明「本章时间线＋同线前章=第N章，非全局上一章」（单线零打扰）；世界切片 §6.5 回退链（本切片无设定→上一切片）随线内前驱自动按线取；数据层冒烟 multiline-context-smoke.mjs 14/14（双线交错：过去线第4章前驱=过去线第2章而非主线第3章；回退取幕0B 不串幕1C；单线零回归）；三道门 795 例全绿。
- 2026-09-16 14:2x：**平台层任务 #1 完成**（提交见 03-平台层.md 当天日志，主题「多时间线模型层贯通」）——SliceEntry.line 透传（shared/types + shared/slices.listSliceEntries，章头「时间线」缺省=主线；老项目零迁移）+ 线枚举 shared/line.ts 新增 LineInfo/listLinesFromEntries（出现序+章数，正文为源）+ main/slices.ts 新增 listLines/writeLinesRegistry（.zhijuan/lines.json 与 slices.json 同构、可重建）；slices:list IPC 自动带 line（已存在）；project.md 模板「时间线总纲」段改多线可列举示例（人读说明，不承担数据职责）；devShim.listSlices 共用 shared 实现自动同口径（零改）；单测 +7 例、数据层冒烟 scripts/multiline-slices-smoke.mjs 11/11（双线交错线内序 [1,3,5]/[2,4]、全局排序不变、lines.json 落盘回读、老项目全缺省=单线零回归）；三道门 typecheck/build/79 文件 800 例全绿。
- 2026-09-16 14:5x：**体验层任务 #5 完成**（3f2eaf8）——时间线页按线分组+筛选（chips「全部/线名·N章」、每线独立时间序、单线项目零打扰零回归）；章节列表章项线徽标（多线项目显示、单线不显示）；devShim 新增多线演示项目 demo-multiline（双线书：5 章交错，1主/2过/3主/4过/5主）；无头冒烟 scripts/multiline-view-ui-smoke.mjs 12/12（分组/线内序/筛选/徽标/单线零回归/零 JS 异常）+ 截图 timeline-multiline-1445.png / novel-line-badges-1445.png；三道门 79 文件 800 例全绿。
- 2026-09-16 15:45：**创作层任务 #3 完成**（0181b8d）——建章向导/多章连写新增「时间线」选择：新建章节对话框新增时间线输入（预填上一章线，chapterLine 归一口径；可手输新线）+ 已有线快捷 chips（线数>1 才显示、点击填入、值匹配高亮）；创建时线=空或主线**不写**约定头（缺省=主线零迁移零冗余），非主线写 `时间线: <名>`；接线 lines:list IPC（复用平台层 main/slices.listLines 现扫章头，正文为源）+ preload listLines + devShim 同口径（shared 单一权威源）；新冒烟 scripts/createchapter-line-ui-smoke.mjs 13/13（多线：预填主线/不写字段/手输新线写入/chip 选线写入/重开预填跟随；单线：零 chips 零写入回归）+ 截图 create-line-multiline-1602.png / create-line-chips-1602.png；chapter-prefill 回归 P1–P10 过（P11 撞既有 MilkdownError「headingAttr」，git stash 对比 HEAD 同错=非本轮引入，已登记创作层候选 1）；三道门（typecheck/build/800 例）全绿。
- 2026-09-16 16:0x：**周增量任务 #1–#5 全部落地**；#6（devShim 同口径/多线冒烟/单测/project.md 模板）随各层完成。周五 20:00 验收口径待主会话执行（三道门绿、冒烟齐、截图齐，真机走查项若仍锁屏则按机制登记顺延）。
- 2026-09-16 17:0x：**平台层全量冒烟基线刷新（第四次）**——`smoke-ui --all` 155 脚本实跑：多线四冒烟（audit/context/slices/view）全 PASS、三道门 800 例绿；16 项失败全部归因（9 CDP 长跑退化已单独复跑证实、【search-fields 种子漂移已当轮修复：devShim 项目 3→5 致硬编码 cards===3 假失败→断言数据驱动化】、6 项既有登记）。基线文档 §九 已记；平台侧验收口径「冒烟齐」满足。
- 2026-09-16 20:4x：**平台层全量冒烟基线刷新（第五次）**——`smoke-ui --all` 157 脚本实跑 PASS 151 · FAIL 6（全有主：4 既有登记+float-kbd 环境波及已复跑证实+2 既有环境超时）；**CDP tab 治理接入**（跑后清理 141 tab、全程无失联）；acts-adopt 偶发根治（15/15）。基线文档 §十 已记；平台侧验收口径「冒烟齐」保持满足。
