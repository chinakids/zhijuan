// ===== 织卷 · 工作区落档文档（在 文档/ 下初始化写入） =====
// 面向使用者的说明与约定；写入前若已存在则保留（只落在空档），改文档请原处编辑。

const GUIDE = `# 织卷 · 使用说明

织卷是给写长篇的作者准备的创作工作台：正文是源，设定跟着正文走，写作 agent 在右侧随时待命。

## 快速开始

1. **新建项目**（首页右上角）后先走引导：填世界观 → 列主要人物 → 完成。之后可以随时在世界观 / 人物页面补。
2. **新建章节**：在正文页点「+」，先填本章故事要素（目标 / 冲突 / 关键事件 / 前情），章节就按要素初始化好。
3. **写正文时**：右侧 agent 区可以边聊边生成；选中一段再点「引用选中」，可让 agent 只对着那一段改。
4. **保存正文**（⌘S）会触发“切片同步”：模型读完本章后，把发生变化的“人物现时状态 / 世界观变化”写成**提案**，你确认后再应用到设定文件。

## 各模块

- **正文创作**：一章一个文件（\`正文/第NN章_题名.md\`），每章是一个“时间切片”。章首约定头是关键，切片同步靠它定位。
- **人物设定**：每个人物一个档案（\`人物/<姓名>.md\`），含基础档案和“成长轨迹”；正文保存后新状态会作为提案补进来。
- **世界观设定**：\`世界观/总纲.md\` 是总纲；每个切片另建 \`世界观/切片_<切片名>.md\`。
- **素材库**：按类别放素材文档；想采集素材时用“发起采集”，后台会制成素材卡（见“采集”）。
- **设置**：大模型端点、写作引擎（是否用工具）、常用工具开关、主题、工作区路径都在这。

## 写作引擎（后台）

- **写作引擎**是背后负责写作的模型会话（独立进程）。它能在对话里：读章节 / 读设定 / 列任务清单（todo 卡）/ 需要选择时停下来问你（提问卡，你看选项或自由回答，它接着写）。
- 引擎的端点、模型、以及两个常用工具的开关都在设置里；改完保存，下次对话生效。
- todo 卡和提问卡都只是对话里的展示，不进正文；你不用管它们，想停掉就在设置里关开关。

## 采集

- 在“素材库”页“发起采集”：填需求 → 生成任务卡并进入采集池；本机管道会在后台按要求采素材回填成素材卡。采集来自公开网络，注意保留来源。

## 数据都在本地

- 你的正文、设定、素材、提案都明文存在工作区里的项目库对应目录下，纯 Markdown，好备份好迁移。
`

const CONVENTIONS = `# 织卷 · 目录与约定

这套约定是“正文为源，设定为流”的基础。正文是唯一的内容源头，设定文件随着章节推进被更新。

## 工作区结构

- \`文档/\`：说明与约定（本文件）
- \`项目库/\`：所有项目的家；每个项目一个独立目录

## 项目内结构

\`\`\`
<项目>/
├─ project.md                 项目说明（front matter 含名称/简介）
├─ 正文/…                     每个章节一个文件：第NN章_题名.md
│   （章首 front matter：章号/题名/切片/涉及人物）
├─ 人物/<人物名>.md           每个人物一个档案
├─ 世界观/
│   ├─ 总纲.md                世界观总纲
│   └─ 切片_<切片名>.md       每个时间切片的世界观状态
├─ 素材库/…（含 采集池/）     素材与采集任务
└─ .zhijuan/                  工具数据（提案、会话记录），不作设定本体
\`\`\`

## 章节约定头（front matter）

\`\`\`yaml
---
章号: 1
题名: 夏夜的信
切片: 第一幕_夏夜
涉及人物: [林晚, 顾知远]
---
\`\`\`

- **切片**是时间切片名，正文保存时要写进切片的设定文件，必须唯一且有意义。
- **涉及人物**是个列表；“切片同步”会把他们的现时状态作为提案补进人物档案。

## 保存与切片同步

保存正文后发生的流程：

1. 写作引擎读这一章 + 相关人物档案 + 当前切片设定；
2. 判断本章带来了哪些“人物状态变化 / 世界观变化”；
3. 生成为**提案**（含原状态要点、新状态全文、理由），在顶栏待确认；
4. 你确认后，写入对应设定文件的小节（按标题定位，新增则追加在末尾）。

提案是对设定的**改写建议**，正文永远是源；不同意就驳回，什么都不损失。
`

export const WORKSPACE_DOCS: Record<string, string> = {
  '使用说明.md': GUIDE,
  '约定与结构.md': CONVENTIONS
}
